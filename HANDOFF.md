# 再開用メモ（2026-09-19 時点）

最新コミット: `babae71`（リモート main と一致）

`work-record.md` に各修正の詳細と**却下した案の理由**を全て記録してある。
このファイルは「次に何をするか」だけを書く。

---

## 1. 判断待ち（ユーザーの指示が要る）

### A. 跳んでも結果が変わらない
実測（リリース時・2.5m以内の守備者、14試合141本）
```
2.5m以内に守備が居た: 91.5%
  うち 空中に居た      : 17.8%   成功率 60.9%
  うち 跳べる状態だった : 72.9%   成功率 59.6%
```
位置取りと反応は問題ない。`move/reaction/contest-block.ts` の `bestBlocker` で
空中の守備者に足されるのは `+0.18 * close` だけで、**跳ぶことにほとんど意味がない**。
→ この加点を上げるか？ ⚠️ ブロック確率の上限 `clamp(p, 0, 0.85)` は
　 「今のバランスでOK」と指示済みなので**触らない**。

### B. リリース時のフリー3P 42%
間合いを詰めても変わらない。**フリーの3Pの 83.2% はキャッチ&シュート**で、
1.84m に付いていてもパスが渡って一歩踏み込まれればリリース時は 2.2m 超になる。
＝**クローズアウトの遅れ**であって、間合いの設計とは別問題。
→ 「パスが飛んでいる間に守備が反応して詰める」仕組みを作るか？
　 ⚠️ 間合いの係数いじりは**4回失敗している**。同じ方向へ行かないこと。

### C. 3Pの曲線の下限
`threeSkillTerm = 0.60 - 2.05 * (1 - L精度/100)^2`
L精度70で素 **41.5%** と高い。ミドルは指定の2点（95・2m=75% / 65・6m=25%）から
作り直したが、3Pは未調整。→ 目標水準の指示があれば同じ手順で作り直せる。

---

## 2. 未着手（指示済みだが手を付けていない）

### 縦パスを消す
「守備がマークしているときにパスコースを消す」。`laneVetoed` でコースに守備が
居れば投げない仕組みは**存在する**が、効いているか未計測。
⚠️ マーク外し（`tryShake`）の能力差分岐とスタミナ消費は実装済み。

---

## 3. 未解決（原因は特定済み、対処が未確定）

### 深い3P
全3Pの 62% がラインから1.2m超。**打っているのは全員オフボール**（ハンドラー0%）で、
**持ち場はリムから7.25mと正しい**のに実際に打つ位置は 0.7m 以上外。
原因は持ち場周りの**周回**（`freePhase`、半径0.7〜1.6m）。
「待機中にラインの**内側**へ 23% 入る」という既知の問題と**同じ動き**。
さらに L精度85以上は 2.50m 外から打っており、`effShootRange`（L速度で射程が決まる）が
**「打てる限界」ではなく「打つ位置」の判断に使われている**。

対処の候補（どちらも未実施）
1. 周回の半径をラインからの余裕に応じて縮める（内側への食い込みにも同時に効く）
2. `effShootRange` を打つ位置の判断に使わない（成否の減衰にだけ使う）

⚠️ **3回失敗している**（距離の罰を射程で緩和 / 守備距離で緩和 / `stepToArc` で詰める）。
　 「打つかどうかの罰」では位置は直らない、というところまでは分かっている。

### コート幅の左右の偏り
左の帯 0.93人 / 右の帯 1.42人 / どちらかが0人 48.8%。
⚠️ ポストの左右固定が原因という仮説は**棄却済み**（反転させたら48試合で悪化し、
　 偏りも直らなかった）。原因は未特定。

---

## 4. この案件で繰り返した失敗（同じ轍を踏まないこと）

1. **12試合の `probe-box` で ±2.5点の差を根拠にした** → カオスに支配されている。
   判断には **48試合以上**（種8通り×6試合）か、サンプル数の多い機構レベルの指標を使う。
2. **平均で見て中央値で見なかった** → カット通過の外れ値で 1.18m が 4.71m に化けた。
3. **プローブが `game.shooter` の切り替わり（構え始め）で結果を読んでいた**
   → `shotPoints`/`shotMade` はリリース時に決まるので1本前の値を拾う。
   `ballMode` が "shot" になったフレームで読むこと。**3Pの本数を半分取りこぼしていた。**
4. **重みを既存の項の幅と比べずに足した** → `bestOpenSpot` の `open * 1.6` は 0〜16 で、
   追加した重み(3.0〜6.6)が押し流されていた。
5. **連続値で解こうとした** → マンマークは「付くか/放置してヘルプか」の二択が正しい。
6. **団子の指標にスクリーン中・カット中を含めた** → 実際の団子率は50%ではなく16%だった。

## 5. 計測の道具

- `sh /tmp/runprobe.sh <probe名>` で低優先度ビルド＋実行
- `probe-box` … 総合（`SEEDS` 環境変数で種を複数。既定3通り）
- `probe-space` … 3Pの角度/距離/守備距離、スペーシング、フリーの原因の切り分け
- `probe-rim` … ゴール下（守備距離別、高さの差別、跳べているか）
- `probe-def` … マンマークとゾーンの間合いを同じ試合で比較
- `probe-midcalc` … 試合を回さず確率式を直接叩く（本数のブレが無い）
- `probe-cpos` / `probe-corner8` … 役割別の位置、持ち場ごとの就いている選手
