# workPlan.md — basketball-simulation

## Project Goal

Babylon.js 製フルコート 5対5 の**バスケットボールゲーム**。
試合そのものは AI が進める（観戦）が、プレイヤーは**クオーター毎のポーカー**でチームを強化する。
1on1・リバウンド・パスカットの勝敗は「選手能力 × 特殊能力 × 局所的な物理」で決まり、
**なぜその結果になったかが見て分かる**こと。スマホでも快適に動くこと。

前身 `developsecond/basketball-sim`（commit 76c9eca）から本体を移植して出発する。
移植元は「操作なしの観戦シミュレーター」だった。本作はそこに**プレイヤーの意思決定
（ポーカー）**と**局所物理**を足してゲームにする。

## 移植元から引き継ぐもの / 変えるもの

| | 移植元 basketball-sim | 本作 basketball-simulation |
| --- | --- | --- |
| 試合の中身 | 5v5 決定論シミュレーション | そのまま引き継ぐ |
| 選手データ | 25能力値 + 特殊能力21種 + DB 4015人 | そのまま引き継ぐ |
| 操作 | なし（観戦専用） | **ポーカーのみ操作**（試合中は観戦） |
| 物理 | 剛体物理ゼロ（「変更しないこと」と明記） | **判定の瞬間だけ局所物理**（下記4を参照） |
| 対戦相手 | CPU 対 CPU 固定 | CPU対CPU / CPU対ユーザー を切替 |

⚠️ 「物理エンジン不使用」は移植元 workPlan の大前提だった。本作はこれを
**「常時の剛体シミュレーションはしない。判定が発生する一瞬だけ、その数人とボールに限って
物理を解く」**へ改める（要望3の「完全に物理演算をするわけではないが」の解釈）。
ボールの通常軌道は従来どおり計算で飛ばす。

---

## 機能1 — ポーカーによるチーム強化

### ルール（確定分）
- 手札は**試合開始時に5枚**配られる。CPU対CPUでも同じ処理で回る。
- **交換はクオーター毎に1回**（Q1開始前 / Q2前 / Q3前 / Q4前 の計4回まで）。
- **役を確定させるタイミングはホームチームが決める**。確定した瞬間に両チームの手が公開され、
  役に応じた**チーム全体の強化**がその時点から試合終了まで乗る。
  - 早く確定 = 弱い役でも長く効く / 引っ張る = 強い役を狙えるが効く時間が短い、のトレードオフ。
  - 確定せずに Q4 の交換まで来たら、Q4 開始時に自動確定する。
- **捨てたカードは即座に選手個人を強化**する（確定を待たない）。捨てる枚数だけ強化が入るので、
  「役を狙う」と「個人を強化する」がトレードオフになる。

### 既定（ユーザー確認前の仮置き。調整可能にする）
- **両チームが手札を持つ**。アウェイ側は CPU が同じロジックで打つ。確定タイミングの決定権
  だけがホームの特権。
- **捨て札の宛先はプレイヤーが選ぶ**（CPU は自動）。1枚 = 1選手。
- 効果量は**補助的**に留める。大きな戦力差がひっくり返らないこと（下記の検証で担保する）。

### カード → 強化の対応
- **スートが能力の系統**を決める: ハート=オフェンス系 / スペード=ディフェンス系 /
  ダイヤ=シュート系 / クラブ=フィジカル系。
- **ランクが量と対象**を決める（A=最大, 絵札=特定能力へ集中, 数札=小さく広く）。
- 「選手の能力でどう強化されるかが決まる」= **同じカードでも選手によって効きが変わる**。
  その系統の適性が高い選手ほど大きく乗る（伸びしろ型ではなく、長所を伸ばす型）。

### 手番
- **相手が先に手を決める**（捨てる札と置き先まで）。その結果を見てからこちらが決める。
- **役を確定させるかどうかはホームが決める**。
- ⚠️ **将来: 監督（ヘッドコーチ）の能力で先攻後攻を決める**。当面は「相手が先」で固定。
  ヘッドコーチという概念自体が未実装なので、能力値・データの持ち方から設計が要る。

### CPU の判断
- 捨てる札: 標準的なドローポーカーの定石（ペア以上/4枚フラッシュ・ストレートドローを残す）。
- 置き先: 「自軍の伸び」と「相手の下げ幅」を同じ物差しで比べて大きい方（同点なら自軍）。対象は先発10人。
- ⚠️ **本格的な AI は将来の課題**（点差・残りクォーター・相手の役を読む等）。現状は上記の即物的な評価のみ。

### 今後の追加実装（ユーザー合意済み・未着手）
ポーカーは今後も足していく。判明している宿題:
- [ ] **監督（ヘッドコーチ）の能力で先攻後攻を決める**。ヘッドコーチの概念自体が未実装なので、
      能力値・データの持ち方から設計が要る。当面は「相手が先」で固定。
- [ ] **CPU の本格的な AI**。現状は即物的な評価（定石で残す＋その場の増減が大きい方へ置く）のみ。
      点差・残りクォーター・相手の役・自分が確定を急ぐべきかを読む判断は未実装。
- [ ] **強い役の手応え**。実測でロイヤル(+12.5pt)と平均的な役(+9.0pt)の差が小さい。
      上位役だけ引き上げると上限が60%へ近づくので、上げ幅は実測しながら決める。
- [ ] 演出の詰め（思考時間の長さ、札を伏せるか表にするか）は実機で触ってから。

### バランス目標（headless で実測して守る）
- 強化なし同士の勝率 50% に対し、**片側だけ最良運のポーカーを引いた場合の勝率を 60% 未満**に抑える。
- 戦力差のある対戦（OVR差 大）では、ポーカーで**勝敗が逆転する頻度が 10% 未満**。

---

## 機能2 — 1on1 の勝敗を能力・特殊能力で決める

- 現状はドライブ突破がジューク/リーンの連続処理で、**勝敗が「いつ決まったか」が見えない**。
  これを**1on1 を明示的な勝負として解決する**形に変える（仕掛け → 判定 → 決着モーション）。
- ムーブ追加: フェイント（シェイク/ヘジテーション）、ユーロステップ、スピン、ステップバック。
  どのムーブを選ぶかは能力＋特殊能力（スラッシャー/アイソレーション/ボールキープ等）で決まる。
- ダンク／ブロックの勝敗を明確化: 「ヘッド(dunk)」「ジャンプ」「バランス」「S威力」で
  ダンク成立 / ブロック / ファウル / チップアウト を**離散的な結末**に落とす。
- 移植元の資産: `ai/offense/onball/drive.ts`、`move/reaction/contest-block.ts`、
  `animation/action/*`、vendor の motion クリップ（dribbleCross/dribbleHesi/doubleClutch 等）。

## 機能3 — 判定の瞬間だけの局所物理

対象は3つ。いずれも**その場に居る数人＋ボールだけ**を短時間まわす。

1. **リバウンドの位置取り争い**: ボックスアウトを、質量・重心・押し合いの力積で解く。
   現状の「距離＋守備ボーナスの重み付き抽選」を置き換える。
2. **1on1 の接触**: 抜く/止めるの決着に、体重・進入速度・角度を入れる
   （中央メモリ `reference_contact_knockdown_balance` の「強さは接触力でなく近づく速さ」を適用）。
3. **インターセプト/パスカット**: ボールの実軌道と手の到達可能域の交差で判定する
   （現状は確率抽選）。届いたか届かなかったかが見て分かるようにする。

## 機能4 — 高速化とローディングの可視化

- **実測済みの出発点**: 移植直後のバンドルは **11.98MB（gzip 2.71MB, 単一チャンク）**。
  スマホの初回読み込みで最も効く改善余地はここ。
- やること: Babylon の import をサイドエフェクト単位へ絞る / コード分割 /
  選手DB（約 1.9MB のTS 3ファイル）の遅延読み込み / ボクセル選手メッシュの生成コスト削減。
- **ローディングの可視化**: 現状は「クラブ決定 → 26人ぶんのボクセル生成」等で無言で固まる箇所がある
  （`main.ts` のコメントに実際そう書かれている）。進行中であることを必ず画面に出す。
- 目標値は実測してから決める（初回読み込み ◯秒 / 実行中 ◯fps）。憶測で決めない。

---

---

## 機能5 — 選手モデルとモーションの作り直し

### 実測した現状（`vendor/objcts/player/voxel/data/body-*.json`、ボクセル1.5cm・基準身長1.81m）

| | 幅(X) | 厚み(Z) | 厚み/幅 | 人体のおよそ |
| --- | --- | --- | --- | --- |
| 胴 (normal) | 0.360 | 0.270 | **0.75** | 0.6〜0.7 |
| 腰 (normal) | 0.300 | 0.285 | **0.95** | 0.7 |
| 腰 (muscle) | 0.330 | 0.390 | **1.18**（幅より厚い） | 0.7 |

→ 「厚みが大きすぎる」はデータでも裏付けられる。とくに腰はほぼ円柱で、muscle は幅より厚い。

- **腕の骨**: 上腕 0.262m / 前腕 0.204m（肩→肘→手首の実距離）。**比は 1:0.78 で人体標準どおり**。
  ただし**腕全体が標準より約22%短い**（1.81mなら上腕0.337 / 前腕0.264 が標準）。
  ⚠️ つまり「前腕が長すぎ・上腕が短すぎ」は**骨の比率では説明できない**。原因はメッシュ側の
  見え方か、動かし方（肘の可動域・ラグ）の可能性がある。**直す前に実測して切り分ける**。
- **関節の継ぎ目**: 素体は**部位ごとの剛体メッシュ**をボーンノードへ直付けしている
  （`player-voxel.ts` のコメントにも「剛体パーツ同士」と明記）。一方**服はスキニング済み**
  （`matricesWeights` + `linkTransformNode`）。→ **スキニングの仕組みは既にある**ので、
  素体をそちらへ載せ替えれば継ぎ目は消せる。
- **IK/FK**: 共有ライブラリ `@objcts/player/ik.ts` / `fk.ts` は存在するが **src からは未使用**。
  src には独自の2ボーンIK `animation/action/reach-ik.ts` があるがリーチ限定で、通常の腕振りは
  `animation/basic/arms.ts` の直接回転＋イージング。
- ⚠️ **焼き直しツール `tools/build-voxel-parts.mjs` はこのPCのどこにも無い**
  （元の objcts リポジトリごと存在しない）。素材の .vox は `game-assets/vox/` `game-assets/bot/`
  にあるが、焼き直すにはツールを書き起こす必要がある。当面は**焼き済みJSONを変形する**方が現実的。

### 体格を「身長に対する体重」で決める（仕様変更）

現状の作り（実測）:
- 体型は `variantFor(balance)` で **3段階に離散化**（balance≤55=skinny / 56〜79=normal / ≥80=muscle）。
  つまり**見た目の体格を能力値のボディバランスが決めている**。
- 太さは `partStretch(variant, height, widthExponent…)` が解く。`widthExponent = 0.5` 固定で、
  **X（幅）と Z（厚み）に同じ係数が掛かる**（`e.x*(k-1)` / `e.z*(k-1)`）。
  → **厚みだけを独立に動かす仕組みは今は無い。ここを分けるのが変更の中心**。
- `DbPlayer` は `heightCm` を持つが**体重は無い**（`identity.ts`）。
- 元データ側: WE2010 の Excel には 2026-08-25 に体重列を追加済み（実測値4035件／推定492件）。
  → **再抽出すれば体重は入る**。

やること:
- [ ] W1: `identity.ts` に `weightKg` を追加（Excel から再抽出。既存の抽出手順に従う）。
- [ ] W2: **体格指標**を身長と体重から作る（BMI = kg/m² など）。バスケ選手の実在帯は BMI 22〜27 程度。
      これを 0..1 に正規化して「厚み係数」にする。
- [ ] W3: `widthExponent` を **X と Z に分離**し、Z を体格指標で駆動する。
      ベイク済みの3体型は「見本」として残し、その間を厚み方向の層数で埋める。
- [ ] W4: **厚みが乗る場所の配分表**を作る（部位別の重み）。腹・腰・胸・上腕には乗せ、
      前腕・すね・頭には乗せない、など。`PartStretch{x,y,z}` は既に部位別なので受け皿はある。
- [ ] W5: ボディバランス(balance)の役割を整理する。見た目を体重が決めるなら、balance は
      **接触の強さの能力値**に戻す。⚠️ ただし game 側は `holdWeight` 等で balance を使っている。
      体重を接触計算の質量として使うかは**機能3（接触物理）と一体で決める**。

### 素体と衣類を1つのメッシュにするか（検討結果）

⚠️ **前提の確認: 現状すでに二重にはなっていない**。`uncoveredVoxels()` が服に覆われた地肌の
ボクセルを落としている（しかも「隣接したら消す」だと袖口・襟に穴が開くので、「同じ高さ・
同じ方位で外側に服がある」＋「6方向のうち3方向以上・5セル以内に服がある」で判定する作り）。
つまり**ジオメトリの重複は無い**。

残っている問題は**メッシュが2系統に分かれていること**:
- 素体 = 部位ごとの**剛体メッシュ13個**（ボーンノードに直付け）
- 服 = **スキニング済みの別メッシュ**
- 加えて頭・髪 → 1人あたり 16 メッシュ前後。

- [ ] M5: **素体と衣類を1つのスキニングメッシュに統合する**（＝ユーザー提案の「衣類の箇所は
      衣類で Body を作る」）。M2（素体のスキニング化）と**同時にやる**。別々にやると二度手間。
      - 利点: 関節の継ぎ目が消える／素体と服の境目も消える／1人16メッシュ→1〜2メッシュで
        **描画コストが大きく下がる（機能4に直結）**。
      - 注意: キット色をメッシュに焼き込むと**着替えのたびに素体ごと作り直し**になる。
        現状も着替えで26人ぶんの服を作り直していて「ボタンの反応が数秒止まる」と
        `main.ts` に明記があるので、**統合前後で作り直しコストを実測してから決める**。

### 方針: 外部モデル（`Downloads/player-one`）を**ボクセル化して**現行モデルと差し替える

⚠️ ポリゴンのまま使うのではなく、**既存のボクセル化パイプラインに通して現行と同じ形式にする**。
これなら現行の仕組み（パレット役割による肌/髪/キットの塗り替え、身長のボクセル層挿入、
部位別の剛体メッシュ、モーションクリップ）が**そのまま使える**。

**必要な道具は全て揃っていることを確認した**:

| 工程 | 実体 | 出力 |
| --- | --- | --- |
| 1. モデル → ボクセル | `developsecond/voxel-pipeline`（config駆動・uv/typer/Blender） | `game-assets/vox-model/<name>/*.vox` + `grid.json` + `weights.json` + `skeleton` + **`*.morph.json`（体型スライダー）** |
| 2. ボクセル → ゲーム用JSON | `function-lab/objcts/player/voxel/tools/buildParts.mjs`（髪は `buildHair.mjs`） | `objcts/player/voxel/data/body-*.json` / `cloth-*.json` |
| 3. 取り込み | `scripts/sync-objcts.mjs` | 本リポジトリの `vendor/objcts/` |

- 現行の元アセットは `game-assets/vox-model/male_avatar{,_skinny,_muscle}`
  （**Auto-Rig Pro 303ボーン・身長1.845m**）。`body.Body_Skinny/Muscle/Chubby.morph.json` が既にあり、
  **体型モーフの仕組みは動いている**（Chubby は現行ゲームでは未使用）。
- voxel-pipeline は **新キャラ = config 1個追加**が目標の作り。`bone_map` でソースのリグ名を
  QM ボーン名へ移植（`--no-lbs` 必須＝ソース形状を保ったままボーン名だけ揃える）。

`player-one` を通すための手順と、埋める必要がある穴:
- [ ] V1: FBX を Blender で **A-pose の .blend** にする。⚠️ Babylon が読むわけではないので
      glTF 変換は不要（工程1が Blender で完結する）。
- [ ] V2: **`bone_maps/player-one.json` を作る**。このモデルは **LimbNode 55本**（3ds Max 系の
      リグと思われる。ボーン名は未確認＝Blender に読み込んで実物を見る必要がある）。
      現行は ARP 303ボーンなので、**ボーン数が大きく違う**。移植が素直に通るかはここで決まる。
- [ ] V3: `configs/player-one.json` を書く。`parts` に body / head / hair / shirt / pants /
      socks / shoes を列挙（OBJ のマテリアル分けと一致: Arms_Legs, Torso, face, eyes, lashes,
      hair, scalp_hair, shirt, pants, Socks, shoes）。
- [ ] V4: **体型モーフを自作する**。⚠️ このFBXは **BlendShape 0** なので、
      Blender で痩せ/太りのシェイプキーを作り、config の `morphs` に指定する。
      **ここが唯一の新規オーサリング作業**。W2〜W4（身長×体重で体格を決める）はこの上に載る。
- [ ] V5: `buildParts.mjs` で焼き、`sync-objcts.mjs` で取り込み、実機で確認。

引き継ぎ可否（ボクセル化する前提での結論）:
- ✅ **肌の色**: パレット役割 `Skin` で現行どおり塗り替えられる。
- ✅ **ユニフォームのチームカラー**: 役割 `Jersey`/`Shorts`/`Shoes` で現行どおり。
- △ **髪型138種**: `hair.json` は male_avatar の頭に合わせて作られている。同じ QM リグへ移植すれば
      頭の**位置**は揃うが、**頭の形状差ぶんの合わせ直しが要るか**は実物で確認が要る。
- △ **顔**: 顔パーツの塗り込みは body JSON 側の定義なので仕組みは残るが、**塗る面の座標は
      焼き直しが要る**。
- ⚠️ **ライセンス**: 配布元の利用条件（ゲームへの組み込み可否）は要確認。ここは判断できない。

参考（現行の三角形数。`function-lab/objcts/player/voxel/README.md` の実測）:
1体（着衣・normal 1.85m）で **1cm格子=約150,000三角形 / 2cm格子=約41,000**。
26人×2＋審判で **1cm=409万 / 2cm=107万**。⚠️ 格子の辺長がそのまま描画コストに効くので、
**player-one を焼くときの辺長は現行と揃える**（現行データの `voxelSize` は 0.015）。

### 参考: ポリゴンのまま使う案（不採用）

調べた事実:
- **FBX Binary・スキニング済み**（Skin 1 / Cluster 52 / LimbNode 55）＝ ボーン55本のリグ付き。
- **BlendShape 0** ＝ **モーフターゲットは入っていない**。体型バリエーションに流用できる形状は無い。
- **アニメーションは実質入っていない**（AnimationCurve 14本のみ）。実体は `Animations/*.max` で
  **3ds Max がないと読めない**。
- ポリゴン数: **頂点 47,110 / 三角形 89,368**（三角20,188＋四角34,590×2）。
- マテリアルは部位別に12個（Arms_Legs / eyes / face / hair / lashes / pants / scalp_hair /
  shirt / shoes / Socks / Torso）。テクスチャ13枚（shirt は PSD もあり）。
- 環境: **Blender 5.0 あり**（`C:\Program Files\Blender Foundation\Blender 5.0`）。
  ⚠️ **Babylon.js は FBX を読めない**ので、必ず glTF/GLB へ変換する工程が要る。

現行モデルとの比較（headless 実測 `headless_sim/probe-meshcost.ts`、normal / 1.95m）:
- 現行の**素体だけ**で メッシュ9 / 頂点14,048 / **三角形 7,024**（服の下の地肌は除去済み。服・髪・顔は別）。
- 新モデルは **1人 89,368 三角形** ＝ 素体比で **約12.7倍**。
  **26人で約232万三角形**。⚠️ **機能4（スマホで快適に動かす）と正面から衝突する**。
  採用するなら減面 / LOD / ベンチの簡略化が必須。

引き継げるもの / 引き継げないもの:
- ✅ **肌の色**: 現行の `look` の skin index をそのまま使い、Face / Torso / Arms Legs の
  テクスチャを実行時に着色（乗算 or canvas 焼き）すればよい。
- ✅ **ユニフォームのチームカラー**: shirt / pants / Socks が独立マテリアルなので、
  現行の「キット色を焼く」処理をテクスチャ着色へ置き換えれば実現できる。
- ❌ **髪型138種は引き継げない**。現行はボクセルの髪メッシュで、ポリゴンの頭には合わない。
  新モデルの髪は「テクスチャ＋アルファ板＋scalp」で頭と一体。**作り直しになる**。
- ❌ **顔の作り分けも失われる**。現行は顔のパーツをボクセル頭に描き込んで個体差を出しているが、
  新モデルは Face.jpg 1枚＝**全員同じ顔**になる（テクスチャを何種類か用意すれば緩和可）。
- △ **体型バリエーション**: BlendShape が無いので、Blender で**シェイプキーを作って
  glTF の morph target として書き出す**のが本命（骨のスケールだけだと継ぎ目が出る）。
- △ **モーション**: 付属アニメは使えないが、現行は自前の手続きアニメ＋クリップなので、
  **55本のリグを `boneMapping.ts` で標準ボーン名へリマップすれば今の動きを載せられる可能性**がある。
  リグのボーン名を調べてマッピング表を作る作業が要る。
- ⚠️ **ライセンス**: 配布元の利用条件（ゲームへの組み込み可否）は要確認。ここは判断できない。

ポリゴンのまま Babylon で使う案は**不採用**（上のボクセル化方針に置き換え）。
理由: 1人 89,368三角形 = 現行素体(7,024)の約12.7倍、26人で約232万三角形。機能4と衝突する。
また Babylon は FBX を読めないため glTF 変換が要り、髪型138種と顔の作り分けも失われる。
- [ ] X1: **採用可否を決めるための PoC**（本実装の前に必ずやる）
      Blender で FBX → GLB 変換 → Babylon で読み込み → リグを標準ボーン名へリマップ →
      **26人並べて実機のフレームレートを測る**。ここで通らなければ減面か不採用を決める。

### やること（形状）
- [ ] M1: **体の厚みを減らす**。身長と同じ流儀＝**Z方向のボクセル層を間引く**（軸別スケールは
      「立方体でなくなる」とコード内で否定されている作り）。腰→胴→上腕の順に効かせる。
      W3 と同じ仕組みなので、**W3 の実装で M1 を兼ねる**のが素直。
- [ ] M2: **素体をスキニングへ**（関節の継ぎ目を消す）。服の仕組みをそのまま流用する。
- [ ] M3: **腕の長さの調整**。まず「メッシュのどこが長く見えるのか」をヘッドレスで実測して
      切り分ける（NullEngine + `getVerticesData` で骨軸に沿った広がりを測る）。
- [ ] M4: **腕を IK/FK で動かす**。共有 `ik.ts` / `fk.ts` を使い、`reach-ik.ts` との重複を整理する。

## フェーズ計画

- [x] Phase 0: basketball-sim の移植（Vite + Babylon 構成へ、build ✓）
- [x] **Phase 1: ポーカー（機能1）の一巡目** — ロジック / 妨害 / UI / 手番の演出まで。
      追加実装は上記「今後の追加実装」に列挙（継続）。
- [ ] Phase 1.5: ローディング表示（機能4の一部だけ先出し）
      — クラブ決定後の26人ぶんのボクセル生成など、無言で固まる箇所に進行表示を出す。
      小さく、プレイテストの障害を先に取り除ける。
- [ ] **Phase M: 選手モデルとモーションの土台（機能5）** — M1 厚み → M2 スキニング →
      M3 腕の長さ → M4 IK/FK。
      **Phase 2b（ムーブ追加）より先にやる**。腕のリグと見た目を直す前にモーションを作ると、
      直した後に作り直しになるため。ここは実機でしか検証できないので、プレイテストの回路も通る。
- [ ] **Phase 2: 1on1 の決着（機能2 ＋ 機能3の「1on1の接触」）**
      ここは2と3が同じコードパスなので一体で行う。分けると同じ場所を二度書き直すことになる。
      - [ ] 2a: 勝敗を明示的な判定に。現状 `ai/offense/onball/drive.ts` は `chance()` の連鎖で、
            「いつ・何で決まったか」が残らない。仕掛け → 判定 → 決着 の形にし、決着理由を保持する。
            同時に接触（体重・進入速度・角度）を判定へ入れる。
      - [ ] 2b: ムーブ追加（ヘジテーション / ユーロステップ / スピン / ステップバック）とモーション。
            vendor の既存クリップ（dribbleCross/dribbleHesi/doubleClutch 等）を使う。
      - [ ] 2c: ダンク / ブロックの結末を離散化。現状 `move/reaction/contest-block.ts` は
            確率を返して game.ts が1回コインを振るだけ。ダンク成立 / ブロック / チップアウト /
            ファウル に分ける。
- [ ] Phase 3: 残りの局所物理（機能3）
      - [ ] 3a: リバウンドの位置取り争い。現状 `move/reaction/rebound.ts` は能力値だけの
            確保確率で、位置取りが存在しない。2a の接触モデルを再利用する。
      - [ ] 3b: インターセプト / パスカット。現状 `move/reaction/pass-risk.ts` は確率抽選。
            ボールの実軌道と手の到達可能域の交差で判定する。
- [ ] Phase 4: 最適化（機能4の本体）— バンドル分割・Babylon の import 整理・選手DBの遅延読み込み

Phase 4 は最後だが、**バンドルサイズは各フェーズの終わりに毎回計測**する（増えたまま進まない）。

## 設計上の前提

- 座標系: X=幅 / Z=長さ / Y=上。Team0 → +Z 攻め、Team1 → -Z 攻め（`config.ts` 冒頭）。
  手続きメッシュのみ = グローバルrule6 の forward/回転懸念は対象外。
- パラメータは `src/config.ts`、能力値/戦術は `src/attributes.ts` に集約する。
- 高速再生はメインループの整数サブステップ（dt を大きくしない）。
- 乱数: 移植元は `Math.random()` 直呼び。ポーカーは再現性が要るので `src/rng.ts`
  （シード付き mulberry32）を使う。

## 検証の手段

- ロジック・バランスは **headless harness**（`headless_sim/`、中央メモリ
  `reference_headless_sim_harness`）で多試合まわして実測する。
- ⚠️ 見た目（モーション・ローディング表示）はブラウザ実機でしか検証できない。
  実機未検証のものは必ずそう明記する。
