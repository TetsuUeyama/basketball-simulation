# work-record

## 2026-09-03

- `C:\Users\user\developsecond\basketball-simulation` にプロジェクトを新規作成。
- 構成: Node + TypeScript（ヘッドレス）。Node の型ストリッピングで `.ts` を直接実行する。
- 追加ファイル: `package.json` / `tsconfig.json` / `.gitignore` / `README.md` / `workPlan.md` / `src/index.ts` / `src/rng.ts`
- リモート `https://github.com/TetsuUeyama/basketball-simulation.git` を origin に設定。
- 確認済み: `npm install` / `npm run typecheck` / `npm start` / `npm run build` が通ること。
- シミュレーション本体は未着手。次は workPlan の「未確定」を決めるところから。

---

## 2026-09-03（続き） basketball-sim を踏襲してゲーム化を開始

ユーザーの要望は4つ: ①クオーター毎のポーカーによる強化 ②1on1 の勝敗を能力で決める
③判定時の局所物理 ④スマホでの高速化とローディング表示。

### 決めたこと（ユーザー確認済み）

- **本体は basketball-simulation**（この新リポジトリ）。basketball-sim から移植して出発する。
- **描画は Babylon.js 3D を踏襲**（既存のボクセル選手・カメラ・HUD をそのまま使う）。
- **ユーザー操作はポーカーのみ**。試合そのものは従来どおり AI が進める（観戦）。
- ポーカーの核心ルール: **手札交換を打ち切って役を確定させるタイミングをホームチームが決める**。
  早く確定すれば効果が長く効き、引っ張れば強い役を狙えるが恩恵は短い。

### 1. 移植（commit 39a1ca1）

- basketball-sim の commit `76c9eca` から `src/`(約3万行) / `vendor/objcts` / `docs` /
  `scripts` / `headless_sim/*.ts` / `index.html` / `vite.config.ts` をコピー。
- ヘッドレス雛形の `package.json`・`tsconfig.json` を Vite + Babylon 構成へ差し替え
  （NodeNext + erasableSyntaxOnly → bundler + DOM lib）。`src/rng.ts` だけ雛形から残置。
- 確認: `npm install` / `npm run build` ✓。
- **実測: バンドル 11.98MB（gzip 2.71MB, 単一チャンク）**。要望4の出発点の数字。

### 2. ポーカー強化システム（機能1）

新規 `src/poker/`。Babylon にも DOM にも依存しない純粋ロジックにして、ヘッドレスで検証できる形にした。

| ファイル | 役割 |
| --- | --- |
| `cards.ts` | 札とデッキ。乱数は `src/rng.ts` のシード付き（再現性のため `Math.random` を使わない） |
| `hands.ts` | 役判定（`evalHand`）と役同士の比較 |
| `effects.ts` | **バランス調整ノブの集約**。カード→能力の対応、捨て札の量、役ごとのチーム強化 |
| `state.ts` | `PokerMatch` — 配札/交換/確定/巻き戻し。乗せた強化は全て記録して `revert()` できる |
| `ai.ts` | CPU の捨て札選択・強化先の選択・（ホームなら）確定タイミングの判断 |

- **カード→強化**: スートが系統（♥=オフェンス / ♦=ディフェンス / ♠=シュート / ♣=フィジカル）、
  ランクが量（A=主能力+5 / 絵札=系統内の特定能力+3 / 数札=主能力+2）。
  **同じカードでも選手によって効きが変わる**（その能力が高い選手ほど大きい、0.6〜1.4倍）。
- **捨て札は即座に個人強化**（確定を待たない）。1回の交換で捨てられるのは3枚まで（`MAX_DISCARDS`）。
- **役の確定でチーム全13人に強化**。tier(0..9) → 系統+0..6 / 全能力+0..4（`HAND_BUFF`）。
- 強化は `PlayerDef.attr` に直接足し、差分を `applied` に記録。試合リセット/BACK で `revert()`
  して素の能力値へ戻す（`Player.attr` は def へのライブ参照なので、これが唯一安全な戻し方）。

### 3. 試合本体への配線

- `Game` に `poker` / `pokerGate` / `onPokerRound` と `awaitPoker(round, next)` /
  `resumeFromPoker()` を追加。`core/gameflow.ts` のクォーター切り替え（引き上げ→ハドル→次Q）の
  間に `awaitPoker` を挟んだ。UI が居なければ素通りするので、ヘッドレスも従来どおり動く。
- `src/ui/ui-poker.ts` 新規: 手札の表示・捨てる札の選択・**捨て札ごとに強化先の選手を選ぶ**
  プルダウン（効果をその場で表示）・確定/持ち越しの選択・確定時の相手手札の公開。
- 試合前バーに「ポーカー: 自分で打つ ⇄ CPU同士」トグル（`POKER_OPTS.userTeam`）。
  CPU同士では画面を出さず自動で進む＝**CPU対CPUとCPU対ユーザーの両方が同じ処理で回る**。
- ポーカー画面が出ている間は `ui.simPaused` でシムだけ止める（描画とカメラは動かす）。

### 4. 検証

- `headless_sim/probe-poker.ts`: **52C5 の全 2,598,960 通りを列挙**して役の頻度を理論値と照合
  → 10種すべて完全一致（ロイヤル4 / SF36 / フォーカード624 / フルハウス3744 / フラッシュ5108 /
  ストレート10200 / スリーカード54912 / ツーペア123552 / ワンペア1098240 / ハイカード1302540）。
  境界（ホイールA2345 / K-A-2-3-4は非ストレート / ロイヤル）も期待どおり。
- CPU同士4000試合ぶんのポーカー: 確定ラウンドは R1:2.0% R2:18.8% R3:35.2% R4:44.0%、
  確定役はワンペア29.5% ツーペア29.0% スリーカード20.9%。1人あたりの平均上昇 0.84点/能力。
### 5. バランス実測（`headless_sim/probe-poker-balance.ts`、各200試合）

同じクラブ対戦カード（`n%CLUBS` vs `(n*7+3)%CLUBS`）を全条件で使って比較する。
team0 と team1 で引くクラブが違うため、**基準（ポーカー無し）が 50% ではなく 40%** である点に注意。

| MODE | 内容 | team0 勝率 | 平均スコア | 格下の勝ち |
| --- | --- | --- | --- | --- |
| off | ポーカー無し（基準） | 40.0% | 11.3 - 12.5 | 21.8% |
| both | 両チームがポーカー（実プレイの既定） | 42.0% | 11.9 - 12.4 | 15.8% |
| one | team0 だけポーカー | 50.0% | 12.1 - 11.6 | 27.8% |
| max | team0 が毎試合ロイヤル（上振れの上限） | **75.0%** | 15.9 - 11.0 | 29.3% |

- 実プレイの形（both）では基準+2.0pt = **ほぼ中立**。格下の勝ちはむしろ 21.8%→15.8% に減った。
- 片側だけなら +10.0pt。**補助的な範囲**に収まっている。
- ⚠️ **max が 75% で workPlan の目標「60%未満」を超えた**。原因は `HAND_BUFF` の `all`
  （全25能力）で、`group`（系統4〜5能力）の5倍効いていたこと。ロイヤルで13人×25能力×+4 =
  1689点が乗っていた。
- 対策: `all` を全廃し、**系統への集中**に振り直した（ワンペア group+2 … ロイヤル group+12）。
  再実測の結果は下記。

| MODE（調整後） | team0 勝率 | 平均スコア | 格下の勝ち | team0 の強化量 |
| --- | --- | --- | --- | --- |
| both | 46.0% | 11.9 - 12.2 | 19.5% | 224点 |
| one | 50.0% | 12.0 - 11.7 | 15.0% | 224点 |
| max | **52.5%** | 13.2 - 12.7 | 22.6% | 778点 |

- **max が 75.0% → 52.5% に収まり、目標「60%未満」を満たした**。
- 格下の勝ちは基準 21.8% に対し both 19.5% / one 15.0% / max 22.6% =
  **どの条件でも基準を上回らない**。ポーカーは番狂わせを増やしていない。
- ⚠️ 一方で **max(ロイヤル毎試合, 778点) が one(平均的な役, 224点) とほぼ同じ勝率**になった。
  系統に集中させると効果が飽和するため。N=200 の標準誤差は約3.5ptなので、この2条件は
  現状の試行数では区別できない。強い役に「効いた感」を持たせたいなら上位役だけ引き上げる
  余地がある（ただし max が60%へ近づく）。

### 6. ユーザー指示による変更（同日）

- **ポーカーの1手目を「両チーム決定後・選手紹介とティップオフの前」へ移動**。
  `ui.onPrepare`（コートに26人を並べる）と `ui.onStart`（選手紹介ツアー→ティップオフ）に分割し、
  TIP OFF ボタンは `onPrepare() → beginPoker(() => onStart())` の順で呼ぶ。
- **CPU同士 / あなた対CPU の選択を一番最初の画面へ**。タイトルの「クラブチーム対戦 /
  ランダムクラブ / ランダム対戦」を選ぶ**前**に決める形にし、試合前バーのトグルは廃止した。
- **ポーカー画面を作り直し**: 説明文を全廃し、**現在の役・コートに見立てた盤・手札・ボタン**だけに。
  盤の上に先発5人の顔アイコンを立ち位置（PG=トップ / SG・SF=ウイング / PF・C=ゴール下）に並べ、
  **手札をドラッグして選手の上に置くと、その選手が強化される**（タップでも: 札を選ぶ→選手を叩く）。
  置いた札には効果（例 `♥7 OFF+3`）が付き、タップで手札へ戻せる。置ける枚数は `MAX_DISCARDS`。

- **控え8人にも札を置けるようにした**。盤を 430px → 330px に縮め、コート横に控え8人を
  2列×4段で並べる（画面幅660px未満では盤の下に4列×2段へ回り込む）。先発と控えは同じ
  `playerCell` で作り、13人の誰でもドロップ先になる。CPU の宛先選択は先発5人のまま
  （出場時間が長いぶん有利なため、方針として変えていない）。
### 7. ユーザー指示による追い込み（2026-09-04）

- **ドラッグ中の影を 52×74 → 34×48 に縮小**。
- **追従性の改善**: 影の移動を `left/top`（再レイアウトが起きる）から
  `transform: translate3d(...)` + `will-change` へ変更し、ドロップ先の矩形は
  **ドラッグ開始時に一度だけ**測るようにした（従来は pointermove ごとに13個ぶん
  `getBoundingClientRect` を呼んでいた＝毎フレーム強制レイアウト）。
  併せて `setPointerCapture` で指が要素外へ出ても move が途切れないようにした。
  ⚠️ 実際に滑らかになったかは実機でしか確認できない。
- **捨て札の上限を 3 → 5 枚**（`MAX_DISCARDS`）。手札を全部捨てられる。
  CPU は最良の1枚を残す方針なので最大4枚（役を完全に捨てる打ち方はしない）。
- **札を置いてもアイコンがずれない**ように、置いた札のチップを
  `position: absolute` でセルの外へ浮かせた（セルの箱の大きさが変わらない）。
  横並び＋折り返しにして下方向へ伸びすぎないようにし、盤の `overflow: hidden` を外し、
  PG の立ち位置を y79% → 74% に上げてチップが手札に被らないようにした。

- **強化は1人1枚まで**にした（横並びでも見にくかったため、ユーザー指摘）。同じ選手へ2枚目を
  置くと前の札は手札へ戻る。表示は**名前の下に1行**へ戻し、その1行分の高さ(13px)は
  **札が無くても最初から確保**する。置いてもアイコンはずれず、控えの行間も最初から空く。
  （※これに伴い、いったん入れた「アイコンの右横／右寄りは左横へ反転」は撤去）
- 旧: - **置いた札の表示をアイコンの真横へ**（下に積むと他の選手アイコンが隠れるため、ユーザー指摘）。
  文字を 10px → 8.5px に縮小し、縦に積む。コート右側の先発とベンチ右列は**左横へ反転**させ、
  盤やベンチ枠からはみ出しにくくした。

#### 捨て札上限 3→5 の影響（各200試合）

| MODE | 上限3 | 上限5 |
| --- | --- | --- |
| one（片側のみ） | 50.0% | 49.0% |
| both（実プレイの形） | 46.0% | 47.5% |
| team0 の強化量 | 224点 | 229点 |

CPU の打ち方では**ほぼ変化なし**。上限3が CPU の選択を縛る場面が少なかったため
（CPU は役の draw を残す打ち方をするので、そもそも3枚以上は捨てない）。
⚠️ ただしこれは CPU の打ち方の測定であって、**人間が毎回5枚捨てる打ち方は別**。
それを測るため `MODE=dump`（毎ラウンド全捨て・役は最終ラウンドで自動確定）を追加した。

実測（200試合）: **dump 44.5%**（基準40.0% / 通常の打ち方 one 49.0%）。強化量131点。
確定役は ハイカード101 / ワンペア84 で、役はほぼ作れない。
→ **全捨ては最適解ではない**（役を狙う打ち方より 4.5pt 弱い）。捨てる枚数の上限を5にしても、
　「個人強化を取るか役を取るか」のトレードオフは成立している。

### 8. 妨害（相手選手の能力を下げる）を追加（2026-09-04）

- 画面の**一番上に相手の段**を新設: **伏せた手札5枚**（役が確定すると表になる）と、
  **相手の先発5人**の顔アイコン。相手選手に札を置くと**妨害**になる。
- `hinderEffect()`: カード→能力の対応は強化と同じで、符号が負・効率は `HINDER_SCALE`(0.8)。
  強化と同じく「その能力が高い相手ほど大きく下がる」＝主力を潰しにいく手になる。
- 捨て札の置き先を `number`（自軍のロスター番号）から **`DiscardTarget {team, idx}`** へ変更。
  自軍なら強化・相手なら妨害を適用し、`applied` に負の差分として記録して `revert()` で戻す。
- CPU も妨害を使う: `pickTarget` が「自軍の伸び」と「相手の下げ幅」を同じ物差しで比べ、
  大きい方を選ぶ（同点なら自軍）。1人1枚の制約も CPU 側で守る。
- ⚠️ 計測プローブの修正: 妨害が入ったことで、MODE=one/dump（team0 だけの効果を測る条件）で
  **team1 に打たせると team0 の能力値へ直接効いてしまう**ため、これらの条件では team1 は
  打たせないようにした。この修正前の one/dump の数値は妨害を含まないので比較対象は both のみ。

#### 妨害ありのバランス実測（各200試合、基準=ポーカー無し 40.0%）

| MODE | 妨害なし | 妨害あり |
| --- | --- | --- |
| both（実プレイの形） | 47.5% | **45.5%** |
| one（片側のみ） | 49.0% | **53.5%** |

- both は基準+5.5pt でほぼ中立のまま。格下の勝ちも 20.3%（基準21.8%）で増えていない。
- one は +9.0pt → +13.5pt に増えた。妨害は「相手の主力の長所を削る」ぶん効率が良い。
  それでも目標の60%未満には収まっている。

### 9. 画面の並びを組み替え（ユーザー指示）

- 盤を**横長フルコート**（28×15、左右にリム）に描き替え、**左半分に相手の先発5人**、
  **右半分に自分の先発5人**を配置。相手側に置けば妨害、自分側に置けば強化。
- **控え8人は手札の下**へ（4列×2段）。コート横のベンチは廃止。
- **現在の役は交換ボタンの横**へ移動（確定後は相手の役も並べて出す）。
- 相手の伏せ手札は最上段のまま。
- **相手の控え8人**も相手の伏せ手札の下に配置（自分側と対称、4列×2段）。ここにも札を置いて
  妨害できる。これで札の置き先は 自分13人 + 相手13人 = 26人。
  CPU の選択は先発10人（自軍5・相手5）のままにしている（控えは出場時間が短く効率が悪いため）。

### 10. 手番を明示（相手が先 → 自分 → ホームが確定判断）

- 実装上は元から「CPU の交換 → ユーザーの交換 → ホームの確定判断」の順だったが、
  **相手が何をしたのかが画面に出ていなかった**ため、先手の意味が無かった。
- `AppliedDelta` に `by`（打ったチーム）と `round`（何ラウンド目か）を追加し、
  **そのラウンドで既に適用された増減を選手アイコンの下にチップで表示**するようにした。
  枠線が打った側のチームカラー。これで「相手が誰を強化し、こちらの誰を妨害したか」を
  見てから自分の手を決められる。
- チップ行は絶対配置にしてセルの幅にも影響させない（相手の手が入っても配置がずれない）。
- 将来「監督（ヘッドコーチ）の能力で先攻後攻を決める」を workPlan に記載。
  ヘッドコーチという概念自体が未実装なので、能力値・データの持ち方から設計が要る。
- CPU の捨て札判断は**ランダムではなく**、ドローポーカーの定石（ペア以上/4枚ドローを残す）＋
  置き先は「自軍の伸び」と「相手の下げ幅」の大きい方。本格的な AI（点差・残りクォーター・
  相手の役を読む）は将来の課題として workPlan に記載した。

### 11. 相手の手番を見せる（思考 → 札を置く → 補充）

指摘: 画面を開いた時点で既に相手の交換が済んでいて、何が起きたのか分からない。

- `cpuPlan()` を新設して CPU の判断を「決めるだけ」に分離（`cpuExchange` はそれを即適用する
  薄いラッパにして、ヘッドレス計測は従来どおり）。
- UI に手番の段階 `pokerStage = "cpu"` を追加。この間はボタンを出さず、選手アイコンへの
  クリックも張らない（座標だけは登録して、札を飛ばす先に使う）。
- 見せ方: **思考時間**（伏せ札が軽く上下する）→ **捨てる札が1枚ずつ、手札の位置から
  置き先の選手へ飛ぶ**（受け取った選手が一瞬光る）→ 交換を適用 → **補充された札が
  手札に入る**（上から差し込まれる）。補充が終わってから自分の手番へ渡す。
- 時間は `POKER_OPTS` で設定: `thinkMs`(900) / `stepMs`(420) / `dealMs`(260)。
- `AppliedDelta` に `card` を追加し、何の札で動いたかを記録できるようにした。
- ⚠️ 捨てた札は**表で**飛ばす（何を捨てたかは見せる）。手札の中身は伏せたまま。

### 12. 選手データにポジション(C〜PG)と体重を取り込み（2026-09-04）

出典: `OneDrive/デスクトップ/WE2010 0903.xlsx` Sheet1（ヘッダ2行目・データ3行目〜、4015人）。
抽出スクリプトは `scripts/update-identity.cjs`（node+xlsx。python は Store スタブで動かない）。

- **列の対応**: D=選手名 / **J=C K=PF L=SF M=SG N=PG（守れるポジションのフラグ）** /
  O=身長 / **P=体重** / R=利き足。⚠️ 旧ブックとは列配置が違う（旧は CI..DE が能力値）。
- ⚠️ **行の並びが identity.ts と一致しない**（並び順での一致は 316/4015）。**名前で照合**した。
  同名が42組あるので、まず「名前＋身長」で当て、残りを名前順で当てる → **4015人全員一致・
  取り違えゼロ**を確認。
- **role の決め方**: 守れるポジションの**中央値**（C..PG 順）。
  結果の分布 C:734 PF:1001 SF:753 SG:975 PG:552（旧: C:650 PF:825 SF:855 SG:1073 PG:612）。
  守れるポジション数は 1個:2808人 / 2個:974 / 3個:176 / 4個:48 / 5個:9。
- **追加したデータ**: `identity.ts` の行末に `weightKg` と `posMask`
  （C=1 / PF=2 / SF=4 / SG=8 / PG=16 のビット和）。体重は全員あり（50〜99kg・中央75）。
- **配線**: `DbPlayer` に2項目追加 → `PlayerDef.weight` / `PlayerDef.posMask` →
  `Player.weight` / `Player.posMask`（applyDef で反映）。
  `roleFit()` が posMask を見るようになり、**交代・ラインナップの適格判定が実データになった**
  （従来は手書きの EXTRA_POSITIONS 1件のみ）。UI の「守れるポジション」も posMask を正とし、
  posMask を持たない def は従来の身長/脚力の推定にフォールバック。

- **利き手も Excel の R列（利き足）で更新した**（ユーザー指示）。**1238人が変更**。
  更新後の分布は R:3131 / L:884 で Excel と一致。
  ⚠️ 名前＋身長での照合は 4015人全員一致・取り違えゼロなので、**行対応はずれていない**。
  にもかかわらず個別が3割ずれていた＝**旧ブックと新ブックで利き足の値そのものが違う**
  （旧抽出時の列違い、または元データの更新）。新ブックを正とした。
- `scripts/update-identity.cjs` は**再実行できる**（旧6要素・新8要素のどちらの行も受ける）。
  実行: `WRITE=1 NODE_PATH=<xlsxを入れた場所>/node_modules node scripts/update-identity.cjs`

### 13. player-one のボクセル化に着手（V1/V2）

**リグは Mixamo/Unity humanoid 命名の52骨**（Hips/Spine/Spine1/Spine2/Neck/Head/
LeftShoulder/LeftArm/LeftForeArm/LeftHand/…/LeftUpLeg/LeftLeg/LeftFoot/LeftToeBase）。
→ **既存の `bone_maps/player.json`（SportsAvatar_3 = 同じ命名の66骨）が52骨すべてをカバー**
していたので、新規のマッピング作成は不要だった（余る14キーは指先/つま先で player-one 側は
ボーンでなく EMPTY）。

- 実測: メッシュ1個（47,116頂点 / 89,368ポリゴン / **シェイプキー0**）、マテリアル11、
  身長 1.815m、rest は **T-pose**（上腕が水平から約6°下）。
- ⚠️ **メッシュが1つしかなく部位分けが material にしか無い**。voxel-pipeline の `parts` は
  mesh オブジェクト単位なので、前処理で material → 部位オブジェクトへ割り直した。
  `blender_scripts/preprocess_player_one.py` を新規作成（`preprocess_player.py` が前例）。
  body = Torso+Arms Legs+face / hair = hair+scalp hair / eyes = eyes+lashes。
- 前処理の結果: T-pose を素に戻し、上腕を QM(ARP) の A-pose 方向へ **23.07°/22.95° 回転
  （残差 0.0°）**。分割は body 11,499頂点 / jersey 10,272 / shorts 4,463 / socks 7,467 /
  shoes 6,362 / hair 6,403 / eyes 802。
- `configs/player_one.json` を作成し、`--dry-run` で工程解決を確認
  （cleanup skip → modifiers → transplant --no-lbs → voxelize → fill skip → morphs skip）。

**V3 完了: ボクセル化に成功**（7部位・77.4秒・manifest conformant）。
出力 `game-assets/vox-model/player_one/`。

| | player_one | male_avatar（現行） |
| --- | --- | --- |
| voxel_size | 0.00746m | 0.00748m |
| 身長 | 1.842m | 1.845m |
| 腕を広げた幅 | 1.842m | 1.738m |
| **奥行き（厚み）** | **0.402m** | **0.476m** |
| body のボクセル数 | 104,534 | 195,824 |

→ **新モデルの方が厚みが薄い（0.402/0.476 = 84%）**。「厚みが大きすぎる」の是正に有利。
→ ボーン割当は ARP/QM 名で正しく付いた（c_spine_03_bend.x / head.x / c_arm_stretch.l/r /
　 c_thigh_stretch / c_forearm_stretch / shoulder）＝ **transplant は成功**。
　 body に c_leg_stretch がほぼ無いのは、すねが socks パーツ側にあるため（正しい）。

⚠️ **焼き込み（V5）で判明している要修正点**
（`function-lab/objcts/player/voxel/tools/buildParts.mjs`）:
- `VARIANTS` が `male_avatar{,_skinny,_muscle}` に**ハードコード**されている。
- `UNIFORM_PREFIXES = {tshirt, jeans, shoes}` で、player_one の prefix（jersey/shorts/socks/
  shoes）と合わない。
- player_one には**体型モーフが無い**ので変種が1つしか作れない。ゲーム側 `DATA` は
  skinny/normal/muscle の3つを要求するため、当面は同じものを3つに割り当てるか、
  シェイプキーを自作する（V4）まで1変種で動くようにするかの判断が要る。

### 14. player_one をゲーム用データへ焼き、モデル確認ページを作成（V5）

**焼き込み（`function-lab/objcts/player/voxel/tools/buildParts.mjs`）を汎用化**:
- `VARIANTS` を環境変数で差し替え可能に（`VOX_VARIANTS` に JSON。先頭 `+` で既定へ追加）。
- パーツ一覧を固定配列から**ディレクトリ実在チェック**へ（モデルで服の prefix が違うため）。
- `PRIO` を未知 prefix でも「服」扱いになる Proxy に、`ROLE` に jersey→Jersey /
  shorts→Shorts / socks→Shoes を追加。`segments.mjs` の `UNIFORM_PREFIXES` にも同3つを追加。
- 実行: `VOX_VARIANTS='{"p1":"player_one"}' VOX_SIZE=0.015 node tools/buildParts.mjs`
  → `data/body-p1.json`(484KB) / `cloth-p1.json`(538KB)。全パーツ **未割当0**。

**焼き上がりの比較（同じ物差しで実測）**

| | player_one | 現行 normal | 現行 muscle |
| --- | --- | --- | --- |
| 胴 厚み/幅 | 0.79 | 0.75 | 0.92 |
| **腰 厚み/幅** | **0.80** | 0.95 | 1.18 |
| 三角形/人 | **5,088** | 7,024 | 8,232 |
| 26人ぶん | 132,288 | 182,624 | 214,032 |

→ **腰の厚みが自然になり（0.95→0.80）、三角形は28%減った**。

⚠️ **重要な発見: 腕の長さはモデルを変えても変わらない。**
上腕 0.262m / 前腕 0.204m（比 1:0.78）は player_one でも現行と**完全に同一**だった。
**関節位置は QM(ARP) スケルトンから来ている**ため、素体を差し替えても骨は変わらない。
腕全体が身長比 25.8%（人体標準 33%）と短いのは**スケルトン側の性質**。
→ 「前腕が長すぎ・上腕が短すぎ」に見える件は**モデル差し替えでは直らない**。
　 直すならスケルトン（restPose / QM の骨長）か、腕の動かし方（M4 の IK/FK）を触る必要がある。

**モデル確認ページ `/confirm.html`（`src/confirm.ts`）を新設**:
- `voxelBody.ts` に variant `"p1"` と `setVariantOverride()` を追加（未設定なら従来どおり
  `variantFor(balance)`＝ゲーム側の挙動は変わらない）。元の objcts で編集 → `sync-objcts.mjs` で取り込み。
- 左右に別モデルを並べ、モーションを選んで 再生/停止/1コマ送り/速さ変更 ができる。
  三角形数・メッシュ数・fps を常時表示。**ゲームと同じ `Player` / `applyMotion` を通す**。
- `vite.config.ts` に2つ目のエントリ（confirm.html）を追加。`npm run build` ✓。
- ヘッドレスで p1 のメッシュ生成を確認（`probe-meshcost.ts` に `VARIANT` 環境変数を追加）。

### 15. 【訂正】QM への移植をやめ、モデル本来のリグでボクセル化し直した

ユーザー指摘: 「作り方を以前のモデルに寄せすぎ。ボーン設定があるはずだが設定していないのでは」。
**指摘のとおりだった。**

- 実測で確認: 移植版の `player_one/skeleton.json` は **male_avatar と完全に同一**
  （303骨・座標まで一致）。`transplant` は設計上「ボーン構造を QM と完全一致させる」もので、
  `--no-lbs` は**メッシュの形だけ**元のまま残す。つまり **肉は player_one・骨は QM** という
  状態で焼いていた。前回「腕の長さはスケルトン側の性質」と書いたのは、**自分が選んだ工程の
  結果**であって、モデルの性質ではなかった。検証を怠った。
- 対処: `configs/player_one.json` を **`transplant: "none"`** にしてソースの Mixamo 52骨を保つ。
  `voxelize.py` は blend 内の armature をそのまま skeleton.json に書くので、これで
  **モデル本来の関節**が出る（`Mapped vertex groups -> bones: 52/52`）。
- 焼き込み側を**リグ対応**にした（`segments.mjs` / `tools/buildParts.mjs`）:
  `mixamoToSegment` / `MIXAMO_PIVOT_BONE` / `MIXAMO_CHILD_BONE` / `MIXAMO_FINGER_CHAINS` /
  `MIXAMO_JOINT_BONE` を追加し、`detectRig()` で skeleton.json のボーン名からリグを判定して
  `rigTables()` で切り替える。ARP 側の既存テーブルはそのまま（male_avatar は従来どおり焼ける）。

**焼き直しの結果（モデル本来の寸法が出た）**

| | player_one（自前リグ） | 現行 normal（QMリグ） |
| --- | --- | --- |
| 上腕 / 前腕 | **0.231 / 0.239（1:1.04）** | 0.262 / 0.204（1:0.78） |
| 大腿 / 下腿 | **0.429 / 0.418** | 0.393 / 0.417 |
| 腰 厚み/幅 | 0.85 | 0.95 |
| 三角形/人 | 5,076 | 7,024 |

→ 移植版では現行と**完全に同一だった**骨の寸法が、モデル固有の値になった。
⚠️ ただし player_one 自身の比率は **前腕が上腕より長い（1:1.04）**。人体標準は 1:0.78 なので、
　 このモデルは腕の見え方に癖がある。実機で確認して許容できるか判断が要る。

⚠️ ブラウザ実機（`npm run dev`）での見た目・操作感は未検証。ドラッグ＆ドロップと盤のレイアウトは
   実機でしか確認できない。

## 2026-09-05 髪の被覆問題 — 幾何フィットを破棄し「頭皮を髪色に塗る」で対処

**問題**: 髪型 002 などで頭頂・後頭部が髪で覆われず、地肌が透けて見える。

**やって駄目だったこと（すべて破棄済み）**

1. `HAIR_MARGIN` を 12mm → 24mm へ拡大 ＋ 上/後ろへずらす
   → **悪化**。実測で被覆 001: 74%→21%、002: 66%→28%。
   さらに測ると、頭頂ボクセルから最寄りの髪までの距離が中央値 3.6cm。
   「髪が足りない」のではなく **髪の殻が浮いていた**。原因は、髪メッシュの bbox を
   「頭の大きさ」とみなしてスケールを決めていたこと（bbox は髪の**外側**であって頭皮ではない）。
2. 素体の頭皮頂点へレイを飛ばして倍率・前後・上下を自動フィット（縦横別倍率まで含む）
   → 数値上の被覆は 85〜100%、隙間 0〜3.7cm まで改善したが、
   **髪の位置が低くなるなど見た目が崩れた**のでユーザー確認で却下。

→ `blender_scripts/add_hair_player_one.py` は元の方式（短髪 bbox 中央値・一律スケール・
   余裕 12mm）へ戻した。戻した状態で焼き直した `public/vox/player_one` は
   **コミット済みデータとバイト一致**（＝復元が正確であることの裏付け）。

**採用した対処**: `src/voxraw.ts` `applyScalpTint(part)`

- 髪ごとに「高さの層 × 方位」の最大半径を取り（服の `clothCover` と同じ考え方）、
  その内側にある**頭皮ボクセルの頂点カラーを髪の色に書き換える**。
- 髪型を切り替えるたびに `showHair()` から呼ぶ。`""` を渡すと元の肌色へ戻る。
- body メッシュだけ `applyToMesh(mesh, updatable=true)` にして色を後から差し替える。
- ⚠️ 顔（頭頂から 9cm より下の前面）は塗らない。前髪のある髪型だと方位判定が顔まで
  覆ってしまい、顔が髪色に染まる。

**実測**（`headless_sim/probe-scalptint.ts`。ブラウザと同じ `buildRawModel` を通す）

| 項目 | 結果 |
| --- | --- |
| 塗り替えボクセル | 髪型あたり 1,558〜2,046 |
| 塗った色 | 全 13 種で髪の色 (14,6,6) と一致 |
| 頭頂・後頭部の塗り残し | 0%（全種 100% 塗られている） |
| 顔にかかった頂点 | 全種 0 |
| 「髪なし」で元の肌色へ復元 | OK |

⚠️ これは数値上の確認。**ブラウザ実機（`/confirm.html`）での見た目は未確認**。
   髪と肌の境目が目立つ場合は `applyScalpTint` の `margin` で調整する。

**この件で学んだこと**

- `uv run voxel-pipeline <name>` は `--keep-work` を付けないと中間 blend
  （`prep.blend` / `prep_hair.blend`）を消す。作り直しに 2 工程かかった。

## 2026-09-05（続き） 顔（あご）のバリエーションと、首が消えていた件

**やったこと**

1. **顔のバリエーション**（`src/voxraw.ts` `JawShape` / `reshapeJaw` / `setJaw`）
   あご帯（頭頂から 0.130〜0.265m）だけを作り替える。標準 / 丸顔 / 細あご。
   - 横幅と前方向の奥行きを高さに応じて縮める（`JAW_PROFILE`）
   - あご材料を上へ寄せて顔を短くする（`shorten`）。空いた下は首が埋める
   - 実測の出発点: 頬〜あごの幅が 0.166m で**ほぼ一定**＝絞りが無い。これが「四角いあご」の正体
   - `/confirm.html` に「顔（あご）」セレクタを追加

2. **首が丸ごと消えていたのを修正**
   `clothCover` は「同じ高さの層にある服の**最大**半径」で内外を決めるため、首の高さの層に
   肩・胸（半径0.39）が入り、半径0.07 の首は必ず「服の中」と判定されて全層で落ちていた。
   「服の**最内殻**より内側なら襟の穴＝上から見えるので残す」（`inHole`）に変更。

**この件で踏んだ落とし穴（同じことを繰り返さないための記録）**

- ⚠️ **逆写像は1点で引いてはいけない**。幅を 0.8 倍に縮めると元の格子を 1.25 個おきに
  飛ばして拾うので、薄い表面が抜けて穴が開く。実測で丸顔の Blender Y=0 の面が丸ごと消え、
  左右からレイが貫通していた。セルが覆う範囲（±半ボクセル）を写した先を全部見ること。
  その代わり量子化で1ボクセル太るので、削り係数はその分強めにしてある。
- ⚠️ **削除する範囲は高さだけで決めない**。あご帯の高さで「窓の外」を見ずに消したら
  T-pose の腕（|x| 最大 0.9）が丸ごと消えた。輪郭クリップでも下限を切らずにやったら
  頭の真下にある脚・胴の前面が消えた。
- ⚠️ **隙間は「縫い目」だけ埋める**。最大5層（4.4cm）まで埋めたら、あごの下と首の間
  （本来空いている）が塊で埋まり、突起になって首を覆い隠した。
- ⚠️ **削り具合は「出す先の高さ」で決める**。元の高さで決めると、下から持ち上げたあご先が
  「首の材料」と判定されて削られず、突起として残る。
- ⚠️ 髪と同じで、**あごも「大きさ・位置をいじって直す」方向は失敗しやすい**。
  余裕や帯の位置を動かすたびに別の場所が壊れた。原因を実測で特定してから直すこと。

**検証**（`headless_sim/probe-jaw.ts` / `probe-seethrough.ts` / `probe-skinpoke.ts` / `probe-legs.ts`）

| 項目 | 結果 |
| --- | --- |
| あご先（前へ張り出す一番下） | 標準 0.245m → 丸顔 0.210m / 細あご 0.201m |
| あご下の前方への張り出し | 0.096 → 0.026（丸顔・細あご） |
| レイの貫通（透け） | 3形状とも 0 |
| 首の輪の欠け | 全層 16/16 方位 |
| 服からの肌のはみ出し | 0/16 |
| 脚・腰胴・腕肩 | 3形状とも完全一致 |
| 髪の下の頭皮の塗り | 維持 |
| `setJaw()` の張り直し | 独立ビルドと完全一致 |

⚠️ **残件**: 丸顔・細あごで、正面右側の目の高さあたりに1段の横線状の突起が出る（ユーザー報告）。未着手。
⚠️ ブラウザ実機での見た目は都度ユーザー確認に依存。ヘッドレスの数値だけでは判断できない。

## 2026-09-05（続き2） 顔（目・口）を描いて貼る／髪型139種／髭を顎へ

**1. 目と口を「描いて顔に貼る」**（`src/voxraw.ts` `faceMarks`）

実測して分かったこと:
- 顔の前面の色は肌色 70〜75 階調だけで、**唇も眉も色として残らない**
- 暗い領域は唇ではなく**口の中と眼窩**（肌より最大 88mm 奥）
- ソースの眼球メッシュは瞼の内側で肌より **26mm 奥**

→ 元モデル（function-lab の `buildParts.mjs` `splitFace`）と同じく**描く**。
  `eyeBlock(3,2)`（白目3×2・黒目は上段中央1マス）と `mouthLine(HW,1,0)`（横 2*HW）を、
  **body の縦横半分のボクセル(4.37mm)** で倍の枡目にして再現: 白目6×4・黒目2×2・口8×2。
- 眼球パーツは preprocess で削除（material eyes/lashes を落とす）
- 貼り付けは「そのマス自身の列にある一番手前の肌」から 1/4マス食い込ませる＝3.3mm 手前に出る

⚠️ 落とし穴:
- 升目は `Math.round(x/S)` ではなく**セル番号** `floor((x-原点)/S)` で引く。格子の原点は
  ボクセル寸法の倍数ではないので、丸めると隣のセルを見る（実測で 64マス中14マスが 9.8mm 埋没）
- 貼り付け先が空のときは周囲の**一番奥**を採る。一番手前を採ると浮く（実測5マス）

**2. 髪型 139 種 + 遅延読み込み**
- 20MB・438ファイル。全部読むと確認ページが開かないので、**選んだときに読む**（`loadHair`）
- 最初の読み込み 2.2秒・メッシュ7個 / 1種あたり約60ms
- 髪型は単色(14,6,6)で焼けるので、**モデル本来の髪の色分布(182階調)を位置ハッシュで写して**まばらに
- 髪の位置は body のボクセル2個ぶん(17.4mm)上げた（`HAIR_LIFT`）

**3. 髭を顎へ**（`add_hair_player_one.py` `is_beard`）
- コレクションには髪型でなく**髭単体のパーツ**が混ざる。髪型と同じ規則で置くと、実測で
  Man_Hair_133 は「頭頂から 0〜0.078m＝額の高さ」かつ「顔の表面より内側」＝**顔の中に埋まる**
- 髭が乗るべき場所（実測）: 頭頂から 0.159〜0.243m、あごの肌の前端 Y -0.109〜-0.090 より前
- 判定は「顎に収まる大きさか」＝スケール後の実寸で 幅≤22cm・奥行≤16cm・高さ≤17cm の**3辺すべて**。
  ⚠️ 「頭の幅・奥行の3/4を張れるか」で判定したら、幅5.9cmのポニーテール(052)や高さ44cmの
  長髪(130)まで髭にされた。7件（041/042/047/072/080/098/133）が髭。
- 髭付きの髪型（実測 74/139 件）はあごの3形状に追従する。`reshapeJaw` は**体から求めた頭の情報**
  (`JawFit`) を受け取る形にした。髪型自身から中心を取ると髭だけの範囲で計算して顔からずれる。
  首の輪郭クリップと隙間埋めは body 専用（髪に掛けると髭が丸ごと消える）

**4. 頭皮を髪色に塗る機能は削除**（髪の位置調整で対応するため不要になった）

**この期間に踏んだ落とし穴（再掲・重要）**
- ⚠️ **逆写像は1点で引いてはいけない**。縮小率0.8だと元の格子を1.25個おきに飛ばして拾い、
  薄い表面が抜けて穴が開く（実測: 丸顔で Blender Y=0 の面が丸ごと消え左右から貫通）。
  セルが覆う範囲を全部見る。ただし範囲を**ちょうど半ボクセル**にすると倍率1.0でも隣を拾って
  1ボクセル膨らみ、目の高さに横線状の段差が出る。0.49 ボクセルにすること。
- ⚠️ 削除する範囲は高さだけで決めない。腕（|x|最大0.9）や脚の前面が丸ごと消えた。
- ⚠️ **再修正の指示を受けたら、まず前回の修正をリバートしてから着手する**。失敗の上に
  重ねた結果、直っていた「あごの輪郭」まで崩した。ユーザーから明確に指摘された。

⚠️ 残件: 後頭部が縦にまっすぐで、首にかけて細くなるカーブが無い（ユーザー指摘）。未着手。

## 2026-09-05（続き3） 後頭部・ユニフォームの粒度・モーションの腕の角度

**1. 後頭部が縦にまっすぐだった**
ボクセル化の内部塗り潰しは `(x, y)` の列ごとに**垂直にレイを飛ばし交差の偶奇**で内側を決める。
body は髪・頭皮・目を別パーツへ分離したため**穴が空いており**、偶奇がずれて
「後頭部の膨らみと肩の間のくぼみ＝うなじ」まで塗り潰されていた。

| 頭頂から | 元メッシュ | 修正前 | 修正後 |
| --- | --- | --- | --- |
| 0.081 | 0.1082 | 0.107 | 0.107 |
| 0.169 | 0.0853 | 0.107 | **0.088** |
| 0.244 | 0.1070 | 0.107 | **0.116**（肩へ） |

→ body に `--no-interior` を付けて表面のみで焼く。ボクセル 18,136 → 16,485。

**2. ユニフォームの粒度を body に揃え、柄だけ細かく貼る**
- 衣類は 6.26mm(scale 3) で body(9.38mm) より細かかった → **9.38mm(scale 2)** に統一
- エンブレム・背番号・NAME・ロゴだけ `jerseymark` として **4.69mm(scale 4 = 半分)** で貼る
- 抜き出しは `add_jersey_marks.py`。Shirt.jpg の赤でない画素を連結成分に分けると
  背番号 60x94 / エンブレム 29x36 / NAME の文字 12〜15x16（縦横比 1.0〜1.6）に対し
  **サイドのストライプは 23〜26x217〜228（縦横比 8.7〜9.8）**。縦横比 4 で分かれる
- ⚠️ 柄は文字の周りのポリゴンごと抜くので**地の赤が混ざる**（5,279個中2,476個）。
  voxraw 側で「土台の一番多い色に近いボクセル」を落とす（実測で混入 0）

**3. すべてのモーションで腕が横に開いていた**（ユーザー指摘。実測で確定）
`applyMotion` はクリップの回転を**ボーンのローカル回転としてそのまま代入**する。
ローカル回転の基準は rest の向きなので、rest が違うとその差が全モーションのずれになる。

| | 上腕の rest（水平からの下がり） |
| --- | --- |
| ゲーム標準 `male_avatar`（クリップはこのリグ前提・ARP） | **27.4°** |
| player_one（T-pose） | **0.0°** |

実測 idle 34.0° + 27.4° = 61.4°（自然な立ち姿）と一致したので原因確定。
→ `preprocess_player_one.py --apose` を有効化して rest を A-pose で焼く。
⚠️ 以前「transplant=none なので A-pose 化は不要」と判断したのは誤り。**モーションが rest を
　 基準にしている**ことを見落としていた。
⚠️ A-pose の回転は「rest の向きから」計算すると FBX 付属ポーズのぶんずれる
　 （残差 7.5°・結果 21.3°）。**今の向きから**目標へ回すこと（残差 0.0°）。

結果: idle 34.0°→**61.4°**、手の横方向 0.590→**0.426m**。run 28.6〜35.1°→41.8〜60.5°。

**⚠️ 未対応（次の作業）: 身長・体重による体格の変化**
- 選手データには 4015 人分の `heightCm` / `weightKg` があるが、`src/` で使っている箇所は **0件**
- `buildRawModel(scene, baseUrl, {jaw})` は身長も体重も受け取らない
- 材料: `restPose.ts` の `scaleToHeight` / `scaleRestPoseXYZ`(軸別)、あごで作った逆写像の変形
- 方針: ボクセルは焼き直さず**表示時に変形**。まず身長だけ入れて実機確認 → 次に体重による厚み

## 2026-09-05（続き4） 身長・体重による体格

**1. 身長**（`setHeight` / `setBody`）
ボクセルは焼き直さず表示時に伸縮する。
- モデル自身の身長は **180.4cm**（足元＝全部位の最下端 〜 頭頂＝**body** の最上端）を実測して使う
  ⚠️ `grid.json` の bb を使うと髪型139種の高いものが混ざって 206.3cm になる
- 頭だけ伸縮率を変える（`HEAD_EXP = 0.5`）。一様に拡大すると背の高い選手ほど頭が大きい子供体型になる
- 頭を別倍率にすると単純な比では身長が合わない（実測で最大1.1cm ずれ）ので、目標身長に一致する
  倍率を反復で解く

| 指定 | 全体 | 頭 | 実際の頭頂 | 頭の大きさ |
| --- | --- | --- | --- | --- |
| 158cm | x0.869 | x0.932 | 158.2cm | 18.8cm |
| 180.4cm | x1.000 | x1.000 | 180.6cm | 20.2cm |
| 203cm | x1.133 | x1.064 | 203.2cm | 21.4cm |

**2. 体重**（厚み）
厚み倍率 = **sqrt(BMI / 23.1)**。23.1 は選手4015人の BMI 中央値（範囲 17.0〜29.4）の実測。
- **骨の軸から見た横方向だけ**伸縮する。体の中心から一律に伸ばすと腕が外へ飛ぶ
  （T-pose なら 0.9m の位置が 15% 外＝13cm 移動）
- 骨ごとの効き方 `THICK_BY_BONE`: 頭・首 0 / 手足 0.3 / 肩 0.6 / その他 1.0
- ボクセルの箱も同じ割合で広げるので隙間ができない（伸び方は骨の向きで軸ごとに変わる）

| 体重(180.4cm) | BMI | 厚み | 胴の幅 | 胴の厚み | 頭の幅 |
| --- | --- | --- | --- | --- | --- |
| 55kg | 16.9 | x0.855 | 0.291 | 0.235 | 0.188 |
| 75.2kg | 23.1 | x1.000 | 0.338 | 0.272 | 0.188 |
| 95kg | 29.2 | x1.124 | 0.379 | 0.304 | 0.188 |

**3. 細い体型で袖と胴が離れた**（ユーザー指摘）
一番強い骨だけで変形すると、袖(腕の骨)と胴(胸の骨)が別々に縮んで脇に隙間ができる。
→ **スキンウェイトで最大4本の骨の変形を混ぜる**（`thickenBlend`）。

| 体重 | 脇の下の隙間 修正前 → 修正後 |
| --- | --- |
| 50kg | **17.6mm → 7.6mm**（肩は 22.2mm → 9.0mm） |
| 75.2kg | 9.4mm → 9.4mm |

ボクセル1個が 9.4mm なので、修正前は約2個ぶん空いていた。

⚠️ 計測の注意: このユニフォームには**袖がある**（高さ1.45で jersey の幅 0.75m）。
   「胸の幅」を 1.35〜1.45m で測ると袖が混ざる。袖の無い 1.15〜1.25m で測ること。

**⚠️ 未対応（次の作業・ユーザー要望）**
- 細い体型の肩幅が広い。肩幅も体格に応じて狭くしたい
- 脚は太さが変わるが**腕がほぼ変わらない**。原因: 厚みの計算で横方向(Blender x,y)しか
  伸縮しておらず、腕は軸がほぼ水平なので**垂直方向(z)の太さが変わらない**。
  perp を3軸とも伸縮すれば直る（箱の倍率は各軸 m + (1-m)*axis_i^2）

**追記: 腕の太さと肩幅**（ユーザー指摘の2件）
- **腕の太さが変わらなかった**: 厚みの計算で骨の軸に垂直な成分のうち横2方向(x,y)しか
  伸縮していなかった。腕は A-pose で軸が (0.89, 0.06, -0.46) とほぼ水平なので、太さの向きに
  含まれる**垂直成分が伸縮されず**ほぼ変わらなかった。→ **3軸すべて**で伸縮する。
  軸に沿う成分は伸ばさないので手足の長さは変わらない。箱の倍率は各軸 `m + (1-m)*axis^2`。
  実測: 前腕の縦 55kg 0.131→0.118 / 95kg 0.150→**0.162**（修正前はほぼ一定だった）
- **細い体型の肩幅が広い**: 厚みの変形は骨の位置を動かさないので肩幅は変わらない。
  → 肩の骨自体を体格に応じて動かす（`SHOULDER_W = 0.7`）。
  ⚠️ 肩の骨だけでは +6.4% しか変わらない。実測で Chest→Shoulder 0.067m に対し
  **Shoulder→UpperArm（鎖骨）0.135m** が主。両方動かすこと。
  実測: 肩幅 55kg **0.363** / 75.2kg 0.404 / 95kg **0.439**

---

## 2026-09-11 パス・シュートの反動動作（ワインドアップ）

**要望**: パスやシュートがほぼノーモーションで出ている。打つ前に小さくとも反動動作
（腕を少し逆へ動かし、一瞬止めてから投げる）を入れたい。

**計測した原因**（`probe-windup` / `probe-wind2`）
- パス 641 本のうち**溜めがあったのは 15%**。残りはその場で即リリース。
  溜めが入っていたのはターンパス（ピボット）とトラップ越しのキックアウトだけだった。
- シュートの溜めは**中央値 0.017秒 = 1フレーム**。原因は `updateCharge` の
  「目の前まで詰められたら溜めを打ち切る」判定（`ndGap < 1.05 + 反応*0.55`）が
  **溜め0秒でも発火**し、守備者が間合いに居るだけで初回フレームに放っていたため。
- さらにシュートの **57% は `finishAtRim`（レイアップ/ダンク）** で、そもそも charge を
  通らない。こちらは踏み切りの跳躍自体がモーションなので今回は触っていない。

**入れたもの**
- `PASS_WIND = { base: 0.13, perM: 0.008, min: 0.10, max: 0.30 }`（config）と
  `PASS_STYLE[].wind`（chest 1.0 / bounce 1.1 / overhead 1.25 / jump 0.6）。
  距離が伸びるほど大きく引き、P速度が高いほど短い。片手投げは半分。
  強制パス・空中アウトレット・ターンパス（ピボットが反動を兼ねる）は溜めない。
- `pendingPassWind` / `pendingPassDur` / `windReleased` を追加。安全ゲートを通った
  （＝投げると決まった）地点で溜めに入り、溜め切ってから `windReleased` で放つ。
  ⚠️ リリース時にゲートを通し直すと「溜める→拒否→また溜める」で持ち続けるので通さない。
  カット判定（`evalInterception`）はリリース時に走るので、**溜めている間に詰めた守備者は
  間に合う** — 溜めがカット判定に効く形になっている。
- シュート: `QUICK_RELEASE = 0.17 - シュート技術*0.05`。詰められた時の打ち切りと
  ブロック差し込みの早撃ちは、この最低限を溜めてからにする（クロック逼迫だけ例外）。
- ポーズ: 溜め中は `windBallX/Y/Z`（**溜めの1フレーム目のボール位置**）を起点に、
  胸のすぐ前（パス）／おなかの前（シュート）へ smoothstep で引き付け、`u>0.55` で止める。
  手は `catchBall` でボールを追うので、腕がそのまま投げる方向と逆へ動いて止まる。
  ⚠️ 固定の構えへ**瞬間移動させてはいけない**。最初 `passToReceiver` の中で起点を
  捕まえたら、同じフレームでドリブル側がボールを上書きするため 1 フレームずれて飛んだ。
  起点は**次フレームのポーズ側で遅延して捕まえる**（`windBallY = -1` を番兵にする）。

**実測（前 → 後）**
- 溜めがあったパス 15% → **67%**（中央 0.167秒）。残りは強制・空中アウトレット。
- シュートの溜め 中央 0.017秒 → **0.133秒**。charge を通る割合 33% → 43%。
- 引き量（投げる方向を + として）パス 中央 **0.106m** / シュート **0.080m**、
  引き切ってからの静止 0.067秒。
- 副作用: FG 58% → **55%**（ミドルジャンパー 56% → 49%）。守備に 0.13 秒ぶん
  余裕が出たぶん。実在値（2P 約52%）へ寄る方向。得点 15.3 → 14.7（4分ゲーム）。
  ブロック 4.00 → 4.33 本。**ターンオーバーは 1.8 → 1.3 と増えていない**。

### 追記: シュートは「頭上に構える前」が反動動作（ユーザー指摘）

- 指摘: 反動動作は**頭上でボールを構える前**の動きにして、構え終わりにはボール自体が
  頭上にある状態にしてほしい。
- 直前の実装では、溜め中ずっとおなかの前で、**リリースの瞬間に releaseShot が
  2.05m へワープ**させていた（計測: 構え終わりのボール高さ 中央 1.25m）。
- 溜めを3段に作り替えた（進捗 0..1、`setChargeBall`）:
  A) 0〜0.35 反動 = 今ボールがある位置からおなかの前(≈1.09m)へ引き付けて沈む
  B) 0.35〜0.50 そこで一瞬止まる
  C) 0.50〜1.0 頭上の構え(2.05m ＝ リリース点)へ持ち上げる → そのまま放つ
  実測: 構え終わりのボール高さ 中央 **2.05m**。
- **急ぎ撃ちは「途中で切る」のをやめた**（`rushCharge`）。詰められたら残りの進捗を
  短い秒数で駆け上げる。進捗（`chargeP`）で持っているので、残り時間を縮めても
  進捗が飛ばない＝ボールがワープしない。精度の `prepDone` は実際に溜めた秒数。
- `MIN_MOTION = 0.42秒`。⚠️ ポケット(1.09m)から頭上(2.05m)まで 0.95m あるので、
  これより短いとボールが 1フレーム 0.37m（22m/s）で駆け上がって飛んで見える。
- ⚠️ **溜めの頭から守備者を走らせない**（`chargeP >= CHARGE_LIFT` でクローズアウト開始）。
  抱えている段階ではまだシュートと決まっていない。入れないとモーションを長くしたぶん
  一方的に成功率が落ちた（ミドル 49% → 35%）。コンテストの間合いも振り上げ開始で凍結する
  （`chargeDDefLift`）— 上がり始めた後に着いた守備者は遅れたクローズアウト。
- 実測（72試合）: 全体 57% / 3P **32%** / ミドルジャンパー **46%**。
  変更前は 3P 29% / ミドル 56%。ミドルが実在値(約40%)へ寄った。

### 追記: 守備の動き・方向転換・ジャンプの反動動作

**1) 加速の傾き（全選手・見た目のみ・シム影響なし）**
- `Player.accX/accZ` に平滑化した加速度を持ち（`tickMotion`、`ACC_SMOOTH = 11`）、
  `sync()` の既存の傾き（`lean`）に足し込む。踏み出しは進む方へ、ブレーキは逆へ倒れる。
- `ACC_TILT = 0.015 rad / (m/s^2)`、上限 `ACC_TILT_MAX = 0.16`（≈9°）。
  実測: 動いている選手の傾き 中央 5.2° / 95% 11.0°。

**2) 切り返し・急停止のプラント**
- ⚠️ 加速度からは踏ん張りが出てこない。**シムは方向転換を1フレームで済ませ、減速を
  経ない**ため（実測: プラント中に加速度が進行方向と逆だったのは 7% だけ）。
- そこで既にある `plantT`/`plantDur`（切り返し・急停止のスロットル）から直接出す。
  プラントに入った瞬間の進行方向を `plantDirX/Z` に保存し、体を**その逆**へ倒す
  （`PLANT_TILT = 0.23 rad ≈ 13°`）、`plantT` が減るにつれ起き上がる。
  ⚠️ 符号に注意。元の進行方向へ倒すと「流れているだけ」で反動に見えない。
  ⚠️ `setPlant()`（スティールの踏み込み等）でも向きを必ず入れ直す。入れないと
  前のプラントの向きが残る。
- 実測: プラント中の傾き 中央 8.7° / 95% 20.7°。

**3) 踏み切り前の沈み込み（ジャンプ）**
- `jump()` はその場で離陸せず、`loadT = jumpLoadFor(p, height)` を立てるだけにした。
  `updateJump` が使い切った瞬間に離陸する。沈み込み中 `airborne` は **false**。
- 見た目は `applyShootLoad` を流用（`POSE_JUMP = {lean:0.12, thigh:0.46, shin:0.30}`、
  ほぼ垂直に沈む）。`voxel-motion` も `jumpLoad` 中はクリップを止める。
- リバウンドの踏み切り判断（`looseball.chaseLoose`）は沈み込みぶん**早く跳び始め**、
  頂点の時刻も `L` だけ後ろにずらして解く。
- ⚠️ **長さは実測で決めること**（60試合、`probe-rebside`）:
  - 0.10〜0.20秒 → オフェンスリバウンド **32%**（実在値 22〜28% を超える）
  - 0.03〜0.10秒 → **27%**（帯の中。従来の 26〜28% を維持）
  空中の競り合いは数フレームに敏感で、全員の踏み切りが遅れると落下点で競れず、
  こぼれ球が増えて攻撃側に回る。採用は `(0.078 - 敏捷性*0.028) * 深さ` = 0.03〜0.10秒。

**全体の実測（12試合・各4分、変更前 → 後）**
- シュートの溜め 中央 0.017秒 → **0.433秒** / 溜めがあったパス 15% → **68%**
- ブロック 4.00 → 4.08 本 / ターンオーバー 1.8 → 2.2
- 得点 15.3 → **13.6**（要観察。モーションを短くすれば戻るが、その分ノーモーションに近づく）

### 追記: スティールを「突き(パンチ)」モーションにする

- 指摘: ほぼ棒立ちのまま体を斜めにして腕を振っているだけに見える。腕を前に突き出し
  ながら上半身を回転させてリーチを長くし、シュートと同じく小さな反動を入れてほしい。
- 旧 `digReach` は**毎フレーム同じ最終形**を当てていた（胸をボールへツイスト＋肘を
  伸ばしてロック）。段階が無いので「伸ばしっぱなしの腕が付いてくる」ように見えていた。
- スティールは既に `beginAction("steal", 0.12, 0.03, 0.6)`（溜め→突き→回復）の段階を
  持っている。`digReach` をその段階に同期させ、**ボールに当たるフレームと腕が伸び切る
  タイミングを揃えた**（`punchPhase`）。段階を持たない密集のはたき（`game.steal`）は
  `stealReachDur` を立てて時間で割る（溜め 30%）。ルーズボールの掻き出しは従来どおり
  溜め無しで伸ばす。
- 動きの中身（⚠️ **体幹→肩→肘**の順。肘を先に伸ばすと「腕を振っている」だけに見える）:
  - 溜め: 胴を的と**逆**へ 0.40 倍ひねる、肘を 1.20rad 畳んで手を胸元へ引く、少し後傾
  - 突き: ツイストを振り抜いて的へ → 肩が先行(`ramp 0→0.85`) → 肘が遅れて伸び切る
    (`ramp 0.30→1.0`)。反対の腕は逆へ振れてカウンターウェイトになる
  - 踏み込み: `digLeanX/Z` を `sync` の傾きへ足す（溜め -0.05rad → 突き +0.16rad ≈ 9°）
- 実測（`probe-steal`、6試合／7本）: 踏み込み **-0.050 → +0.160**、胴のツイスト
  **-0.108 → +0.475rad(27°)**、手のリーチ 0.486 → 0.550m。
  **逆へ引いてから突いた割合 100%**。
- ⚠️ 未対応: 踏み込みの**足のステップ**は入れていない（`pos` を動かすとスティールの
  成否判定の間合いが変わるため）。体の回転と前傾だけで「突き」を見せている。

### 追記: スティールの踏み込みステップと、マーク位置の基準

**間合いの実測（ユーザー指摘の確認）**
- 指摘どおりだった。担当守備者との距離: リム〜3m 中央 **0.79m**（25% 0.62m）、
  3〜6m **0.99m**、6〜9m 1.46m。`BODY_MIN_DIST = 0.62m` に張り付いている帯があり、
  実在のオンボール（0.9〜1.5m）より明確に近い＝ほぼフェイスガード。
- 原因は2つ:
  1. 深さの係数の**下限** `0.45 + 0.55*depth` → リム前の目標が 1.25*0.45 = 0.56m。
     → `0.68 + 0.32*depth` に変更（遠い側は据え置き）。下限クランプ 0.3 → 0.42。
  2. `postUp` が「ビッグがリムから5.5m以内」で発火し、フェイスアップのビッグまで
     0.35m のボディアップになっていた。→ 背負っている(`postT>0`)か本当のローポスト
     (3.2m以内)のときだけに限定。
- 結果: リム〜3m **0.91m** / 3〜6m **1.02m** / 6〜9m 1.44m。
  ⚠️ リム〜3m は「クッションの目標」ではなく**ドライブの接触**で決まるので、式を
  いじってもほとんど動かない（0.79 → 0.91）。ここは実在でも体が当たる間合い。

**踏み込みステップ**
- `stealLunge`: 突きの溜めで体ごとボールへ **0.34m** 詰め、回復（0.6秒）で戻る。
  体が重なる距離（`BODY_MIN_DIST`）までしか入らない。
- ⚠️ これは見た目だけではない。スティールの成否は**実行フレームの間合い**
  (`gap < reach`) で決まるので、踏み込んだぶん届く。
- ⚠️ **仕掛けの射程も広げないと踏み込めない**。従来は `gap < reach`（腕が届く距離）
  でしか仕掛けず、突きは常に密着から始まっていた（実測: 仕掛け時 0.70m、
  踏み込めたのは 0.146m だけ）。`gap < reach + LUNGE_IN` に変更。実行フレームの判定は
  `reach` のままなので、届かなければ空振り＋隙（`reactT = 0.3`）になる。
- 実測: 踏み込み **0.340m**、マークとの距離 0.928m → 0.721m。

**突きの胴回転**
- 的へ向けるツイスト(tw)は守備者が既に正対しているぶん小さく（実測 0.475rad）、
  それだけではパンチに見えない。**tw を追い越して**突く側の肩を前に出す
  （`PUNCH_TWIST = 0.50` / 溜めで `COCK_SHOULDER = 0.30` 引く）。
- ⚠️ 回転の符号はリグ依存。推測せず `probe-steal` で「手がボールの向きへどれだけ
  出るか」を実測して決めた: **-1 で 0.586m / +1 で 0.509m / 回転なし 0.399m**。
  → `PUNCH_SIGN = -1`。回転を入れるだけでリーチが **0.399 → 0.586m** 伸びる。

**バランス（マーク位置を広げた副作用）**
- 広げただけだと守備が緩み、ミドルジャンパーが 46% → **63%** に跳ねた。
- シュートの溜めのどこからクローズアウトを始めるか（`CLOSEOUT_AT`）で調整:
  | CLOSEOUT_AT | ミドル | 3P | 得点 |
  |---|---|---|---|
  | 0.50（振り上げから） | 63% | 23% | 15.8 |
  | 0.30 | **41%** | **40%** | **16.1** |
  | 0.0（溜めの頭から） | 46% | 21% | 13.3 |
  → **0.30 を採用**。実在値（3P 約36% / ミドル 約40%）に最も近く、得点も戻った
  （溜め導入で 13.6 まで落ちていたものが 16.1）。
- ターンオーバー 2.0／ブロック 3.33本。
- 大きい標本での確認: シュート 72試合 → 全体 59% / **3P 34%** / **ミドルジャンパー 45%** /
  リム至近 69%。リバウンド 48試合 → **オフェンスリバウンド 28%**（実在値 22〜28% の上端）。

### 追記: 確認ページにスティールを追加（試合と同じ経路・その場で調整）

- 確認ページ（`src/confirm.ts`）に「スティール」を追加。⚠️ **試合とまったく同じ手順**で動かす:
  `beginAction("steal", …)` → `tickAction` で段階を進める → `stepLunge` で踏み込み →
  `digReach` でポーズ → `Player.sync()`。確認ページ専用のポーズ処理は一切書いていない。
- そのために調整値を**共有の可変オブジェクト**へ出した。⚠️ 定数に戻さないこと。
  戻すと確認ページで見たものと試合が別物になる（このページが作られた元々の理由）。
  - `animation/action/reach.ts` → `export const PUNCH = { twist, cockShoulder, cockElbow,
    cockLean, lungeLean, thrustT, sign }`
  - `ai/defense/vs-onball/index.ts` → `export const STEAL = { lungeIn, windup, active, cooldown }`、
    `export function lungeWant(d)` / `stepLunge(d, bx, bz, room)`（試合側の `stealLunge` は
    この2つの薄いラッパー）
- スライダー: 相手との距離／相手のボールの高さ／踏み込む距離／溜めの長さ／伸び切るまで／
  肩を出す回転／溜めで肩を引く／溜めで肘を畳む／踏み込みの前傾／胴回転の符号。
  現在値は情報欄に表示するので、良い値が出たらそのままソースの初期値へ書き戻せる。
- ボールは床のアンカーに固定して置く（踏み込みで選手が前へ出るのが見える）。
  突きは 0.6 秒間隔でループ。
- ⚠️ `stepLunge` は試合と同じく**毎フレーム**呼ぶ。段階中だけ呼ぶと終了フレームの
  踏み込みが戻らず、ループのたびに選手が前へジリジリ出ていく。
- ⚠️ `stealLunge` の `clampCourt` は**動いた時だけ**呼ぶ。毎フレーム丸めると、
  ライン外に立っている守備者をコート内へ押し戻してしまう（スローイン時の配置が変わる）。
- `npx vite build` 通過（confirm チャンク 10.06 kB）。

### 追記: 突きの作り直し（ユーザー指摘4件＋リバウンドの同型バグ）

**1) 小刻みな反復運動（致命的）— 閉ループだった**
- `digReach` が決める突く腕は `dribbleWithRight()` を使い、その中で
  `th = root.rotation.y + torsoTwist` を読んでいた。ところが `digReach` は**自分で
  `torsoTwist` を書く**。入れたひねりが次フレームの左右判定を裏返し、
  腕と胴が毎フレーム左右に振れていた（実測: 判定の入れ替わりが毎フレーム）。
- ⚠️ **腕の左右を決める式に `torsoTwist` を混ぜてはいけない**。体の向き(root のヨー)
  だけで決め、決めた側は突いている間ずっと保持する（`punchRight` / `punchHoldT`）。
- **リバウンド側も同じバグ**だった。`stretchOne` が `torsoTwist` を書き、
  `leadArm()` がそれを読んでいる。`SIDE_CLEAR`(0.25m) の境目あたりで同じ振動が出る。
  同様に root のヨーだけで決め、`leadRight` / `leadHoldT` で保持する。
- 実測: 左右の入れ替わり **0 回**（numberSide 両方）。

**2) 当たる瞬間に腕が伸びていなかった**
- `beginAction("steal", 溜め, 突き, 回復)` の **溜めが明けたフレーム(actFired)が
  ボールに当たる瞬間**。以前は「溜め＝引き / その後＝突き」にしていたので、当たる
  瞬間はまだ引いた形で、伸び切るのが 0.13 秒遅れていた。
- 引きも突きも**溜めの中**で終わらせる（`PUNCH.cockFrac` で配分）。溜めを 0.12→**0.24秒**へ。
- 実測: 当たるフレームで突く手が 0.69m 出ている（以前は 0.33m ＝ 引いた形）。

**3) 逆手が前に出ていた**
- 実測で溜め中に前へ 0.13m 出ていた。パンチの逆手は前に出さない。
- 形は「歩く腕振りと同じ肘を曲げたフォーム」（肘 1.00rad 固定、上腕を後ろへ振る）。
- ⚠️ 符号は掃引で決めた（`probe-backarm`、numberSide 両方で同値）。肘 1.0rad のとき
  上腕の振り **-0.8→手首 -0.12m(後ろ) / -0.4→+0.05 / 0.0→+0.22(前)**。負が後ろ。
  採用: 溜め -0.55 → 突き -1.00。実測の最前 -0.045m（静止時 -0.059m ≒ 前に出ていない）。

**4) 突く側の肩が前に出ていなかった（符号が numberSide で反転）**
- ⚠️ 胴のひねりに加える「肩を前に出す」成分は **numberSide を掛ける**。
  掛けないと片側のコートでだけ逆の肩が前に出る。実測（当たるフレームの肩の前後）:
  掛けない場合 numberSide=+1 で **-0.114m（逆側が前）** / -1 で +0.114m。
  掛ければ両方 **+0.114m**。的へ向けるツイスト(tw)はワールドのヨー差から出すのでそのまま。

**5) 上から振り下ろす軌道 → 水平へ**
- 引き手を胸の高さに置いていたので、そこから的へ下りる軌道になっていた（1.26→0.95m）。
- 引き手の高さを「**手が最後に届く高さ**」に合わせる。的が腕の届く範囲なら的の高さ、
  届かないなら腕を伸ばし切った時の高さ（`endY`）。届く的は IK で手先を的に乗せる。
- さらに肩が的より高いぶんは**膝を折って肩を下げる**（`digLoad` / `POSE_DIG`）。
- 実測: 手の高さの振れ幅 0.31m → **0.15m**。⚠️ 完全な水平にはならない。肩を中心と
  した円弧なので、体の横から前へ出す以上は必ず上がる。これ以上は沈み込み
  （確認ページの「沈み込み」スライダー）で詰める。

**踏み込みが潰れていた**
- ⚠️ 突きの溜めの間は通常の間合い取り(`defendOnBall`)を止める。止めないと溜めの
  0.24 秒で体が当たる距離まで詰めてしまい、踏み込む余地が消える
  （実測: 踏み込み 0.34m → 0.08m）。間合いを詰めるのは踏み込みの役目。
  実測: 踏み込み **0.157m**、ターンオーバー 1.8 → **2.3**／試合。

### 追記: スティールの頻度と賭け、体当たり、シュートフェイク

**1) 1on1 でスティールをほぼ使っていなかった**
- 実測: **1試合 1.0 回**。担当守備者が 1.4m 以内に居る時間の **0.9%** しか突いていない。
- 原因は仕掛けの確率が低すぎたこと。`STEAL.base` 0.03 → **0.17**、
  `STEAL.hands`（手の速さの効き）0.10 → **0.36**、`STEAL.minClose`（一歩離れていても
  仕掛ける下限）**0.35** を追加。→ 試合あたり **4.4 回**。
- ⚠️ 頻度は 0.26/0.52 まで上げると 7.4回/試合になるが、得点が 14.8 → 13.4 まで落ちた。
  上げすぎない。

**2) 賭けの代償（空振り＝隙）**
- 空振り時（実行フレームに間合いが足りない）:
  `reactT = 0.35 + (1-敏捷性)*0.55` ／ `setPlant(0.30 + (1-敏捷性)*0.60)` ／
  ハンドラーに `beatenT = 0.30 + 敏捷性*0.35`。体を前へ預けたぶん重心が戻るまで
  守備へ行けず、抜き去られた扱いになる。素早い選手ほど早く立て直す。

**3) シュート前の体当たり（ブロックさせない）— `clearOut`**
- ⚠️ ダンク/レイアップが簡単にブロックされる問題への直接の対策。`shoot` と
  `finishAtRim` の**両方の入口**で、1.35m 以内の守備者全員に判定する
  （ダブルチームなら複数人が崩れる）。
- ボディバランス(`attr.balance`)の差で押し勝つ。崩れた相手は `offBalT` の間
  **`jump()` が即 return する＝跳べない**。半歩押しのけ、`lean` と `setPlant` も入れる。
- 実測: 試合あたり **2.8 回**崩している。

**4) シュートフェイク — `tryShotFake`**
- S技術(`shotTech`)が高いほど仕掛ける。引っかかる確率は相手の守備・反応で下がる。
  引っかかった守備者は `contestLeap` で跳ぶ → 落ちてくる間にシュートへ入る。
- 実装: `decide` の**打つ判断を全部 `goShoot()` / `goFinish()` に通す**。仕掛けた
  フレームは打たずに持ち、`fakeT` が切れた次のフレームに `fakeGo` で本番へ。
  ブザーだけはフェイクしない（時間が無い）。
- ⚠️ `resetMotion` で `fakeT` / `fakeGo` を必ず落とす。持ち越すと次にボールを持った
  瞬間に即シュートする。
- 実測: 試合あたり **3.7 回**。

**全体の実測（12試合・各4分）**
| | 変更前 | 変更後 |
|---|---|---|
| スティールの仕掛け | 1.0 回 | **4.4 回** |
| ブロック | 3.83 本 | **2.33 本**（跳んだシュートの 22% → 16%） |
| 得点 | 14.8 | **17.4** |
| オフェンスリバウンド | 27% | 27% |
- シュート成功率（24試合）: 全体 58% / 3P 33% / ミドルジャンパー 45% / リム至近 70%。
  変更前（59/34/45/69）とほぼ同じ＝確率は崩さずに、得点とブロックだけ動いた。

### 追記: 反射の傾きは下半身だけ — 上半身と頭は水平に保つ

- 指摘: 方向転換・ストップ・切り返しの反射で**体全体が斜め**になっている。
  下半身と膝以下は斜めのまま、上半身はそのぶん逆へ回して頭・胴を地面に対して水平に。
- 傾きを2つに分けて持つようにした:
  - `tiltX/tiltZ` … 従来どおり root（＝全身）に当てる合計
  - `reflexTiltX/reflexTiltZ` … そのうち**反射ぶん**（加速・切り返し・急停止・突きの
    踏み込み）だけ。⚠️ 守備の重心移動(`lean`)は含めない — あれは体ごと傾けてよい。
- `applyUpright(vb, reflexTiltX, reflexTiltZ, sign)` を **Spine の親フレーム**で
  掛けて打ち消す（クリップあり／なしの両経路に差し込み、`blendPose`・`limitBoneRate`
  の前）。下半身（腰〜膝〜足）は root ごと傾いたまま、Spine から上だけ起き上がる。
- ⚠️ 符号はリグ依存（`vb.root` が numberSide で π 回っている）。推測せず
  `probe-upright` で実測して決めた。反射 11.5° を入れた時:
  | | 腰 | 膝下 | 胴 | 頭 |
  |---|---|---|---|---|
  | 補正なし(基準 0°) | 0.2° | 6.1° | 2.8° | 1.0° |
  | **sign +1（採用）** | 8.6° | 14.7° | **2.9°** | **1.2°** |
  | sign -1 | 8.6° | 14.1° | 19.5° | 18.3° |
  → +1 なら下半身だけが傾き、胴と頭は無傾斜時とほぼ同じまま。-1 は傾きが倍になる。
- ポーズのみの変更でシムには影響しない。`npx vite build` 通過。

### 追記: パスの飛距離とバウンドパスの跳ね位置

**1) パスが遠すぎた**
- 実測（12試合・602本）: 中央 6.2m / 90% 9.8m / 最長 19.2m。**10m超が 9%**、
  12m超が 3%。コートを横断する球が普通に出ていた。
- 原因は**距離のペナルティが全く無かった**こと。`chooseReceiver` の評価は
  「空いているか」と「リムに近いか」だけで、射程(`MAX_PASS`)内なら距離を問わず
  遠い相手が勝てた。
- `MAX_PASS` 13 → **11**、`LONG_PASS = { free: 8.0, cost: 1.2 }` を追加
  （8m を超えた分だけ 1m あたり評価を 1.2 下げる）。
- ⚠️ **効かせ方が重要**。短くしすぎるとボールが回らず得点が落ちる:
  | 設定 | 10m超 | 得点 |
  |---|---|---|
  | 制限なし（従来） | 9% | 15.3 |
  | MAX_PASS 9.5 / free 5.5 cost 0.9 | 3% | **13.1** |
  | MAX_PASS 11 / free 6.5 cost 0.55 | 7% | 13.8 |
  | **MAX_PASS 11 / free 8.0 cost 1.2（採用）** | **5%** | **16.3** |
  8m までは無罰、そこから急に重くするのが良い。普段の球回し(3〜8m)を邪魔せず、
  長い振りだけが消える。得点はむしろ上がった（近くの良い選択肢へ回るため）。
- ⚠️ 残る最長 17.5m は**スローイン**(`game.throwIn`)。ここは `passToReceiver` を
  通らないので `MAX_PASS` が効かない。1試合1本程度で、実際のバスケットでも
  ロングのスローインはあるので今回は触っていない。

**2) バウンドパスの跳ね位置**
- 跳ね位置 `kb` 0.58（前 58% / 後 42%）→ **0.75**（前 75% / 後 25%）。
  バウンド後は従来の **約6割**の距離になる（要望の「半〜3分の2」の範囲）。
- `passHeightAt` は判定と描画の単一ソースなので、当たり判定も同時に変わる。

**⚠️ 未対応（要判断）: ミドルと3Pの成功率が実在値から離れた**
- 60試合での実測: 全体 61% / 3P **23%** / ミドルジャンパー **61%** / リム至近 71%。
  スティール・体当たり・シュートフェイクを入れる前（72試合）は 59% / 34% / 45% / 69%。
- ミドルが 45% → 61%（実在 約40%）、3P が 34% → 23%（実在 約36%）。
- 原因の見立て（未検証）: シュートフェイク（3.7回/試合）で守備者が跳ぶと、その
  シュートはほぼ完全にフリーになる → ミドルが上がる。3P は溜め(0.42秒)にフェイクの
  0.30秒が乗るので、詰められて急ぎ撃ちになる割合が増えた可能性。
- 調整の当て先: `FAKE` の仕掛け確率／引っかかる確率（`tryShotFake`）、
  または `CLOSEOUT_AT`。ユーザーの要望（ブロッカーが居ても打てる）と直結するので、
  勝手に薄めず判断を仰ぐこと。

## 2026-09-12 選手のボクセルを一段大きくした（resolution 110 → 55）

**やったこと**
- `voxel-pipeline/configs/player_one.json` の `resolution` を **110 → 55**。
  `scale_factor` は据え置き（body=2 / jerseymark=4）。
  → body/髪/パンツ/靴のボクセルが **9.38mm → 18.75mm（ちょうど2倍）**、
  背番号などの jerseymark が 4.69mm → 9.38mm。共通グリッドは 18.75 → 37.5mm。
- 再生成は `uv run voxel-pipeline player_one --from voxelize --keep-work`（235秒）。
  ⚠️ `--keep-work` が無いと中間 blend を消され、prep の作り直しに3工程かかる。
  ⚠️ 実行前に `prep.blend` / `prep_hair.blend` / `nomod.blend` を
  `.bak_res110` としてバックアップ済み（`G:/voxel-pipeline-work/player_one/`）。
- 出力を `public/vox/player_one` へ差し替え。**18MB → 4.9MB**（441ファイルは同数）。

**ゲーム側で直したもの**
1. **顔の目・口が倍の大きさになる**（`MARK_GRID`）
   マスの実寸 = body のボクセル × `MARK_HALF`(0.5) なので、ボクセルを倍にすると
   目・口だけ倍になる。実測(`probe-facefit`)で **64マス中 38〜44マスが顔から外れ**、
   最大 98.9mm 浮いていた。マス数を半分へ: `{eyeW:6,eyeH:4,pupil:2,mouthW:8,mouthH:2}`
   → `{eyeW:3,eyeH:2,pupil:1,mouthW:4,mouthH:1}`。
   → 16マス中 14マスが肌に乗り、「隠れる恐れ」は 0 になった。
2. **設定身長より実寸が約1%高い**（`voxraw.ts` の身長測定）
   ⚠️ ボクセルの**中心**で上下端を測っていたので、描かれる立方体の両端半ボクセルぶん
   （合計1ボクセル）短く見積もり、そのぶん拡大率が大きくなっていた。ボクセルが粗いほど効く。
   実測: 193cm 設定が **195.7cm**（+1.4%）。立方体の上下端で測るよう修正 → **193.6cm**。
   全体で 設定比 0.9956〜1.0030（残りは 18.75mm 格子の量子化ぶん ±0.6cm）。
   ※ これは粗くして初めて見えた**元からのズレ**。細かいボクセルでは +0.9cm で目立たなかった。

**実測（すべて通過）**
- 146パーツ / 441ファイル、ボクセル数と weights が全パーツ一致（合計 134,371 ボクセル）
- 髪型 139種すべて読める（失敗0）。髭の 90/91 種があごの形に追従
- 26人が生モデルで組め、60フレーム動かして例外なし。背番号の板 26/26
- 服の透け 0 箇所 / 色は body・髪・ユニ・背番号・靴・靴下・顔すべて指定どおり
- `npx vite build` 通過

**⚠️ 未確認・残件**
- 目・口のマスが 16 中 8 で 5.2mm 浮いている。粗くする**前と同じ量**なので新規の劣化では
  ないが、実機で目立つなら `MARK_POS` の深さで詰める。
- 髪の持ち上げ量は prep 側に **8.7mm（旧ボクセル1個ぶん）**で焼き込まれている。
  新しいボクセル(18.75mm)の半分以下になったので、頭に少し沈んで見える可能性がある。
  直すなら `blender_scripts/add_hair_player_one.py` の持ち上げ量を変えて prep から作り直す。
- ブラウザ実機（`/` と `/confirm.html`）での見た目は未確認。

## 2026-09-12 ポリゴン（デフォルメ）路線の実現性を Blender で検証

**描画コストの実測（比較の前提）**
- 現状のボクセル（18.75mm）選手1人: メッシュ8枚 / **85,728頂点 / 40,896三角形**。
  選手ごとにメッシュを**複製**している（体型ごとに厚みを変形するため共有できない）。
  頂点データは1人 約4.6MB、26人で約119MB。コート上10人で 857k頂点 / 409k三角形。
- 元の Blender モデル（実写調・ボクセル化前）: **46,982頂点 / 88,852三角形**。
  → ボクセルは三角形こそ元の半分以下だが、**頂点は1.8倍**（立方体は面ごとに法線と色が
  違うので頂点を共有できない）。
- ⚠️ **グリーディメッシュ（面の結合）は 0%**（実測）。ボクセル1個ずつが固有の色を持つので
  隣接面を結合できない。解像度を下げる以外に減らす手段が無い。
- 衣類が全体の66%（ユニ27k＋パンツ16k＋靴下14k頂点）。体は12.6kだけ。

**検証結果（`--background` の Blender で実行）**

1. **体型変化 … できる（ボーンで）**
   - 身長: 脚と背骨のボーンを長さ方向 1.10 倍 → 1.815m → **2.150m（+18.5%）**
   - 体重(太さ): 胴と腿のボーンを太さ方向 1.25 倍 → 胴幅 0.388m → **0.885m（+128%）**
   - ⚠️ 入れ子のボーン（Spine→Spine1→Spine2）はスケールが**掛け算**になる。
     1.25 のつもりが 1.25³=1.95 倍になる。1段だけ掛けるか `inherit_scale='NONE'` が要る。
   - ⚠️ **シェイプキーは全メッシュ 0 個**。腹の出方・筋肉の付き方のような「形」の変化は
     ボーンでは出ないので、必要なら Blender で自作する。

2. **髪の差し替え … できる**
   - `C:/Users/user/Downloads/hair/Man_Hair_Collection_fbx/Man_Hair_Collection.fbx`
     に **279 メッシュ**。アーマチュア無し → Head ボーンへ 100% ウェイトで付ける
     （既存の `add_hair_player_one.py` と同じ手順）。
   - ⚠️ **1つ 4,116 三角形（中央値、最小3,338 / 最大217,600）**。
     現状のボクセル髪 2,152 三角形の**約2倍**。デフォルメの体（3〜8k想定）と同じ重さが
     髪だけで乗る。**そのままでは軽くならない — 間引き(decimate)が前提**。
   - 寸法はモデルと別スケール（幅1.65m・z 3.45〜5.36）。頭に合わせる処理は既存script有り。

3. **目 … できる / 口 … ジオメトリが無い**
   - 目: `eyes` + `lashes` マテリアルを別オブジェクトへ切り出せた
     （**802頂点 / 1,344三角形**、z 1.673〜1.707）。移動・回転とも反映を確認。
     ⚠️ オブジェクト原点が目の重心に無いので、回すと大きく振れる（原点を置き直すこと）。
   - 口: **専用マテリアルが無い**。`face`(7,228面) の **Face.jpg (4000x4000)** に
     描かれている（顔のUVは 3686x3837px を占める）。位置や角度を変えるには
     **テクスチャを描き替える**か、**口だけ別の板(デカール)を貼る**しかない。
     顎のボーンも無い（頭まわりは Neck と Head の2本だけ）。

**結論**
- 描画コストはデフォルメ・ポリゴンの方が有利（体だけなら5〜20倍軽い）。ただし
  **髪をそのまま使うと帳消しになる**ので間引きが必須。
- 体型と目は既存の素材のまま動かせる。**口だけは作り足しが要る**（現状のボクセル版は
  `faceMarks` で目と口を板として描いており、その仕組みはそのまま流用できる）。

### 追記: ポリゴン検証ページ（`/poly.html`）を作った

**素材の書き出し（Blender `--background --factory-startup`）**
- `public/poly/player.glb` 6.03MB … 元の FBX を読み、**目とまつげを別オブジェクトへ切り出し**、
  原点を目の重心へ置いてから書き出し。⚠️ 原点を置き直さないと回した時に大きく振れる。
  テクスチャは 4000x4000 → 1024 へ縮小（そのままだと GLB が巨大になる）。
  ボーン52本（Hips / Spine / LeftUpLeg / RightUpLeg / LeftLeg / RightLeg / Head すべて有り）。
  頂点 52,266 / 三角形 89,368（うち目が 1,344）。
- `public/poly/hair_low.glb` 1.02MB / `hair.glb` 3.98MB … Man Hair Collection から8種を抜き、
  頭幅 0.19m に合わせて縮め、原点を中心へ。生と間引き25%の2本。
  ⚠️ 8種の三角形は **生 6,016〜50,368 / 間引き 1,504〜12,592** とばらつきが大きい。
  （279種全体の中央値は 4,116 なので、この8種は重い側に寄っている）
- ⚠️ **glTF のメッシュ名はオブジェクト名にならない**（`Mesh`, `Mesh.001` になる）。
  Babylon は**ノード名**でメッシュを作るので、そちらに `bodyAll` / `eyes` /
  `Man_Hair_001` が入っていることを確認して名前で引いている。

**ページでできること**（`src/poly.ts`。ゲーム本体のコードは通していない）
- 体型: 脚の長さ / 胴の長さ / 太さ / 頭の大きさ をボーンのスケールで変える。
  ⚠️ 入れ子のボーンはスケールが掛け算になるので、**背骨は Spine 1段だけ**に掛けている。
  素のスケールを覚えておき毎回そこから掛け直す（掛け算で溜めない）。
- 髪: 8種＋なし。生／間引きを切り替えてコスト差を見る。大きさ・上下・前後で頭に合わせる。
  ⚠️ 髪も口も**頭ボーンへ付ける**。付けないと体型を変えた時に顔から離れる。
- 目: 左右・上下・前後・傾き（縦/横）。元モデルのジオメトリをそのまま動かす。
- 口: **元モデルに口のジオメトリが無い**ので、板を貼る案を実演（上下・前後・大きさ・傾き）。
  現行のボクセル版 `faceMarks` と同じ考え方。
- 情報欄に **実測身長 / メッシュ数 / 頂点 / 三角形** を出し、現行ボクセル(85,728頂点 /
  40,896三角形)と並べて比べられるようにした。

**⚠️ 未確認**
- ブラウザでの実際の描画（`bone.setScale` がスキニングに効くか、GLB が読めるか）は未確認。
  Node には XMLHttpRequest が無く Babylon の内部がそれを直接使うため、ヘッドレスでは
  GLB を読み込めなかった。**`/poly.html` を開いて確認すること**。
- `@babylonjs/loaders@8.56.2` を新規に追加した（GLB 読み込みのため）。

**⚠️ 修正: スライダーを動かしても何も変わらなかった件**
- 原因: **glTF ローダーはボーンを TransformNode にリンクする**
  （`@babylonjs/loaders/glTF/2.0/glTFLoader.js:1228` の `babylonBone.linkTransformNode(...)`）。
  リンクがある間、ボーンのローカル行列は毎フレームそのノードから作り直されるので、
  `bone.setScale()` に書いた値は**次のフレームで消える**。
- 直し: リンクされた TransformNode があればそちら（`bone.getTransformNode().scaling`）を
  触る。無いときだけ `bone.setScale()` を使う。素のスケールはボーン名をキーに覚える。
- 情報欄に「ボーン N 本（ノード連動 M 本）」を出すようにした。M が 0 でないことが
  この経路を通っている証拠になる。読み込み直後に `applyBody()` を一度当てるようにもした。

**⚠️ 「スライダーを動かしても何も変わらない」の原因は3つあった（すべて実測で特定）**

1. **ボーンに書いたスケールが毎フレーム消えていた**
   glTF ローダーはボーンを TransformNode にリンクする
   （`@babylonjs/loaders/glTF/2.0/glTFLoader.js:1228`）。リンクがある間、ボーンの
   ローカル行列は毎フレームそのノードから作り直される。実測（`probe-poly3`、
   足のボーンのワールド位置で確認）:
   | 触った先 | 足のボーンの高さ |
   |---|---|
   | 素 | 0.1114 |
   | **TransformNode.scaling を 2 倍** | **-0.7335（動く○）** |
   | `bone.setScale()` を 2 倍 | 0.1114（動かない×） |
   | ボーンのローカル行列を直接書き換え | 0.1114（動かない×） |
   → リンクがあるときは **`bone.getTransformNode().scaling`** を触る。

2. **メッシュ名が一致しない**
   glTF ローダーはノードのメッシュを**マテリアルごとに分割**し、
   `"<ノード名>_primitive<N>"` という名前にする。実測で `bodyAll` は
   `bodyAll_primitive0`〜`primitive8` の9枚、`eyes` は `eyes_primitive0/1` の2枚だった。
   `m.name === "eyes"` では引けず、目のスライダーが何も掴んでいなかった。
   → 前方一致（`startsWith(name + "_primitive")`）で拾う。目は2枚まとめて動かす。
   ※ 髪はメッシュ1つにマテリアル1つなので分割されず、`Man_Hair_001` のままだった。

3. **身長の表示が出鱈目だった**
   `refreshBoundingInfo({applySkeleton:false})` だと箱が潰れる（実測 -0.005〜0.004m）。
   `applySkeleton:true` なら正しい（-0.003〜1.816m）が、**ボーンを動かした後は更新されない**
   （足のボーンが 0.85m 動いても箱は 1mm も変わらなかった）。
   → **素の姿勢で一度だけ** `applySkeleton:true` で測って「頭のボーン→頭頂」と
   「足のボーン→足裏」の差を取り、以後は `bone.getAbsolutePosition(mesh)` から出す。
   実測で 素 181.9cm / 脚1.20倍 208.8cm / 胴1.20倍 191.5cm / 脚0.85倍 163.9cm /
   頭1.25倍 187.2cm と正しく動くようになった。

- ヘッドレスで GLB を読む方法: **data URL で渡す**（`"data:;base64," + b64`）。
  Node に XMLHttpRequest が無く Babylon の内部がそれを直接使うため、ファイルパスでは読めない。
  `scene.render()` にはカメラが要る（NullEngine でも「No camera defined」で落ちる）。

**⚠️ 本当の原因: `skeleton.prepare(true)` を呼んでいなかった（v5 で修正）**
- 症状: 版の目印も身長の数字も更新されるのに、**見た目が1ミリも変わらない**。
- ⚠️ ボーンの位置や身長は「ボーンのローカル/絶対行列」から出るので更新される。
  一方**シェーダへ送る行列（`getTransformMatrices`）は別**で、そちらが古いままだった。
  数字が動くことは、描画が動く証拠にならない。
- `skeleton.js` の `prepare()`:
  1. 冒頭で「今フレーム既に呼ばれていたら何もしない」（`_currentRenderId`）
  2. リンクノード → ボーンへ写す
  3. **`if (!this._isDirty) return;`** ← ここで抜けると行列を作り直さない
- 実測（`probe-poly4`、`getTransformMatrices` の最大差）:
  | やったこと | 行列の変化 |
  |---|---|
  | ノードの scaling へ書くだけ | **0.0000（描画は変わらない）** |
  | + `bone.markAsDirty()` | 0.0000 |
  | **+ `skeleton.prepare(true)`** | **46.98（変わる○）** |
- 直し: `applyBody()` の最後と描画ループの毎フレームで `skel.prepare(true)` を呼ぶ。
  実測（`probe-poly5`、ページと同じ手順）: 脚1.2倍 17.3 / 胴1.2倍 8.7 / 太さ1.3倍 47.1 /
  頭1.25倍 15.8 / 脚0.85倍 10.9、**素へ戻すと 0.000**（掛け算で溜まっていない）。

**この件で学んだこと**
- ⚠️ Babylon で「ボーンを動かしたのに描画が変わらない」ときは、**`getTransformMatrices`
  を直接比べる**こと。ボーンの位置や境界箱を見ても、シェーダが見ている行列とは別物。
- 検証の足場: GLB は **data URL** で渡せば Node でも読める（`"data:;base64," + b64`）。
  `scene.render()` には NullEngine でもカメラが要る。

**頂点まで追った確認（probe-poly6）**
- シェーダが使うのと同じボーン行列で、CPU 側でスキニングを手計算して頂点を比べた。
- 脚のボーンを 1.20 倍 → **頂点の 23% が動き、最大 22.8cm**。0.85 倍 → 23% / 最大 17.1cm。
  動いた割合が脚の頂点の割合と一致するので、狙ったボーンだけが効いている。
- ⚠️ この手計算は**相対的な移動量しか信用できない**（絶対位置は 12m など出鱈目になる。
  重みの正規化とボーン行列の並び方を厳密に合わせていないため）。絵が変わるかどうかの
  判断には使えるが、身長の値をここから取ってはいけない。

**⚠️⚠️ 本当の本当の原因: GLB のアニメーションが毎フレーム上書きしていた（v6 で修正）**
- `player.glb` には **14 個のアニメーション**が入っており、1つ目
  `Armature|Take 001|BaseLayer` が **52 本すべてのボーンの translation / rotation /
  scale**（156 チャンネル）を持っている。
- glTF ローダーは**既定で最初のアニメーションを自動再生する**
  （`GLTFFileLoader.AnimationStartMode = FIRST`）。つまりこちらが書いたスケールは
  毎フレーム元の値へ戻されていた。ユーザーの見立て（「上書きしている」）が正しかった。
- 実測（`probe-poly7`、脚のノードへ 1.5 を書いてから描画ループを回す）:
  | | ノードの縦 | 行列の差 |
  |---|---|---|
  | 書いた直後 | 1.500 | 19.574 |
  | **止めないまま 10 フレーム後** | **1.000** | **0.000（戻される）** |
  | 止めてから 10 フレーム後 | 1.500 | 19.574（保たれる） |
- 直し: 読み込み直後に `animationGroups` を `stop()` + `dispose()` し、
  `scene.stopAllAnimations()` も呼ぶ。髪の GLB も同様。情報欄に「止めたアニメ N 個」を出す。
- ⚠️ **なぜ見落としたか**: それまでの probe は書いた直後に 1〜3 フレームしか回さずに
  測っていた。アニメーションが上書きするのは次のフレーム以降なので**再現しなかった**。
  「ブラウザで動かない」を再現するには、**描画ループと同じだけフレームを回すこと**。

### 追記: 髪が出ない・目の軸が合わない・口の形（v7〜v8）

**原因は2つとも同じ: 頭ボーンの行列に 1/39.4 のスケールが入っている**
- 実測: `Head` ノードのワールド行列を分解すると **scale 0.0254**（＝インチ→メートル。
  FBX 由来のリグの単位がそのまま残っている）。
- そのため頭ボーンへ付けたものは **39.4 分の1に縮み**、位置の指定も 39.4 分の1になる。
  実測: 幅 0.190m の髪が **0.0048m**、目の +0.05m の移動が **+0.001m**。
- 直し: 頭ボーンへ付けたノードの子に「1/boneScale 倍」の補正ノードを1枚挟む。
  以降はメートルで書ける。実測で髪は倍率 1.00 のまま 0.190m に収まった。
- ⚠️ `matrix.decompose(sc)` は **3つとも渡すこと**。scale だけ渡すと y,z が埋まらず
  (0.0254, 0, 0) になる。平均を取ると 1/3 の 0.00847 になり、補正が3倍ずれた。

**目はそもそも1ミリも動いていなかった**
- ⚠️ **スキン中のメッシュは自分の位置を変えても動かない**。頂点は骨の行列だけで決まる。
  実測: `position.x` に +0.05 を入れてもワールドでの移動は **0.000m**。
- 直し: `mesh.applySkeleton(skel)` で今の見た目を頂点へ焼き込み、`skeleton = null` にして
  ふつうのメッシュにする。親は付けない（焼き込んだ頂点に骨のスケールが掛かると y が
  -11m まで飛ぶ）。毎フレーム、頭ボーンの位置の**差分**で追従させる。
- ⚠️ 焼き込んだ頂点がどの空間に入るかは**推測しない**。一度測って差分（`eyeBase`）で
  合わせる。素直に頭ボーンの位置を足すと二重になり y が 1.69→3.91、`setPivotPoint` を
  使うと 0.53m ずれた（どちらも実測で外れた）。
- 回転は目の中心まわりにする（位置へ `c - R·c` を足す）。`setPivotPoint` は使わない。
- 実測: 素の位置 y **1.690m（狙いどおり）**、左右/上下/前後とも **+2cm → 2.0cm** で一致。

**口: 形を変えられるようにした**
- 元モデルに口のジオメトリは無い（顔テクスチャに描かれている）ので、板に描いて貼る。
  横幅・縦幅・上下・前後・傾き（縦/横）に加えて、**口角**（笑い〜への字）と**開き**を
  `DynamicTexture` へ描き直す方式で用意した。

### 追記: のっぺらぼう化（v9〜v10）

**やったこと**（`tools_flatten_face.py`。Blender `--background --factory-startup` で実行）
1. 目・まつげの面を消す
2. ⚠️ **消した後に**頂点集合を作る。面を消すと頂点番号がずれるので、先に作ると
   まったく別の頂点を掴む（実測: 顔の凹凸が 2.79 → 14.40mm と「増えた」ように見えた）
3. 目と口の穴を塞ぐ（⚠️ **平滑化より前**。後だと縁が残る）
4. 平滑化（Smooth モディファイア。細かい凹凸に効く）
5. 出っ張りの押し込み（鼻・耳。楕円体の外へ出た頂点を内側へ寄せる）

**実測**
- 顔の凹凸 2.92mm → **0.58mm（80%減）** / 腕の凹凸 2.26mm → **0.55mm（76%減）**
- 穴の縁 2819本 → 2677本（94ループのうち **9ループを塞いだ**）。
  **顔の前面に残った小さい穴は 0 個**（残り 2677 本は首・頭皮など大きい境目で、顔の穴ではない）
- 頭の幅 0.1840m / 高さ 0.2309m（平滑化ぶんの縮みのみ）
- 三角形 88,148（平滑化では減らない）／間引き30%版 **26,443**

**⚠️ 途中で外した手**
- `bmesh.ops.holes_fill(..., sides=0)` は**1枚も塞がない**。上限として大きい値（200）を渡す。
- 三角形化は**塞いでできた面だけ**に掛ける。全面に掛けると元の四角形まで割れて面が荒れる。
- 頭の縁を全部塞いではいけない。首・頭皮・シャツの境目まで塞がる。
  縁をループごとに分け、**7cm 未満・顔の前面**のものだけを対象にする。
- 平滑化は細かい凹凸には効くが、**鼻や耳のような大きい出っ張りには効かない**
  （実測: 鼻 38.0→30.7mm、耳 68.2→60.0mm と2割程度しか減らない）。押し込みが要る。
- 押し込みの楕円体は**軸ごとに分位点を取ってはいけない**。3軸ぶん足し合わさって
  小さくなり、頭ごと縮む（実測: 頭の半径 97.5mm → 65.0mm）。まず最大で囲ってから
  「頂点の何割が内側に入るか」で一度だけ縮める。

**ページ（`/poly.html` v10）**
- 「素体」で 3 つを切り替えて見比べられる:
  のっぺらぼう 88,148三角形 / のっぺらぼう＋間引き30% 26,443三角形 / 元モデル 89,368三角形
- ⚠️ のっぺらぼう版には目のジオメトリが無いので、目のスライダーは効かない（口は板なので効く）。

### 追記: 穴を「段差なく」塞ぐ・元モデルの髪を消す（v11）

**「塞ぐ」＝周囲の顔と段差なく繋ぐ**
- 平らな蓋をするだけでは縁に折れ目が残る。蓋を細分してから頭ごと平滑化して均す。
- 段差の測り方: 穴のまわり半径 3cm の辺について、**辺を挟む2面のなす角**を集めて中央値と95%点。
  実測: 塞いだ直後 中央 8.3°／95% 73.7° → 仕上げ後 **中央 5.4°／95% 45.0°**。
- ⚠️ **自作の Laplacian 段差取りは逆効果だった**。40回で 51.2°→62.0° と悪化し、
  80回では 90°（面が折れて破綻）。**平滑化モディファイアだけが最良**。
- ⚠️ 平滑化を強めると中央値は下がる（20回 5.4° → 80回 3.4°）が、95%点は上がる
  （45.0° → 55.2°）。強ければ良いわけではない。採用は 20 回。
- ⚠️ **穴ふさぎで頂点が増える（細分）ので、頂点集合を作り直すこと**。作り直さないと
  増えた頂点が平滑化の対象から外れ、段差取りをしたのに折れ目が悪化した（51.2°→63.8°）。
  面を消した時（頂点番号がずれる）と同じ罠。この件で2回踏んだ。

**元モデルの髪を消す**
- `hair` と `scalp hair` のマテリアルの面を削除。残すと差し替えた髪と二重に生える。
- ⚠️ 消すと**頭頂に大きな穴が空く**。穴の選別を「7cm 未満・顔の前面」から
  「30cm 未満（頭の縁は全部）」へ緩めた。首やシャツの縁は `head_v`（Head ボーンの重み
  0.5 超）に入らないので巻き込まれない。実測: **頭に残った穴の縁 0 本**。
- 三角形 78,613（髪なし）／間引き30%版 23,582。マテリアルは shirt/pants/Socks/
  Torso/Arms Legs/face/shoes の7つだけになった。

**髪の標準位置**
- ⚠️ 頭ボーンは後頭部寄りにあるので、位置 0 だと髪が下がって前へずれる。
  標準を **上下 +10.0cm / 前後 +3.0cm** にした（スライダーの範囲も合わせて広げた）。

### 追記: 髪の基準点を統一・顔は「お面」で被せる（v12〜v13）

**髪の基準点（ユーザー指摘: 下限位置基準だと髪型ごとに揃わない）**
- 実測: 髪コレクションは**カタログ状に並べてあり、共通の頭を共有していない**
  （中心x が -29.9〜+7.5、中心y が 18.7〜40.7 にばらける）。そのままでは共通座標が使えない。
- ⚠️ bbox の下端や中心を基準にしてはいけない。長い髪ほど下へ引っ張られて基準がずれる。
- 採用した統一基準（`tools_export_hair.py`）:
  **「上から20%の帯の中心(x,y)」と「上端(z)」を原点へ**。倍率も帯の幅の中央値から出した
  **全髪型で共通の1つ**だけを使う（大きさの差は髪型本来の差なので残す）。
  実測: 8種すべて **上端 +0.0000m**、幅は 0.171〜0.250m。
- ページ側は倍率を合わせ直さない（合わせ直すと大きさの差が消える）。上下の既定は
  読み込み時に実測した「頭ボーン→頭頂」(`crownGap`)を入れる。

**顔（ユーザー指摘: のっぺらぼう化ができていない）**
- ⚠️ 元モデルの顔は写真テクスチャ＋細かい凹凸で、**均しても「のっぺらぼう」にはならない**。
  平滑化・穴ふさぎ・出っ張り押し込みを重ねても、質感が写実のままなので狙いに届かない。
- 方針を変えて、**単純な曲面を顔へ被せる（お面）**。球の一部を切り出し、材質は髪と同じ
  素の StandardMaterial（テクスチャなし・光沢なし）にして質感を揃える。
  幅・高さ・奥行・上下・前後・横の回り込み・縦の切り取りを調整できる。
