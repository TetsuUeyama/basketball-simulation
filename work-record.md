# work-record

## 2026-09-03

- `C:\Users\user\developsecond\basketball-simulation` にプロジェクトを新規作成。
- 構成: Node + TypeScript（ヘッドレス）。Node の型ストリッピングで `.ts` を直接実行する。
- 追加ファイル: `package.json` / `tsconfig.json` / `.gitignore` / `README.md` / `workPlan.md` / `src/index.ts` / `src/rng.ts`
- リモート `https://github.com/TetsuUeyama/basketball-simulation.git` を origin に設定。
- 確認済み: `npm install` / `npm run typecheck` / `npm start` / `npm run build` が通ること。
- シミュレーション本体は未着手。次は workPlan の「未確定」を決めるところから。

---

## 2026-09-03（続き） basketball-sim を踏襲してゲーム化を開始

ユーザーの要望は4つ: ①クオーター毎のポーカーによる強化 ②1on1 の勝敗を能力で決める
③判定時の局所物理 ④スマホでの高速化とローディング表示。

### 決めたこと（ユーザー確認済み）

- **本体は basketball-simulation**（この新リポジトリ）。basketball-sim から移植して出発する。
- **描画は Babylon.js 3D を踏襲**（既存のボクセル選手・カメラ・HUD をそのまま使う）。
- **ユーザー操作はポーカーのみ**。試合そのものは従来どおり AI が進める（観戦）。
- ポーカーの核心ルール: **手札交換を打ち切って役を確定させるタイミングをホームチームが決める**。
  早く確定すれば効果が長く効き、引っ張れば強い役を狙えるが恩恵は短い。

### 1. 移植（commit 39a1ca1）

- basketball-sim の commit `76c9eca` から `src/`(約3万行) / `vendor/objcts` / `docs` /
  `scripts` / `headless_sim/*.ts` / `index.html` / `vite.config.ts` をコピー。
- ヘッドレス雛形の `package.json`・`tsconfig.json` を Vite + Babylon 構成へ差し替え
  （NodeNext + erasableSyntaxOnly → bundler + DOM lib）。`src/rng.ts` だけ雛形から残置。
- 確認: `npm install` / `npm run build` ✓。
- **実測: バンドル 11.98MB（gzip 2.71MB, 単一チャンク）**。要望4の出発点の数字。

### 2. ポーカー強化システム（機能1）

新規 `src/poker/`。Babylon にも DOM にも依存しない純粋ロジックにして、ヘッドレスで検証できる形にした。

| ファイル | 役割 |
| --- | --- |
| `cards.ts` | 札とデッキ。乱数は `src/rng.ts` のシード付き（再現性のため `Math.random` を使わない） |
| `hands.ts` | 役判定（`evalHand`）と役同士の比較 |
| `effects.ts` | **バランス調整ノブの集約**。カード→能力の対応、捨て札の量、役ごとのチーム強化 |
| `state.ts` | `PokerMatch` — 配札/交換/確定/巻き戻し。乗せた強化は全て記録して `revert()` できる |
| `ai.ts` | CPU の捨て札選択・強化先の選択・（ホームなら）確定タイミングの判断 |

- **カード→強化**: スートが系統（♥=オフェンス / ♦=ディフェンス / ♠=シュート / ♣=フィジカル）、
  ランクが量（A=主能力+5 / 絵札=系統内の特定能力+3 / 数札=主能力+2）。
  **同じカードでも選手によって効きが変わる**（その能力が高い選手ほど大きい、0.6〜1.4倍）。
- **捨て札は即座に個人強化**（確定を待たない）。1回の交換で捨てられるのは3枚まで（`MAX_DISCARDS`）。
- **役の確定でチーム全13人に強化**。tier(0..9) → 系統+0..6 / 全能力+0..4（`HAND_BUFF`）。
- 強化は `PlayerDef.attr` に直接足し、差分を `applied` に記録。試合リセット/BACK で `revert()`
  して素の能力値へ戻す（`Player.attr` は def へのライブ参照なので、これが唯一安全な戻し方）。

### 3. 試合本体への配線

- `Game` に `poker` / `pokerGate` / `onPokerRound` と `awaitPoker(round, next)` /
  `resumeFromPoker()` を追加。`core/gameflow.ts` のクォーター切り替え（引き上げ→ハドル→次Q）の
  間に `awaitPoker` を挟んだ。UI が居なければ素通りするので、ヘッドレスも従来どおり動く。
- `src/ui/ui-poker.ts` 新規: 手札の表示・捨てる札の選択・**捨て札ごとに強化先の選手を選ぶ**
  プルダウン（効果をその場で表示）・確定/持ち越しの選択・確定時の相手手札の公開。
- 試合前バーに「ポーカー: 自分で打つ ⇄ CPU同士」トグル（`POKER_OPTS.userTeam`）。
  CPU同士では画面を出さず自動で進む＝**CPU対CPUとCPU対ユーザーの両方が同じ処理で回る**。
- ポーカー画面が出ている間は `ui.simPaused` でシムだけ止める（描画とカメラは動かす）。

### 4. 検証

- `headless_sim/probe-poker.ts`: **52C5 の全 2,598,960 通りを列挙**して役の頻度を理論値と照合
  → 10種すべて完全一致（ロイヤル4 / SF36 / フォーカード624 / フルハウス3744 / フラッシュ5108 /
  ストレート10200 / スリーカード54912 / ツーペア123552 / ワンペア1098240 / ハイカード1302540）。
  境界（ホイールA2345 / K-A-2-3-4は非ストレート / ロイヤル）も期待どおり。
- CPU同士4000試合ぶんのポーカー: 確定ラウンドは R1:2.0% R2:18.8% R3:35.2% R4:44.0%、
  確定役はワンペア29.5% ツーペア29.0% スリーカード20.9%。1人あたりの平均上昇 0.84点/能力。
### 5. バランス実測（`headless_sim/probe-poker-balance.ts`、各200試合）

同じクラブ対戦カード（`n%CLUBS` vs `(n*7+3)%CLUBS`）を全条件で使って比較する。
team0 と team1 で引くクラブが違うため、**基準（ポーカー無し）が 50% ではなく 40%** である点に注意。

| MODE | 内容 | team0 勝率 | 平均スコア | 格下の勝ち |
| --- | --- | --- | --- | --- |
| off | ポーカー無し（基準） | 40.0% | 11.3 - 12.5 | 21.8% |
| both | 両チームがポーカー（実プレイの既定） | 42.0% | 11.9 - 12.4 | 15.8% |
| one | team0 だけポーカー | 50.0% | 12.1 - 11.6 | 27.8% |
| max | team0 が毎試合ロイヤル（上振れの上限） | **75.0%** | 15.9 - 11.0 | 29.3% |

- 実プレイの形（both）では基準+2.0pt = **ほぼ中立**。格下の勝ちはむしろ 21.8%→15.8% に減った。
- 片側だけなら +10.0pt。**補助的な範囲**に収まっている。
- ⚠️ **max が 75% で workPlan の目標「60%未満」を超えた**。原因は `HAND_BUFF` の `all`
  （全25能力）で、`group`（系統4〜5能力）の5倍効いていたこと。ロイヤルで13人×25能力×+4 =
  1689点が乗っていた。
- 対策: `all` を全廃し、**系統への集中**に振り直した（ワンペア group+2 … ロイヤル group+12）。
  再実測の結果は下記。

| MODE（調整後） | team0 勝率 | 平均スコア | 格下の勝ち | team0 の強化量 |
| --- | --- | --- | --- | --- |
| both | 46.0% | 11.9 - 12.2 | 19.5% | 224点 |
| one | 50.0% | 12.0 - 11.7 | 15.0% | 224点 |
| max | **52.5%** | 13.2 - 12.7 | 22.6% | 778点 |

- **max が 75.0% → 52.5% に収まり、目標「60%未満」を満たした**。
- 格下の勝ちは基準 21.8% に対し both 19.5% / one 15.0% / max 22.6% =
  **どの条件でも基準を上回らない**。ポーカーは番狂わせを増やしていない。
- ⚠️ 一方で **max(ロイヤル毎試合, 778点) が one(平均的な役, 224点) とほぼ同じ勝率**になった。
  系統に集中させると効果が飽和するため。N=200 の標準誤差は約3.5ptなので、この2条件は
  現状の試行数では区別できない。強い役に「効いた感」を持たせたいなら上位役だけ引き上げる
  余地がある（ただし max が60%へ近づく）。

### 6. ユーザー指示による変更（同日）

- **ポーカーの1手目を「両チーム決定後・選手紹介とティップオフの前」へ移動**。
  `ui.onPrepare`（コートに26人を並べる）と `ui.onStart`（選手紹介ツアー→ティップオフ）に分割し、
  TIP OFF ボタンは `onPrepare() → beginPoker(() => onStart())` の順で呼ぶ。
- **CPU同士 / あなた対CPU の選択を一番最初の画面へ**。タイトルの「クラブチーム対戦 /
  ランダムクラブ / ランダム対戦」を選ぶ**前**に決める形にし、試合前バーのトグルは廃止した。
- **ポーカー画面を作り直し**: 説明文を全廃し、**現在の役・コートに見立てた盤・手札・ボタン**だけに。
  盤の上に先発5人の顔アイコンを立ち位置（PG=トップ / SG・SF=ウイング / PF・C=ゴール下）に並べ、
  **手札をドラッグして選手の上に置くと、その選手が強化される**（タップでも: 札を選ぶ→選手を叩く）。
  置いた札には効果（例 `♥7 OFF+3`）が付き、タップで手札へ戻せる。置ける枚数は `MAX_DISCARDS`。

- **控え8人にも札を置けるようにした**。盤を 430px → 330px に縮め、コート横に控え8人を
  2列×4段で並べる（画面幅660px未満では盤の下に4列×2段へ回り込む）。先発と控えは同じ
  `playerCell` で作り、13人の誰でもドロップ先になる。CPU の宛先選択は先発5人のまま
  （出場時間が長いぶん有利なため、方針として変えていない）。
### 7. ユーザー指示による追い込み（2026-09-04）

- **ドラッグ中の影を 52×74 → 34×48 に縮小**。
- **追従性の改善**: 影の移動を `left/top`（再レイアウトが起きる）から
  `transform: translate3d(...)` + `will-change` へ変更し、ドロップ先の矩形は
  **ドラッグ開始時に一度だけ**測るようにした（従来は pointermove ごとに13個ぶん
  `getBoundingClientRect` を呼んでいた＝毎フレーム強制レイアウト）。
  併せて `setPointerCapture` で指が要素外へ出ても move が途切れないようにした。
  ⚠️ 実際に滑らかになったかは実機でしか確認できない。
- **捨て札の上限を 3 → 5 枚**（`MAX_DISCARDS`）。手札を全部捨てられる。
  CPU は最良の1枚を残す方針なので最大4枚（役を完全に捨てる打ち方はしない）。
- **札を置いてもアイコンがずれない**ように、置いた札のチップを
  `position: absolute` でセルの外へ浮かせた（セルの箱の大きさが変わらない）。
  横並び＋折り返しにして下方向へ伸びすぎないようにし、盤の `overflow: hidden` を外し、
  PG の立ち位置を y79% → 74% に上げてチップが手札に被らないようにした。

- **強化は1人1枚まで**にした（横並びでも見にくかったため、ユーザー指摘）。同じ選手へ2枚目を
  置くと前の札は手札へ戻る。表示は**名前の下に1行**へ戻し、その1行分の高さ(13px)は
  **札が無くても最初から確保**する。置いてもアイコンはずれず、控えの行間も最初から空く。
  （※これに伴い、いったん入れた「アイコンの右横／右寄りは左横へ反転」は撤去）
- 旧: - **置いた札の表示をアイコンの真横へ**（下に積むと他の選手アイコンが隠れるため、ユーザー指摘）。
  文字を 10px → 8.5px に縮小し、縦に積む。コート右側の先発とベンチ右列は**左横へ反転**させ、
  盤やベンチ枠からはみ出しにくくした。

#### 捨て札上限 3→5 の影響（各200試合）

| MODE | 上限3 | 上限5 |
| --- | --- | --- |
| one（片側のみ） | 50.0% | 49.0% |
| both（実プレイの形） | 46.0% | 47.5% |
| team0 の強化量 | 224点 | 229点 |

CPU の打ち方では**ほぼ変化なし**。上限3が CPU の選択を縛る場面が少なかったため
（CPU は役の draw を残す打ち方をするので、そもそも3枚以上は捨てない）。
⚠️ ただしこれは CPU の打ち方の測定であって、**人間が毎回5枚捨てる打ち方は別**。
それを測るため `MODE=dump`（毎ラウンド全捨て・役は最終ラウンドで自動確定）を追加した。

実測（200試合）: **dump 44.5%**（基準40.0% / 通常の打ち方 one 49.0%）。強化量131点。
確定役は ハイカード101 / ワンペア84 で、役はほぼ作れない。
→ **全捨ては最適解ではない**（役を狙う打ち方より 4.5pt 弱い）。捨てる枚数の上限を5にしても、
　「個人強化を取るか役を取るか」のトレードオフは成立している。

### 8. 妨害（相手選手の能力を下げる）を追加（2026-09-04）

- 画面の**一番上に相手の段**を新設: **伏せた手札5枚**（役が確定すると表になる）と、
  **相手の先発5人**の顔アイコン。相手選手に札を置くと**妨害**になる。
- `hinderEffect()`: カード→能力の対応は強化と同じで、符号が負・効率は `HINDER_SCALE`(0.8)。
  強化と同じく「その能力が高い相手ほど大きく下がる」＝主力を潰しにいく手になる。
- 捨て札の置き先を `number`（自軍のロスター番号）から **`DiscardTarget {team, idx}`** へ変更。
  自軍なら強化・相手なら妨害を適用し、`applied` に負の差分として記録して `revert()` で戻す。
- CPU も妨害を使う: `pickTarget` が「自軍の伸び」と「相手の下げ幅」を同じ物差しで比べ、
  大きい方を選ぶ（同点なら自軍）。1人1枚の制約も CPU 側で守る。
- ⚠️ 計測プローブの修正: 妨害が入ったことで、MODE=one/dump（team0 だけの効果を測る条件）で
  **team1 に打たせると team0 の能力値へ直接効いてしまう**ため、これらの条件では team1 は
  打たせないようにした。この修正前の one/dump の数値は妨害を含まないので比較対象は both のみ。

#### 妨害ありのバランス実測（各200試合、基準=ポーカー無し 40.0%）

| MODE | 妨害なし | 妨害あり |
| --- | --- | --- |
| both（実プレイの形） | 47.5% | **45.5%** |
| one（片側のみ） | 49.0% | **53.5%** |

- both は基準+5.5pt でほぼ中立のまま。格下の勝ちも 20.3%（基準21.8%）で増えていない。
- one は +9.0pt → +13.5pt に増えた。妨害は「相手の主力の長所を削る」ぶん効率が良い。
  それでも目標の60%未満には収まっている。

### 9. 画面の並びを組み替え（ユーザー指示）

- 盤を**横長フルコート**（28×15、左右にリム）に描き替え、**左半分に相手の先発5人**、
  **右半分に自分の先発5人**を配置。相手側に置けば妨害、自分側に置けば強化。
- **控え8人は手札の下**へ（4列×2段）。コート横のベンチは廃止。
- **現在の役は交換ボタンの横**へ移動（確定後は相手の役も並べて出す）。
- 相手の伏せ手札は最上段のまま。
- **相手の控え8人**も相手の伏せ手札の下に配置（自分側と対称、4列×2段）。ここにも札を置いて
  妨害できる。これで札の置き先は 自分13人 + 相手13人 = 26人。
  CPU の選択は先発10人（自軍5・相手5）のままにしている（控えは出場時間が短く効率が悪いため）。

### 10. 手番を明示（相手が先 → 自分 → ホームが確定判断）

- 実装上は元から「CPU の交換 → ユーザーの交換 → ホームの確定判断」の順だったが、
  **相手が何をしたのかが画面に出ていなかった**ため、先手の意味が無かった。
- `AppliedDelta` に `by`（打ったチーム）と `round`（何ラウンド目か）を追加し、
  **そのラウンドで既に適用された増減を選手アイコンの下にチップで表示**するようにした。
  枠線が打った側のチームカラー。これで「相手が誰を強化し、こちらの誰を妨害したか」を
  見てから自分の手を決められる。
- チップ行は絶対配置にしてセルの幅にも影響させない（相手の手が入っても配置がずれない）。
- 将来「監督（ヘッドコーチ）の能力で先攻後攻を決める」を workPlan に記載。
  ヘッドコーチという概念自体が未実装なので、能力値・データの持ち方から設計が要る。
- CPU の捨て札判断は**ランダムではなく**、ドローポーカーの定石（ペア以上/4枚ドローを残す）＋
  置き先は「自軍の伸び」と「相手の下げ幅」の大きい方。本格的な AI（点差・残りクォーター・
  相手の役を読む）は将来の課題として workPlan に記載した。

### 11. 相手の手番を見せる（思考 → 札を置く → 補充）

指摘: 画面を開いた時点で既に相手の交換が済んでいて、何が起きたのか分からない。

- `cpuPlan()` を新設して CPU の判断を「決めるだけ」に分離（`cpuExchange` はそれを即適用する
  薄いラッパにして、ヘッドレス計測は従来どおり）。
- UI に手番の段階 `pokerStage = "cpu"` を追加。この間はボタンを出さず、選手アイコンへの
  クリックも張らない（座標だけは登録して、札を飛ばす先に使う）。
- 見せ方: **思考時間**（伏せ札が軽く上下する）→ **捨てる札が1枚ずつ、手札の位置から
  置き先の選手へ飛ぶ**（受け取った選手が一瞬光る）→ 交換を適用 → **補充された札が
  手札に入る**（上から差し込まれる）。補充が終わってから自分の手番へ渡す。
- 時間は `POKER_OPTS` で設定: `thinkMs`(900) / `stepMs`(420) / `dealMs`(260)。
- `AppliedDelta` に `card` を追加し、何の札で動いたかを記録できるようにした。
- ⚠️ 捨てた札は**表で**飛ばす（何を捨てたかは見せる）。手札の中身は伏せたまま。

### 12. 選手データにポジション(C〜PG)と体重を取り込み（2026-09-04）

出典: `OneDrive/デスクトップ/WE2010 0903.xlsx` Sheet1（ヘッダ2行目・データ3行目〜、4015人）。
抽出スクリプトは `scripts/update-identity.cjs`（node+xlsx。python は Store スタブで動かない）。

- **列の対応**: D=選手名 / **J=C K=PF L=SF M=SG N=PG（守れるポジションのフラグ）** /
  O=身長 / **P=体重** / R=利き足。⚠️ 旧ブックとは列配置が違う（旧は CI..DE が能力値）。
- ⚠️ **行の並びが identity.ts と一致しない**（並び順での一致は 316/4015）。**名前で照合**した。
  同名が42組あるので、まず「名前＋身長」で当て、残りを名前順で当てる → **4015人全員一致・
  取り違えゼロ**を確認。
- **role の決め方**: 守れるポジションの**中央値**（C..PG 順）。
  結果の分布 C:734 PF:1001 SF:753 SG:975 PG:552（旧: C:650 PF:825 SF:855 SG:1073 PG:612）。
  守れるポジション数は 1個:2808人 / 2個:974 / 3個:176 / 4個:48 / 5個:9。
- **追加したデータ**: `identity.ts` の行末に `weightKg` と `posMask`
  （C=1 / PF=2 / SF=4 / SG=8 / PG=16 のビット和）。体重は全員あり（50〜99kg・中央75）。
- **配線**: `DbPlayer` に2項目追加 → `PlayerDef.weight` / `PlayerDef.posMask` →
  `Player.weight` / `Player.posMask`（applyDef で反映）。
  `roleFit()` が posMask を見るようになり、**交代・ラインナップの適格判定が実データになった**
  （従来は手書きの EXTRA_POSITIONS 1件のみ）。UI の「守れるポジション」も posMask を正とし、
  posMask を持たない def は従来の身長/脚力の推定にフォールバック。

- **利き手も Excel の R列（利き足）で更新した**（ユーザー指示）。**1238人が変更**。
  更新後の分布は R:3131 / L:884 で Excel と一致。
  ⚠️ 名前＋身長での照合は 4015人全員一致・取り違えゼロなので、**行対応はずれていない**。
  にもかかわらず個別が3割ずれていた＝**旧ブックと新ブックで利き足の値そのものが違う**
  （旧抽出時の列違い、または元データの更新）。新ブックを正とした。
- `scripts/update-identity.cjs` は**再実行できる**（旧6要素・新8要素のどちらの行も受ける）。
  実行: `WRITE=1 NODE_PATH=<xlsxを入れた場所>/node_modules node scripts/update-identity.cjs`

### 13. player-one のボクセル化に着手（V1/V2）

**リグは Mixamo/Unity humanoid 命名の52骨**（Hips/Spine/Spine1/Spine2/Neck/Head/
LeftShoulder/LeftArm/LeftForeArm/LeftHand/…/LeftUpLeg/LeftLeg/LeftFoot/LeftToeBase）。
→ **既存の `bone_maps/player.json`（SportsAvatar_3 = 同じ命名の66骨）が52骨すべてをカバー**
していたので、新規のマッピング作成は不要だった（余る14キーは指先/つま先で player-one 側は
ボーンでなく EMPTY）。

- 実測: メッシュ1個（47,116頂点 / 89,368ポリゴン / **シェイプキー0**）、マテリアル11、
  身長 1.815m、rest は **T-pose**（上腕が水平から約6°下）。
- ⚠️ **メッシュが1つしかなく部位分けが material にしか無い**。voxel-pipeline の `parts` は
  mesh オブジェクト単位なので、前処理で material → 部位オブジェクトへ割り直した。
  `blender_scripts/preprocess_player_one.py` を新規作成（`preprocess_player.py` が前例）。
  body = Torso+Arms Legs+face / hair = hair+scalp hair / eyes = eyes+lashes。
- 前処理の結果: T-pose を素に戻し、上腕を QM(ARP) の A-pose 方向へ **23.07°/22.95° 回転
  （残差 0.0°）**。分割は body 11,499頂点 / jersey 10,272 / shorts 4,463 / socks 7,467 /
  shoes 6,362 / hair 6,403 / eyes 802。
- `configs/player_one.json` を作成し、`--dry-run` で工程解決を確認
  （cleanup skip → modifiers → transplant --no-lbs → voxelize → fill skip → morphs skip）。

**V3 完了: ボクセル化に成功**（7部位・77.4秒・manifest conformant）。
出力 `game-assets/vox-model/player_one/`。

| | player_one | male_avatar（現行） |
| --- | --- | --- |
| voxel_size | 0.00746m | 0.00748m |
| 身長 | 1.842m | 1.845m |
| 腕を広げた幅 | 1.842m | 1.738m |
| **奥行き（厚み）** | **0.402m** | **0.476m** |
| body のボクセル数 | 104,534 | 195,824 |

→ **新モデルの方が厚みが薄い（0.402/0.476 = 84%）**。「厚みが大きすぎる」の是正に有利。
→ ボーン割当は ARP/QM 名で正しく付いた（c_spine_03_bend.x / head.x / c_arm_stretch.l/r /
　 c_thigh_stretch / c_forearm_stretch / shoulder）＝ **transplant は成功**。
　 body に c_leg_stretch がほぼ無いのは、すねが socks パーツ側にあるため（正しい）。

⚠️ **焼き込み（V5）で判明している要修正点**
（`function-lab/objcts/player/voxel/tools/buildParts.mjs`）:
- `VARIANTS` が `male_avatar{,_skinny,_muscle}` に**ハードコード**されている。
- `UNIFORM_PREFIXES = {tshirt, jeans, shoes}` で、player_one の prefix（jersey/shorts/socks/
  shoes）と合わない。
- player_one には**体型モーフが無い**ので変種が1つしか作れない。ゲーム側 `DATA` は
  skinny/normal/muscle の3つを要求するため、当面は同じものを3つに割り当てるか、
  シェイプキーを自作する（V4）まで1変種で動くようにするかの判断が要る。

### 14. player_one をゲーム用データへ焼き、モデル確認ページを作成（V5）

**焼き込み（`function-lab/objcts/player/voxel/tools/buildParts.mjs`）を汎用化**:
- `VARIANTS` を環境変数で差し替え可能に（`VOX_VARIANTS` に JSON。先頭 `+` で既定へ追加）。
- パーツ一覧を固定配列から**ディレクトリ実在チェック**へ（モデルで服の prefix が違うため）。
- `PRIO` を未知 prefix でも「服」扱いになる Proxy に、`ROLE` に jersey→Jersey /
  shorts→Shorts / socks→Shoes を追加。`segments.mjs` の `UNIFORM_PREFIXES` にも同3つを追加。
- 実行: `VOX_VARIANTS='{"p1":"player_one"}' VOX_SIZE=0.015 node tools/buildParts.mjs`
  → `data/body-p1.json`(484KB) / `cloth-p1.json`(538KB)。全パーツ **未割当0**。

**焼き上がりの比較（同じ物差しで実測）**

| | player_one | 現行 normal | 現行 muscle |
| --- | --- | --- | --- |
| 胴 厚み/幅 | 0.79 | 0.75 | 0.92 |
| **腰 厚み/幅** | **0.80** | 0.95 | 1.18 |
| 三角形/人 | **5,088** | 7,024 | 8,232 |
| 26人ぶん | 132,288 | 182,624 | 214,032 |

→ **腰の厚みが自然になり（0.95→0.80）、三角形は28%減った**。

⚠️ **重要な発見: 腕の長さはモデルを変えても変わらない。**
上腕 0.262m / 前腕 0.204m（比 1:0.78）は player_one でも現行と**完全に同一**だった。
**関節位置は QM(ARP) スケルトンから来ている**ため、素体を差し替えても骨は変わらない。
腕全体が身長比 25.8%（人体標準 33%）と短いのは**スケルトン側の性質**。
→ 「前腕が長すぎ・上腕が短すぎ」に見える件は**モデル差し替えでは直らない**。
　 直すならスケルトン（restPose / QM の骨長）か、腕の動かし方（M4 の IK/FK）を触る必要がある。

**モデル確認ページ `/confirm.html`（`src/confirm.ts`）を新設**:
- `voxelBody.ts` に variant `"p1"` と `setVariantOverride()` を追加（未設定なら従来どおり
  `variantFor(balance)`＝ゲーム側の挙動は変わらない）。元の objcts で編集 → `sync-objcts.mjs` で取り込み。
- 左右に別モデルを並べ、モーションを選んで 再生/停止/1コマ送り/速さ変更 ができる。
  三角形数・メッシュ数・fps を常時表示。**ゲームと同じ `Player` / `applyMotion` を通す**。
- `vite.config.ts` に2つ目のエントリ（confirm.html）を追加。`npm run build` ✓。
- ヘッドレスで p1 のメッシュ生成を確認（`probe-meshcost.ts` に `VARIANT` 環境変数を追加）。

### 15. 【訂正】QM への移植をやめ、モデル本来のリグでボクセル化し直した

ユーザー指摘: 「作り方を以前のモデルに寄せすぎ。ボーン設定があるはずだが設定していないのでは」。
**指摘のとおりだった。**

- 実測で確認: 移植版の `player_one/skeleton.json` は **male_avatar と完全に同一**
  （303骨・座標まで一致）。`transplant` は設計上「ボーン構造を QM と完全一致させる」もので、
  `--no-lbs` は**メッシュの形だけ**元のまま残す。つまり **肉は player_one・骨は QM** という
  状態で焼いていた。前回「腕の長さはスケルトン側の性質」と書いたのは、**自分が選んだ工程の
  結果**であって、モデルの性質ではなかった。検証を怠った。
- 対処: `configs/player_one.json` を **`transplant: "none"`** にしてソースの Mixamo 52骨を保つ。
  `voxelize.py` は blend 内の armature をそのまま skeleton.json に書くので、これで
  **モデル本来の関節**が出る（`Mapped vertex groups -> bones: 52/52`）。
- 焼き込み側を**リグ対応**にした（`segments.mjs` / `tools/buildParts.mjs`）:
  `mixamoToSegment` / `MIXAMO_PIVOT_BONE` / `MIXAMO_CHILD_BONE` / `MIXAMO_FINGER_CHAINS` /
  `MIXAMO_JOINT_BONE` を追加し、`detectRig()` で skeleton.json のボーン名からリグを判定して
  `rigTables()` で切り替える。ARP 側の既存テーブルはそのまま（male_avatar は従来どおり焼ける）。

**焼き直しの結果（モデル本来の寸法が出た）**

| | player_one（自前リグ） | 現行 normal（QMリグ） |
| --- | --- | --- |
| 上腕 / 前腕 | **0.231 / 0.239（1:1.04）** | 0.262 / 0.204（1:0.78） |
| 大腿 / 下腿 | **0.429 / 0.418** | 0.393 / 0.417 |
| 腰 厚み/幅 | 0.85 | 0.95 |
| 三角形/人 | 5,076 | 7,024 |

→ 移植版では現行と**完全に同一だった**骨の寸法が、モデル固有の値になった。
⚠️ ただし player_one 自身の比率は **前腕が上腕より長い（1:1.04）**。人体標準は 1:0.78 なので、
　 このモデルは腕の見え方に癖がある。実機で確認して許容できるか判断が要る。

⚠️ ブラウザ実機（`npm run dev`）での見た目・操作感は未検証。ドラッグ＆ドロップと盤のレイアウトは
   実機でしか確認できない。

## 2026-09-05 髪の被覆問題 — 幾何フィットを破棄し「頭皮を髪色に塗る」で対処

**問題**: 髪型 002 などで頭頂・後頭部が髪で覆われず、地肌が透けて見える。

**やって駄目だったこと（すべて破棄済み）**

1. `HAIR_MARGIN` を 12mm → 24mm へ拡大 ＋ 上/後ろへずらす
   → **悪化**。実測で被覆 001: 74%→21%、002: 66%→28%。
   さらに測ると、頭頂ボクセルから最寄りの髪までの距離が中央値 3.6cm。
   「髪が足りない」のではなく **髪の殻が浮いていた**。原因は、髪メッシュの bbox を
   「頭の大きさ」とみなしてスケールを決めていたこと（bbox は髪の**外側**であって頭皮ではない）。
2. 素体の頭皮頂点へレイを飛ばして倍率・前後・上下を自動フィット（縦横別倍率まで含む）
   → 数値上の被覆は 85〜100%、隙間 0〜3.7cm まで改善したが、
   **髪の位置が低くなるなど見た目が崩れた**のでユーザー確認で却下。

→ `blender_scripts/add_hair_player_one.py` は元の方式（短髪 bbox 中央値・一律スケール・
   余裕 12mm）へ戻した。戻した状態で焼き直した `public/vox/player_one` は
   **コミット済みデータとバイト一致**（＝復元が正確であることの裏付け）。

**採用した対処**: `src/voxraw.ts` `applyScalpTint(part)`

- 髪ごとに「高さの層 × 方位」の最大半径を取り（服の `clothCover` と同じ考え方）、
  その内側にある**頭皮ボクセルの頂点カラーを髪の色に書き換える**。
- 髪型を切り替えるたびに `showHair()` から呼ぶ。`""` を渡すと元の肌色へ戻る。
- body メッシュだけ `applyToMesh(mesh, updatable=true)` にして色を後から差し替える。
- ⚠️ 顔（頭頂から 9cm より下の前面）は塗らない。前髪のある髪型だと方位判定が顔まで
  覆ってしまい、顔が髪色に染まる。

**実測**（`headless_sim/probe-scalptint.ts`。ブラウザと同じ `buildRawModel` を通す）

| 項目 | 結果 |
| --- | --- |
| 塗り替えボクセル | 髪型あたり 1,558〜2,046 |
| 塗った色 | 全 13 種で髪の色 (14,6,6) と一致 |
| 頭頂・後頭部の塗り残し | 0%（全種 100% 塗られている） |
| 顔にかかった頂点 | 全種 0 |
| 「髪なし」で元の肌色へ復元 | OK |

⚠️ これは数値上の確認。**ブラウザ実機（`/confirm.html`）での見た目は未確認**。
   髪と肌の境目が目立つ場合は `applyScalpTint` の `margin` で調整する。

**この件で学んだこと**

- `uv run voxel-pipeline <name>` は `--keep-work` を付けないと中間 blend
  （`prep.blend` / `prep_hair.blend`）を消す。作り直しに 2 工程かかった。

## 2026-09-05（続き） 顔（あご）のバリエーションと、首が消えていた件

**やったこと**

1. **顔のバリエーション**（`src/voxraw.ts` `JawShape` / `reshapeJaw` / `setJaw`）
   あご帯（頭頂から 0.130〜0.265m）だけを作り替える。標準 / 丸顔 / 細あご。
   - 横幅と前方向の奥行きを高さに応じて縮める（`JAW_PROFILE`）
   - あご材料を上へ寄せて顔を短くする（`shorten`）。空いた下は首が埋める
   - 実測の出発点: 頬〜あごの幅が 0.166m で**ほぼ一定**＝絞りが無い。これが「四角いあご」の正体
   - `/confirm.html` に「顔（あご）」セレクタを追加

2. **首が丸ごと消えていたのを修正**
   `clothCover` は「同じ高さの層にある服の**最大**半径」で内外を決めるため、首の高さの層に
   肩・胸（半径0.39）が入り、半径0.07 の首は必ず「服の中」と判定されて全層で落ちていた。
   「服の**最内殻**より内側なら襟の穴＝上から見えるので残す」（`inHole`）に変更。

**この件で踏んだ落とし穴（同じことを繰り返さないための記録）**

- ⚠️ **逆写像は1点で引いてはいけない**。幅を 0.8 倍に縮めると元の格子を 1.25 個おきに
  飛ばして拾うので、薄い表面が抜けて穴が開く。実測で丸顔の Blender Y=0 の面が丸ごと消え、
  左右からレイが貫通していた。セルが覆う範囲（±半ボクセル）を写した先を全部見ること。
  その代わり量子化で1ボクセル太るので、削り係数はその分強めにしてある。
- ⚠️ **削除する範囲は高さだけで決めない**。あご帯の高さで「窓の外」を見ずに消したら
  T-pose の腕（|x| 最大 0.9）が丸ごと消えた。輪郭クリップでも下限を切らずにやったら
  頭の真下にある脚・胴の前面が消えた。
- ⚠️ **隙間は「縫い目」だけ埋める**。最大5層（4.4cm）まで埋めたら、あごの下と首の間
  （本来空いている）が塊で埋まり、突起になって首を覆い隠した。
- ⚠️ **削り具合は「出す先の高さ」で決める**。元の高さで決めると、下から持ち上げたあご先が
  「首の材料」と判定されて削られず、突起として残る。
- ⚠️ 髪と同じで、**あごも「大きさ・位置をいじって直す」方向は失敗しやすい**。
  余裕や帯の位置を動かすたびに別の場所が壊れた。原因を実測で特定してから直すこと。

**検証**（`headless_sim/probe-jaw.ts` / `probe-seethrough.ts` / `probe-skinpoke.ts` / `probe-legs.ts`）

| 項目 | 結果 |
| --- | --- |
| あご先（前へ張り出す一番下） | 標準 0.245m → 丸顔 0.210m / 細あご 0.201m |
| あご下の前方への張り出し | 0.096 → 0.026（丸顔・細あご） |
| レイの貫通（透け） | 3形状とも 0 |
| 首の輪の欠け | 全層 16/16 方位 |
| 服からの肌のはみ出し | 0/16 |
| 脚・腰胴・腕肩 | 3形状とも完全一致 |
| 髪の下の頭皮の塗り | 維持 |
| `setJaw()` の張り直し | 独立ビルドと完全一致 |

⚠️ **残件**: 丸顔・細あごで、正面右側の目の高さあたりに1段の横線状の突起が出る（ユーザー報告）。未着手。
⚠️ ブラウザ実機での見た目は都度ユーザー確認に依存。ヘッドレスの数値だけでは判断できない。

## 2026-09-05（続き2） 顔（目・口）を描いて貼る／髪型139種／髭を顎へ

**1. 目と口を「描いて顔に貼る」**（`src/voxraw.ts` `faceMarks`）

実測して分かったこと:
- 顔の前面の色は肌色 70〜75 階調だけで、**唇も眉も色として残らない**
- 暗い領域は唇ではなく**口の中と眼窩**（肌より最大 88mm 奥）
- ソースの眼球メッシュは瞼の内側で肌より **26mm 奥**

→ 元モデル（function-lab の `buildParts.mjs` `splitFace`）と同じく**描く**。
  `eyeBlock(3,2)`（白目3×2・黒目は上段中央1マス）と `mouthLine(HW,1,0)`（横 2*HW）を、
  **body の縦横半分のボクセル(4.37mm)** で倍の枡目にして再現: 白目6×4・黒目2×2・口8×2。
- 眼球パーツは preprocess で削除（material eyes/lashes を落とす）
- 貼り付けは「そのマス自身の列にある一番手前の肌」から 1/4マス食い込ませる＝3.3mm 手前に出る

⚠️ 落とし穴:
- 升目は `Math.round(x/S)` ではなく**セル番号** `floor((x-原点)/S)` で引く。格子の原点は
  ボクセル寸法の倍数ではないので、丸めると隣のセルを見る（実測で 64マス中14マスが 9.8mm 埋没）
- 貼り付け先が空のときは周囲の**一番奥**を採る。一番手前を採ると浮く（実測5マス）

**2. 髪型 139 種 + 遅延読み込み**
- 20MB・438ファイル。全部読むと確認ページが開かないので、**選んだときに読む**（`loadHair`）
- 最初の読み込み 2.2秒・メッシュ7個 / 1種あたり約60ms
- 髪型は単色(14,6,6)で焼けるので、**モデル本来の髪の色分布(182階調)を位置ハッシュで写して**まばらに
- 髪の位置は body のボクセル2個ぶん(17.4mm)上げた（`HAIR_LIFT`）

**3. 髭を顎へ**（`add_hair_player_one.py` `is_beard`）
- コレクションには髪型でなく**髭単体のパーツ**が混ざる。髪型と同じ規則で置くと、実測で
  Man_Hair_133 は「頭頂から 0〜0.078m＝額の高さ」かつ「顔の表面より内側」＝**顔の中に埋まる**
- 髭が乗るべき場所（実測）: 頭頂から 0.159〜0.243m、あごの肌の前端 Y -0.109〜-0.090 より前
- 判定は「顎に収まる大きさか」＝スケール後の実寸で 幅≤22cm・奥行≤16cm・高さ≤17cm の**3辺すべて**。
  ⚠️ 「頭の幅・奥行の3/4を張れるか」で判定したら、幅5.9cmのポニーテール(052)や高さ44cmの
  長髪(130)まで髭にされた。7件（041/042/047/072/080/098/133）が髭。
- 髭付きの髪型（実測 74/139 件）はあごの3形状に追従する。`reshapeJaw` は**体から求めた頭の情報**
  (`JawFit`) を受け取る形にした。髪型自身から中心を取ると髭だけの範囲で計算して顔からずれる。
  首の輪郭クリップと隙間埋めは body 専用（髪に掛けると髭が丸ごと消える）

**4. 頭皮を髪色に塗る機能は削除**（髪の位置調整で対応するため不要になった）

**この期間に踏んだ落とし穴（再掲・重要）**
- ⚠️ **逆写像は1点で引いてはいけない**。縮小率0.8だと元の格子を1.25個おきに飛ばして拾い、
  薄い表面が抜けて穴が開く（実測: 丸顔で Blender Y=0 の面が丸ごと消え左右から貫通）。
  セルが覆う範囲を全部見る。ただし範囲を**ちょうど半ボクセル**にすると倍率1.0でも隣を拾って
  1ボクセル膨らみ、目の高さに横線状の段差が出る。0.49 ボクセルにすること。
- ⚠️ 削除する範囲は高さだけで決めない。腕（|x|最大0.9）や脚の前面が丸ごと消えた。
- ⚠️ **再修正の指示を受けたら、まず前回の修正をリバートしてから着手する**。失敗の上に
  重ねた結果、直っていた「あごの輪郭」まで崩した。ユーザーから明確に指摘された。

⚠️ 残件: 後頭部が縦にまっすぐで、首にかけて細くなるカーブが無い（ユーザー指摘）。未着手。

## 2026-09-05（続き3） 後頭部・ユニフォームの粒度・モーションの腕の角度

**1. 後頭部が縦にまっすぐだった**
ボクセル化の内部塗り潰しは `(x, y)` の列ごとに**垂直にレイを飛ばし交差の偶奇**で内側を決める。
body は髪・頭皮・目を別パーツへ分離したため**穴が空いており**、偶奇がずれて
「後頭部の膨らみと肩の間のくぼみ＝うなじ」まで塗り潰されていた。

| 頭頂から | 元メッシュ | 修正前 | 修正後 |
| --- | --- | --- | --- |
| 0.081 | 0.1082 | 0.107 | 0.107 |
| 0.169 | 0.0853 | 0.107 | **0.088** |
| 0.244 | 0.1070 | 0.107 | **0.116**（肩へ） |

→ body に `--no-interior` を付けて表面のみで焼く。ボクセル 18,136 → 16,485。

**2. ユニフォームの粒度を body に揃え、柄だけ細かく貼る**
- 衣類は 6.26mm(scale 3) で body(9.38mm) より細かかった → **9.38mm(scale 2)** に統一
- エンブレム・背番号・NAME・ロゴだけ `jerseymark` として **4.69mm(scale 4 = 半分)** で貼る
- 抜き出しは `add_jersey_marks.py`。Shirt.jpg の赤でない画素を連結成分に分けると
  背番号 60x94 / エンブレム 29x36 / NAME の文字 12〜15x16（縦横比 1.0〜1.6）に対し
  **サイドのストライプは 23〜26x217〜228（縦横比 8.7〜9.8）**。縦横比 4 で分かれる
- ⚠️ 柄は文字の周りのポリゴンごと抜くので**地の赤が混ざる**（5,279個中2,476個）。
  voxraw 側で「土台の一番多い色に近いボクセル」を落とす（実測で混入 0）

**3. すべてのモーションで腕が横に開いていた**（ユーザー指摘。実測で確定）
`applyMotion` はクリップの回転を**ボーンのローカル回転としてそのまま代入**する。
ローカル回転の基準は rest の向きなので、rest が違うとその差が全モーションのずれになる。

| | 上腕の rest（水平からの下がり） |
| --- | --- |
| ゲーム標準 `male_avatar`（クリップはこのリグ前提・ARP） | **27.4°** |
| player_one（T-pose） | **0.0°** |

実測 idle 34.0° + 27.4° = 61.4°（自然な立ち姿）と一致したので原因確定。
→ `preprocess_player_one.py --apose` を有効化して rest を A-pose で焼く。
⚠️ 以前「transplant=none なので A-pose 化は不要」と判断したのは誤り。**モーションが rest を
　 基準にしている**ことを見落としていた。
⚠️ A-pose の回転は「rest の向きから」計算すると FBX 付属ポーズのぶんずれる
　 （残差 7.5°・結果 21.3°）。**今の向きから**目標へ回すこと（残差 0.0°）。

結果: idle 34.0°→**61.4°**、手の横方向 0.590→**0.426m**。run 28.6〜35.1°→41.8〜60.5°。

**⚠️ 未対応（次の作業）: 身長・体重による体格の変化**
- 選手データには 4015 人分の `heightCm` / `weightKg` があるが、`src/` で使っている箇所は **0件**
- `buildRawModel(scene, baseUrl, {jaw})` は身長も体重も受け取らない
- 材料: `restPose.ts` の `scaleToHeight` / `scaleRestPoseXYZ`(軸別)、あごで作った逆写像の変形
- 方針: ボクセルは焼き直さず**表示時に変形**。まず身長だけ入れて実機確認 → 次に体重による厚み

## 2026-09-05（続き4） 身長・体重による体格

**1. 身長**（`setHeight` / `setBody`）
ボクセルは焼き直さず表示時に伸縮する。
- モデル自身の身長は **180.4cm**（足元＝全部位の最下端 〜 頭頂＝**body** の最上端）を実測して使う
  ⚠️ `grid.json` の bb を使うと髪型139種の高いものが混ざって 206.3cm になる
- 頭だけ伸縮率を変える（`HEAD_EXP = 0.5`）。一様に拡大すると背の高い選手ほど頭が大きい子供体型になる
- 頭を別倍率にすると単純な比では身長が合わない（実測で最大1.1cm ずれ）ので、目標身長に一致する
  倍率を反復で解く

| 指定 | 全体 | 頭 | 実際の頭頂 | 頭の大きさ |
| --- | --- | --- | --- | --- |
| 158cm | x0.869 | x0.932 | 158.2cm | 18.8cm |
| 180.4cm | x1.000 | x1.000 | 180.6cm | 20.2cm |
| 203cm | x1.133 | x1.064 | 203.2cm | 21.4cm |

**2. 体重**（厚み）
厚み倍率 = **sqrt(BMI / 23.1)**。23.1 は選手4015人の BMI 中央値（範囲 17.0〜29.4）の実測。
- **骨の軸から見た横方向だけ**伸縮する。体の中心から一律に伸ばすと腕が外へ飛ぶ
  （T-pose なら 0.9m の位置が 15% 外＝13cm 移動）
- 骨ごとの効き方 `THICK_BY_BONE`: 頭・首 0 / 手足 0.3 / 肩 0.6 / その他 1.0
- ボクセルの箱も同じ割合で広げるので隙間ができない（伸び方は骨の向きで軸ごとに変わる）

| 体重(180.4cm) | BMI | 厚み | 胴の幅 | 胴の厚み | 頭の幅 |
| --- | --- | --- | --- | --- | --- |
| 55kg | 16.9 | x0.855 | 0.291 | 0.235 | 0.188 |
| 75.2kg | 23.1 | x1.000 | 0.338 | 0.272 | 0.188 |
| 95kg | 29.2 | x1.124 | 0.379 | 0.304 | 0.188 |

**3. 細い体型で袖と胴が離れた**（ユーザー指摘）
一番強い骨だけで変形すると、袖(腕の骨)と胴(胸の骨)が別々に縮んで脇に隙間ができる。
→ **スキンウェイトで最大4本の骨の変形を混ぜる**（`thickenBlend`）。

| 体重 | 脇の下の隙間 修正前 → 修正後 |
| --- | --- |
| 50kg | **17.6mm → 7.6mm**（肩は 22.2mm → 9.0mm） |
| 75.2kg | 9.4mm → 9.4mm |

ボクセル1個が 9.4mm なので、修正前は約2個ぶん空いていた。

⚠️ 計測の注意: このユニフォームには**袖がある**（高さ1.45で jersey の幅 0.75m）。
   「胸の幅」を 1.35〜1.45m で測ると袖が混ざる。袖の無い 1.15〜1.25m で測ること。

**⚠️ 未対応（次の作業・ユーザー要望）**
- 細い体型の肩幅が広い。肩幅も体格に応じて狭くしたい
- 脚は太さが変わるが**腕がほぼ変わらない**。原因: 厚みの計算で横方向(Blender x,y)しか
  伸縮しておらず、腕は軸がほぼ水平なので**垂直方向(z)の太さが変わらない**。
  perp を3軸とも伸縮すれば直る（箱の倍率は各軸 m + (1-m)*axis_i^2）

**追記: 腕の太さと肩幅**（ユーザー指摘の2件）
- **腕の太さが変わらなかった**: 厚みの計算で骨の軸に垂直な成分のうち横2方向(x,y)しか
  伸縮していなかった。腕は A-pose で軸が (0.89, 0.06, -0.46) とほぼ水平なので、太さの向きに
  含まれる**垂直成分が伸縮されず**ほぼ変わらなかった。→ **3軸すべて**で伸縮する。
  軸に沿う成分は伸ばさないので手足の長さは変わらない。箱の倍率は各軸 `m + (1-m)*axis^2`。
  実測: 前腕の縦 55kg 0.131→0.118 / 95kg 0.150→**0.162**（修正前はほぼ一定だった）
- **細い体型の肩幅が広い**: 厚みの変形は骨の位置を動かさないので肩幅は変わらない。
  → 肩の骨自体を体格に応じて動かす（`SHOULDER_W = 0.7`）。
  ⚠️ 肩の骨だけでは +6.4% しか変わらない。実測で Chest→Shoulder 0.067m に対し
  **Shoulder→UpperArm（鎖骨）0.135m** が主。両方動かすこと。
  実測: 肩幅 55kg **0.363** / 75.2kg 0.404 / 95kg **0.439**

---

## 2026-09-11 パス・シュートの反動動作（ワインドアップ）

**要望**: パスやシュートがほぼノーモーションで出ている。打つ前に小さくとも反動動作
（腕を少し逆へ動かし、一瞬止めてから投げる）を入れたい。

**計測した原因**（`probe-windup` / `probe-wind2`）
- パス 641 本のうち**溜めがあったのは 15%**。残りはその場で即リリース。
  溜めが入っていたのはターンパス（ピボット）とトラップ越しのキックアウトだけだった。
- シュートの溜めは**中央値 0.017秒 = 1フレーム**。原因は `updateCharge` の
  「目の前まで詰められたら溜めを打ち切る」判定（`ndGap < 1.05 + 反応*0.55`）が
  **溜め0秒でも発火**し、守備者が間合いに居るだけで初回フレームに放っていたため。
- さらにシュートの **57% は `finishAtRim`（レイアップ/ダンク）** で、そもそも charge を
  通らない。こちらは踏み切りの跳躍自体がモーションなので今回は触っていない。

**入れたもの**
- `PASS_WIND = { base: 0.13, perM: 0.008, min: 0.10, max: 0.30 }`（config）と
  `PASS_STYLE[].wind`（chest 1.0 / bounce 1.1 / overhead 1.25 / jump 0.6）。
  距離が伸びるほど大きく引き、P速度が高いほど短い。片手投げは半分。
  強制パス・空中アウトレット・ターンパス（ピボットが反動を兼ねる）は溜めない。
- `pendingPassWind` / `pendingPassDur` / `windReleased` を追加。安全ゲートを通った
  （＝投げると決まった）地点で溜めに入り、溜め切ってから `windReleased` で放つ。
  ⚠️ リリース時にゲートを通し直すと「溜める→拒否→また溜める」で持ち続けるので通さない。
  カット判定（`evalInterception`）はリリース時に走るので、**溜めている間に詰めた守備者は
  間に合う** — 溜めがカット判定に効く形になっている。
- シュート: `QUICK_RELEASE = 0.17 - シュート技術*0.05`。詰められた時の打ち切りと
  ブロック差し込みの早撃ちは、この最低限を溜めてからにする（クロック逼迫だけ例外）。
- ポーズ: 溜め中は `windBallX/Y/Z`（**溜めの1フレーム目のボール位置**）を起点に、
  胸のすぐ前（パス）／おなかの前（シュート）へ smoothstep で引き付け、`u>0.55` で止める。
  手は `catchBall` でボールを追うので、腕がそのまま投げる方向と逆へ動いて止まる。
  ⚠️ 固定の構えへ**瞬間移動させてはいけない**。最初 `passToReceiver` の中で起点を
  捕まえたら、同じフレームでドリブル側がボールを上書きするため 1 フレームずれて飛んだ。
  起点は**次フレームのポーズ側で遅延して捕まえる**（`windBallY = -1` を番兵にする）。

**実測（前 → 後）**
- 溜めがあったパス 15% → **67%**（中央 0.167秒）。残りは強制・空中アウトレット。
- シュートの溜め 中央 0.017秒 → **0.133秒**。charge を通る割合 33% → 43%。
- 引き量（投げる方向を + として）パス 中央 **0.106m** / シュート **0.080m**、
  引き切ってからの静止 0.067秒。
- 副作用: FG 58% → **55%**（ミドルジャンパー 56% → 49%）。守備に 0.13 秒ぶん
  余裕が出たぶん。実在値（2P 約52%）へ寄る方向。得点 15.3 → 14.7（4分ゲーム）。
  ブロック 4.00 → 4.33 本。**ターンオーバーは 1.8 → 1.3 と増えていない**。

### 追記: シュートは「頭上に構える前」が反動動作（ユーザー指摘）

- 指摘: 反動動作は**頭上でボールを構える前**の動きにして、構え終わりにはボール自体が
  頭上にある状態にしてほしい。
- 直前の実装では、溜め中ずっとおなかの前で、**リリースの瞬間に releaseShot が
  2.05m へワープ**させていた（計測: 構え終わりのボール高さ 中央 1.25m）。
- 溜めを3段に作り替えた（進捗 0..1、`setChargeBall`）:
  A) 0〜0.35 反動 = 今ボールがある位置からおなかの前(≈1.09m)へ引き付けて沈む
  B) 0.35〜0.50 そこで一瞬止まる
  C) 0.50〜1.0 頭上の構え(2.05m ＝ リリース点)へ持ち上げる → そのまま放つ
  実測: 構え終わりのボール高さ 中央 **2.05m**。
- **急ぎ撃ちは「途中で切る」のをやめた**（`rushCharge`）。詰められたら残りの進捗を
  短い秒数で駆け上げる。進捗（`chargeP`）で持っているので、残り時間を縮めても
  進捗が飛ばない＝ボールがワープしない。精度の `prepDone` は実際に溜めた秒数。
- `MIN_MOTION = 0.42秒`。⚠️ ポケット(1.09m)から頭上(2.05m)まで 0.95m あるので、
  これより短いとボールが 1フレーム 0.37m（22m/s）で駆け上がって飛んで見える。
- ⚠️ **溜めの頭から守備者を走らせない**（`chargeP >= CHARGE_LIFT` でクローズアウト開始）。
  抱えている段階ではまだシュートと決まっていない。入れないとモーションを長くしたぶん
  一方的に成功率が落ちた（ミドル 49% → 35%）。コンテストの間合いも振り上げ開始で凍結する
  （`chargeDDefLift`）— 上がり始めた後に着いた守備者は遅れたクローズアウト。
- 実測（72試合）: 全体 57% / 3P **32%** / ミドルジャンパー **46%**。
  変更前は 3P 29% / ミドル 56%。ミドルが実在値(約40%)へ寄った。

### 追記: 守備の動き・方向転換・ジャンプの反動動作

**1) 加速の傾き（全選手・見た目のみ・シム影響なし）**
- `Player.accX/accZ` に平滑化した加速度を持ち（`tickMotion`、`ACC_SMOOTH = 11`）、
  `sync()` の既存の傾き（`lean`）に足し込む。踏み出しは進む方へ、ブレーキは逆へ倒れる。
- `ACC_TILT = 0.015 rad / (m/s^2)`、上限 `ACC_TILT_MAX = 0.16`（≈9°）。
  実測: 動いている選手の傾き 中央 5.2° / 95% 11.0°。

**2) 切り返し・急停止のプラント**
- ⚠️ 加速度からは踏ん張りが出てこない。**シムは方向転換を1フレームで済ませ、減速を
  経ない**ため（実測: プラント中に加速度が進行方向と逆だったのは 7% だけ）。
- そこで既にある `plantT`/`plantDur`（切り返し・急停止のスロットル）から直接出す。
  プラントに入った瞬間の進行方向を `plantDirX/Z` に保存し、体を**その逆**へ倒す
  （`PLANT_TILT = 0.23 rad ≈ 13°`）、`plantT` が減るにつれ起き上がる。
  ⚠️ 符号に注意。元の進行方向へ倒すと「流れているだけ」で反動に見えない。
  ⚠️ `setPlant()`（スティールの踏み込み等）でも向きを必ず入れ直す。入れないと
  前のプラントの向きが残る。
- 実測: プラント中の傾き 中央 8.7° / 95% 20.7°。

**3) 踏み切り前の沈み込み（ジャンプ）**
- `jump()` はその場で離陸せず、`loadT = jumpLoadFor(p, height)` を立てるだけにした。
  `updateJump` が使い切った瞬間に離陸する。沈み込み中 `airborne` は **false**。
- 見た目は `applyShootLoad` を流用（`POSE_JUMP = {lean:0.12, thigh:0.46, shin:0.30}`、
  ほぼ垂直に沈む）。`voxel-motion` も `jumpLoad` 中はクリップを止める。
- リバウンドの踏み切り判断（`looseball.chaseLoose`）は沈み込みぶん**早く跳び始め**、
  頂点の時刻も `L` だけ後ろにずらして解く。
- ⚠️ **長さは実測で決めること**（60試合、`probe-rebside`）:
  - 0.10〜0.20秒 → オフェンスリバウンド **32%**（実在値 22〜28% を超える）
  - 0.03〜0.10秒 → **27%**（帯の中。従来の 26〜28% を維持）
  空中の競り合いは数フレームに敏感で、全員の踏み切りが遅れると落下点で競れず、
  こぼれ球が増えて攻撃側に回る。採用は `(0.078 - 敏捷性*0.028) * 深さ` = 0.03〜0.10秒。

**全体の実測（12試合・各4分、変更前 → 後）**
- シュートの溜め 中央 0.017秒 → **0.433秒** / 溜めがあったパス 15% → **68%**
- ブロック 4.00 → 4.08 本 / ターンオーバー 1.8 → 2.2
- 得点 15.3 → **13.6**（要観察。モーションを短くすれば戻るが、その分ノーモーションに近づく）

### 追記: スティールを「突き(パンチ)」モーションにする

- 指摘: ほぼ棒立ちのまま体を斜めにして腕を振っているだけに見える。腕を前に突き出し
  ながら上半身を回転させてリーチを長くし、シュートと同じく小さな反動を入れてほしい。
- 旧 `digReach` は**毎フレーム同じ最終形**を当てていた（胸をボールへツイスト＋肘を
  伸ばしてロック）。段階が無いので「伸ばしっぱなしの腕が付いてくる」ように見えていた。
- スティールは既に `beginAction("steal", 0.12, 0.03, 0.6)`（溜め→突き→回復）の段階を
  持っている。`digReach` をその段階に同期させ、**ボールに当たるフレームと腕が伸び切る
  タイミングを揃えた**（`punchPhase`）。段階を持たない密集のはたき（`game.steal`）は
  `stealReachDur` を立てて時間で割る（溜め 30%）。ルーズボールの掻き出しは従来どおり
  溜め無しで伸ばす。
- 動きの中身（⚠️ **体幹→肩→肘**の順。肘を先に伸ばすと「腕を振っている」だけに見える）:
  - 溜め: 胴を的と**逆**へ 0.40 倍ひねる、肘を 1.20rad 畳んで手を胸元へ引く、少し後傾
  - 突き: ツイストを振り抜いて的へ → 肩が先行(`ramp 0→0.85`) → 肘が遅れて伸び切る
    (`ramp 0.30→1.0`)。反対の腕は逆へ振れてカウンターウェイトになる
  - 踏み込み: `digLeanX/Z` を `sync` の傾きへ足す（溜め -0.05rad → 突き +0.16rad ≈ 9°）
- 実測（`probe-steal`、6試合／7本）: 踏み込み **-0.050 → +0.160**、胴のツイスト
  **-0.108 → +0.475rad(27°)**、手のリーチ 0.486 → 0.550m。
  **逆へ引いてから突いた割合 100%**。
- ⚠️ 未対応: 踏み込みの**足のステップ**は入れていない（`pos` を動かすとスティールの
  成否判定の間合いが変わるため）。体の回転と前傾だけで「突き」を見せている。

### 追記: スティールの踏み込みステップと、マーク位置の基準

**間合いの実測（ユーザー指摘の確認）**
- 指摘どおりだった。担当守備者との距離: リム〜3m 中央 **0.79m**（25% 0.62m）、
  3〜6m **0.99m**、6〜9m 1.46m。`BODY_MIN_DIST = 0.62m` に張り付いている帯があり、
  実在のオンボール（0.9〜1.5m）より明確に近い＝ほぼフェイスガード。
- 原因は2つ:
  1. 深さの係数の**下限** `0.45 + 0.55*depth` → リム前の目標が 1.25*0.45 = 0.56m。
     → `0.68 + 0.32*depth` に変更（遠い側は据え置き）。下限クランプ 0.3 → 0.42。
  2. `postUp` が「ビッグがリムから5.5m以内」で発火し、フェイスアップのビッグまで
     0.35m のボディアップになっていた。→ 背負っている(`postT>0`)か本当のローポスト
     (3.2m以内)のときだけに限定。
- 結果: リム〜3m **0.91m** / 3〜6m **1.02m** / 6〜9m 1.44m。
  ⚠️ リム〜3m は「クッションの目標」ではなく**ドライブの接触**で決まるので、式を
  いじってもほとんど動かない（0.79 → 0.91）。ここは実在でも体が当たる間合い。

**踏み込みステップ**
- `stealLunge`: 突きの溜めで体ごとボールへ **0.34m** 詰め、回復（0.6秒）で戻る。
  体が重なる距離（`BODY_MIN_DIST`）までしか入らない。
- ⚠️ これは見た目だけではない。スティールの成否は**実行フレームの間合い**
  (`gap < reach`) で決まるので、踏み込んだぶん届く。
- ⚠️ **仕掛けの射程も広げないと踏み込めない**。従来は `gap < reach`（腕が届く距離）
  でしか仕掛けず、突きは常に密着から始まっていた（実測: 仕掛け時 0.70m、
  踏み込めたのは 0.146m だけ）。`gap < reach + LUNGE_IN` に変更。実行フレームの判定は
  `reach` のままなので、届かなければ空振り＋隙（`reactT = 0.3`）になる。
- 実測: 踏み込み **0.340m**、マークとの距離 0.928m → 0.721m。

**突きの胴回転**
- 的へ向けるツイスト(tw)は守備者が既に正対しているぶん小さく（実測 0.475rad）、
  それだけではパンチに見えない。**tw を追い越して**突く側の肩を前に出す
  （`PUNCH_TWIST = 0.50` / 溜めで `COCK_SHOULDER = 0.30` 引く）。
- ⚠️ 回転の符号はリグ依存。推測せず `probe-steal` で「手がボールの向きへどれだけ
  出るか」を実測して決めた: **-1 で 0.586m / +1 で 0.509m / 回転なし 0.399m**。
  → `PUNCH_SIGN = -1`。回転を入れるだけでリーチが **0.399 → 0.586m** 伸びる。

**バランス（マーク位置を広げた副作用）**
- 広げただけだと守備が緩み、ミドルジャンパーが 46% → **63%** に跳ねた。
- シュートの溜めのどこからクローズアウトを始めるか（`CLOSEOUT_AT`）で調整:
  | CLOSEOUT_AT | ミドル | 3P | 得点 |
  |---|---|---|---|
  | 0.50（振り上げから） | 63% | 23% | 15.8 |
  | 0.30 | **41%** | **40%** | **16.1** |
  | 0.0（溜めの頭から） | 46% | 21% | 13.3 |
  → **0.30 を採用**。実在値（3P 約36% / ミドル 約40%）に最も近く、得点も戻った
  （溜め導入で 13.6 まで落ちていたものが 16.1）。
- ターンオーバー 2.0／ブロック 3.33本。
- 大きい標本での確認: シュート 72試合 → 全体 59% / **3P 34%** / **ミドルジャンパー 45%** /
  リム至近 69%。リバウンド 48試合 → **オフェンスリバウンド 28%**（実在値 22〜28% の上端）。

### 追記: 確認ページにスティールを追加（試合と同じ経路・その場で調整）

- 確認ページ（`src/confirm.ts`）に「スティール」を追加。⚠️ **試合とまったく同じ手順**で動かす:
  `beginAction("steal", …)` → `tickAction` で段階を進める → `stepLunge` で踏み込み →
  `digReach` でポーズ → `Player.sync()`。確認ページ専用のポーズ処理は一切書いていない。
- そのために調整値を**共有の可変オブジェクト**へ出した。⚠️ 定数に戻さないこと。
  戻すと確認ページで見たものと試合が別物になる（このページが作られた元々の理由）。
  - `animation/action/reach.ts` → `export const PUNCH = { twist, cockShoulder, cockElbow,
    cockLean, lungeLean, thrustT, sign }`
  - `ai/defense/vs-onball/index.ts` → `export const STEAL = { lungeIn, windup, active, cooldown }`、
    `export function lungeWant(d)` / `stepLunge(d, bx, bz, room)`（試合側の `stealLunge` は
    この2つの薄いラッパー）
- スライダー: 相手との距離／相手のボールの高さ／踏み込む距離／溜めの長さ／伸び切るまで／
  肩を出す回転／溜めで肩を引く／溜めで肘を畳む／踏み込みの前傾／胴回転の符号。
  現在値は情報欄に表示するので、良い値が出たらそのままソースの初期値へ書き戻せる。
- ボールは床のアンカーに固定して置く（踏み込みで選手が前へ出るのが見える）。
  突きは 0.6 秒間隔でループ。
- ⚠️ `stepLunge` は試合と同じく**毎フレーム**呼ぶ。段階中だけ呼ぶと終了フレームの
  踏み込みが戻らず、ループのたびに選手が前へジリジリ出ていく。
- ⚠️ `stealLunge` の `clampCourt` は**動いた時だけ**呼ぶ。毎フレーム丸めると、
  ライン外に立っている守備者をコート内へ押し戻してしまう（スローイン時の配置が変わる）。
- `npx vite build` 通過（confirm チャンク 10.06 kB）。

### 追記: 突きの作り直し（ユーザー指摘4件＋リバウンドの同型バグ）

**1) 小刻みな反復運動（致命的）— 閉ループだった**
- `digReach` が決める突く腕は `dribbleWithRight()` を使い、その中で
  `th = root.rotation.y + torsoTwist` を読んでいた。ところが `digReach` は**自分で
  `torsoTwist` を書く**。入れたひねりが次フレームの左右判定を裏返し、
  腕と胴が毎フレーム左右に振れていた（実測: 判定の入れ替わりが毎フレーム）。
- ⚠️ **腕の左右を決める式に `torsoTwist` を混ぜてはいけない**。体の向き(root のヨー)
  だけで決め、決めた側は突いている間ずっと保持する（`punchRight` / `punchHoldT`）。
- **リバウンド側も同じバグ**だった。`stretchOne` が `torsoTwist` を書き、
  `leadArm()` がそれを読んでいる。`SIDE_CLEAR`(0.25m) の境目あたりで同じ振動が出る。
  同様に root のヨーだけで決め、`leadRight` / `leadHoldT` で保持する。
- 実測: 左右の入れ替わり **0 回**（numberSide 両方）。

**2) 当たる瞬間に腕が伸びていなかった**
- `beginAction("steal", 溜め, 突き, 回復)` の **溜めが明けたフレーム(actFired)が
  ボールに当たる瞬間**。以前は「溜め＝引き / その後＝突き」にしていたので、当たる
  瞬間はまだ引いた形で、伸び切るのが 0.13 秒遅れていた。
- 引きも突きも**溜めの中**で終わらせる（`PUNCH.cockFrac` で配分）。溜めを 0.12→**0.24秒**へ。
- 実測: 当たるフレームで突く手が 0.69m 出ている（以前は 0.33m ＝ 引いた形）。

**3) 逆手が前に出ていた**
- 実測で溜め中に前へ 0.13m 出ていた。パンチの逆手は前に出さない。
- 形は「歩く腕振りと同じ肘を曲げたフォーム」（肘 1.00rad 固定、上腕を後ろへ振る）。
- ⚠️ 符号は掃引で決めた（`probe-backarm`、numberSide 両方で同値）。肘 1.0rad のとき
  上腕の振り **-0.8→手首 -0.12m(後ろ) / -0.4→+0.05 / 0.0→+0.22(前)**。負が後ろ。
  採用: 溜め -0.55 → 突き -1.00。実測の最前 -0.045m（静止時 -0.059m ≒ 前に出ていない）。

**4) 突く側の肩が前に出ていなかった（符号が numberSide で反転）**
- ⚠️ 胴のひねりに加える「肩を前に出す」成分は **numberSide を掛ける**。
  掛けないと片側のコートでだけ逆の肩が前に出る。実測（当たるフレームの肩の前後）:
  掛けない場合 numberSide=+1 で **-0.114m（逆側が前）** / -1 で +0.114m。
  掛ければ両方 **+0.114m**。的へ向けるツイスト(tw)はワールドのヨー差から出すのでそのまま。

**5) 上から振り下ろす軌道 → 水平へ**
- 引き手を胸の高さに置いていたので、そこから的へ下りる軌道になっていた（1.26→0.95m）。
- 引き手の高さを「**手が最後に届く高さ**」に合わせる。的が腕の届く範囲なら的の高さ、
  届かないなら腕を伸ばし切った時の高さ（`endY`）。届く的は IK で手先を的に乗せる。
- さらに肩が的より高いぶんは**膝を折って肩を下げる**（`digLoad` / `POSE_DIG`）。
- 実測: 手の高さの振れ幅 0.31m → **0.15m**。⚠️ 完全な水平にはならない。肩を中心と
  した円弧なので、体の横から前へ出す以上は必ず上がる。これ以上は沈み込み
  （確認ページの「沈み込み」スライダー）で詰める。

**踏み込みが潰れていた**
- ⚠️ 突きの溜めの間は通常の間合い取り(`defendOnBall`)を止める。止めないと溜めの
  0.24 秒で体が当たる距離まで詰めてしまい、踏み込む余地が消える
  （実測: 踏み込み 0.34m → 0.08m）。間合いを詰めるのは踏み込みの役目。
  実測: 踏み込み **0.157m**、ターンオーバー 1.8 → **2.3**／試合。

### 追記: スティールの頻度と賭け、体当たり、シュートフェイク

**1) 1on1 でスティールをほぼ使っていなかった**
- 実測: **1試合 1.0 回**。担当守備者が 1.4m 以内に居る時間の **0.9%** しか突いていない。
- 原因は仕掛けの確率が低すぎたこと。`STEAL.base` 0.03 → **0.17**、
  `STEAL.hands`（手の速さの効き）0.10 → **0.36**、`STEAL.minClose`（一歩離れていても
  仕掛ける下限）**0.35** を追加。→ 試合あたり **4.4 回**。
- ⚠️ 頻度は 0.26/0.52 まで上げると 7.4回/試合になるが、得点が 14.8 → 13.4 まで落ちた。
  上げすぎない。

**2) 賭けの代償（空振り＝隙）**
- 空振り時（実行フレームに間合いが足りない）:
  `reactT = 0.35 + (1-敏捷性)*0.55` ／ `setPlant(0.30 + (1-敏捷性)*0.60)` ／
  ハンドラーに `beatenT = 0.30 + 敏捷性*0.35`。体を前へ預けたぶん重心が戻るまで
  守備へ行けず、抜き去られた扱いになる。素早い選手ほど早く立て直す。

**3) シュート前の体当たり（ブロックさせない）— `clearOut`**
- ⚠️ ダンク/レイアップが簡単にブロックされる問題への直接の対策。`shoot` と
  `finishAtRim` の**両方の入口**で、1.35m 以内の守備者全員に判定する
  （ダブルチームなら複数人が崩れる）。
- ボディバランス(`attr.balance`)の差で押し勝つ。崩れた相手は `offBalT` の間
  **`jump()` が即 return する＝跳べない**。半歩押しのけ、`lean` と `setPlant` も入れる。
- 実測: 試合あたり **2.8 回**崩している。

**4) シュートフェイク — `tryShotFake`**
- S技術(`shotTech`)が高いほど仕掛ける。引っかかる確率は相手の守備・反応で下がる。
  引っかかった守備者は `contestLeap` で跳ぶ → 落ちてくる間にシュートへ入る。
- 実装: `decide` の**打つ判断を全部 `goShoot()` / `goFinish()` に通す**。仕掛けた
  フレームは打たずに持ち、`fakeT` が切れた次のフレームに `fakeGo` で本番へ。
  ブザーだけはフェイクしない（時間が無い）。
- ⚠️ `resetMotion` で `fakeT` / `fakeGo` を必ず落とす。持ち越すと次にボールを持った
  瞬間に即シュートする。
- 実測: 試合あたり **3.7 回**。

**全体の実測（12試合・各4分）**
| | 変更前 | 変更後 |
|---|---|---|
| スティールの仕掛け | 1.0 回 | **4.4 回** |
| ブロック | 3.83 本 | **2.33 本**（跳んだシュートの 22% → 16%） |
| 得点 | 14.8 | **17.4** |
| オフェンスリバウンド | 27% | 27% |
- シュート成功率（24試合）: 全体 58% / 3P 33% / ミドルジャンパー 45% / リム至近 70%。
  変更前（59/34/45/69）とほぼ同じ＝確率は崩さずに、得点とブロックだけ動いた。

### 追記: 反射の傾きは下半身だけ — 上半身と頭は水平に保つ

- 指摘: 方向転換・ストップ・切り返しの反射で**体全体が斜め**になっている。
  下半身と膝以下は斜めのまま、上半身はそのぶん逆へ回して頭・胴を地面に対して水平に。
- 傾きを2つに分けて持つようにした:
  - `tiltX/tiltZ` … 従来どおり root（＝全身）に当てる合計
  - `reflexTiltX/reflexTiltZ` … そのうち**反射ぶん**（加速・切り返し・急停止・突きの
    踏み込み）だけ。⚠️ 守備の重心移動(`lean`)は含めない — あれは体ごと傾けてよい。
- `applyUpright(vb, reflexTiltX, reflexTiltZ, sign)` を **Spine の親フレーム**で
  掛けて打ち消す（クリップあり／なしの両経路に差し込み、`blendPose`・`limitBoneRate`
  の前）。下半身（腰〜膝〜足）は root ごと傾いたまま、Spine から上だけ起き上がる。
- ⚠️ 符号はリグ依存（`vb.root` が numberSide で π 回っている）。推測せず
  `probe-upright` で実測して決めた。反射 11.5° を入れた時:
  | | 腰 | 膝下 | 胴 | 頭 |
  |---|---|---|---|---|
  | 補正なし(基準 0°) | 0.2° | 6.1° | 2.8° | 1.0° |
  | **sign +1（採用）** | 8.6° | 14.7° | **2.9°** | **1.2°** |
  | sign -1 | 8.6° | 14.1° | 19.5° | 18.3° |
  → +1 なら下半身だけが傾き、胴と頭は無傾斜時とほぼ同じまま。-1 は傾きが倍になる。
- ポーズのみの変更でシムには影響しない。`npx vite build` 通過。

### 追記: パスの飛距離とバウンドパスの跳ね位置

**1) パスが遠すぎた**
- 実測（12試合・602本）: 中央 6.2m / 90% 9.8m / 最長 19.2m。**10m超が 9%**、
  12m超が 3%。コートを横断する球が普通に出ていた。
- 原因は**距離のペナルティが全く無かった**こと。`chooseReceiver` の評価は
  「空いているか」と「リムに近いか」だけで、射程(`MAX_PASS`)内なら距離を問わず
  遠い相手が勝てた。
- `MAX_PASS` 13 → **11**、`LONG_PASS = { free: 8.0, cost: 1.2 }` を追加
  （8m を超えた分だけ 1m あたり評価を 1.2 下げる）。
- ⚠️ **効かせ方が重要**。短くしすぎるとボールが回らず得点が落ちる:
  | 設定 | 10m超 | 得点 |
  |---|---|---|
  | 制限なし（従来） | 9% | 15.3 |
  | MAX_PASS 9.5 / free 5.5 cost 0.9 | 3% | **13.1** |
  | MAX_PASS 11 / free 6.5 cost 0.55 | 7% | 13.8 |
  | **MAX_PASS 11 / free 8.0 cost 1.2（採用）** | **5%** | **16.3** |
  8m までは無罰、そこから急に重くするのが良い。普段の球回し(3〜8m)を邪魔せず、
  長い振りだけが消える。得点はむしろ上がった（近くの良い選択肢へ回るため）。
- ⚠️ 残る最長 17.5m は**スローイン**(`game.throwIn`)。ここは `passToReceiver` を
  通らないので `MAX_PASS` が効かない。1試合1本程度で、実際のバスケットでも
  ロングのスローインはあるので今回は触っていない。

**2) バウンドパスの跳ね位置**
- 跳ね位置 `kb` 0.58（前 58% / 後 42%）→ **0.75**（前 75% / 後 25%）。
  バウンド後は従来の **約6割**の距離になる（要望の「半〜3分の2」の範囲）。
- `passHeightAt` は判定と描画の単一ソースなので、当たり判定も同時に変わる。

**⚠️ 未対応（要判断）: ミドルと3Pの成功率が実在値から離れた**
- 60試合での実測: 全体 61% / 3P **23%** / ミドルジャンパー **61%** / リム至近 71%。
  スティール・体当たり・シュートフェイクを入れる前（72試合）は 59% / 34% / 45% / 69%。
- ミドルが 45% → 61%（実在 約40%）、3P が 34% → 23%（実在 約36%）。
- 原因の見立て（未検証）: シュートフェイク（3.7回/試合）で守備者が跳ぶと、その
  シュートはほぼ完全にフリーになる → ミドルが上がる。3P は溜め(0.42秒)にフェイクの
  0.30秒が乗るので、詰められて急ぎ撃ちになる割合が増えた可能性。
- 調整の当て先: `FAKE` の仕掛け確率／引っかかる確率（`tryShotFake`）、
  または `CLOSEOUT_AT`。ユーザーの要望（ブロッカーが居ても打てる）と直結するので、
  勝手に薄めず判断を仰ぐこと。

## 2026-09-12 選手のボクセルを一段大きくした（resolution 110 → 55）

**やったこと**
- `voxel-pipeline/configs/player_one.json` の `resolution` を **110 → 55**。
  `scale_factor` は据え置き（body=2 / jerseymark=4）。
  → body/髪/パンツ/靴のボクセルが **9.38mm → 18.75mm（ちょうど2倍）**、
  背番号などの jerseymark が 4.69mm → 9.38mm。共通グリッドは 18.75 → 37.5mm。
- 再生成は `uv run voxel-pipeline player_one --from voxelize --keep-work`（235秒）。
  ⚠️ `--keep-work` が無いと中間 blend を消され、prep の作り直しに3工程かかる。
  ⚠️ 実行前に `prep.blend` / `prep_hair.blend` / `nomod.blend` を
  `.bak_res110` としてバックアップ済み（`G:/voxel-pipeline-work/player_one/`）。
- 出力を `public/vox/player_one` へ差し替え。**18MB → 4.9MB**（441ファイルは同数）。

**ゲーム側で直したもの**
1. **顔の目・口が倍の大きさになる**（`MARK_GRID`）
   マスの実寸 = body のボクセル × `MARK_HALF`(0.5) なので、ボクセルを倍にすると
   目・口だけ倍になる。実測(`probe-facefit`)で **64マス中 38〜44マスが顔から外れ**、
   最大 98.9mm 浮いていた。マス数を半分へ: `{eyeW:6,eyeH:4,pupil:2,mouthW:8,mouthH:2}`
   → `{eyeW:3,eyeH:2,pupil:1,mouthW:4,mouthH:1}`。
   → 16マス中 14マスが肌に乗り、「隠れる恐れ」は 0 になった。
2. **設定身長より実寸が約1%高い**（`voxraw.ts` の身長測定）
   ⚠️ ボクセルの**中心**で上下端を測っていたので、描かれる立方体の両端半ボクセルぶん
   （合計1ボクセル）短く見積もり、そのぶん拡大率が大きくなっていた。ボクセルが粗いほど効く。
   実測: 193cm 設定が **195.7cm**（+1.4%）。立方体の上下端で測るよう修正 → **193.6cm**。
   全体で 設定比 0.9956〜1.0030（残りは 18.75mm 格子の量子化ぶん ±0.6cm）。
   ※ これは粗くして初めて見えた**元からのズレ**。細かいボクセルでは +0.9cm で目立たなかった。

**実測（すべて通過）**
- 146パーツ / 441ファイル、ボクセル数と weights が全パーツ一致（合計 134,371 ボクセル）
- 髪型 139種すべて読める（失敗0）。髭の 90/91 種があごの形に追従
- 26人が生モデルで組め、60フレーム動かして例外なし。背番号の板 26/26
- 服の透け 0 箇所 / 色は body・髪・ユニ・背番号・靴・靴下・顔すべて指定どおり
- `npx vite build` 通過

**⚠️ 未確認・残件**
- 目・口のマスが 16 中 8 で 5.2mm 浮いている。粗くする**前と同じ量**なので新規の劣化では
  ないが、実機で目立つなら `MARK_POS` の深さで詰める。
- 髪の持ち上げ量は prep 側に **8.7mm（旧ボクセル1個ぶん）**で焼き込まれている。
  新しいボクセル(18.75mm)の半分以下になったので、頭に少し沈んで見える可能性がある。
  直すなら `blender_scripts/add_hair_player_one.py` の持ち上げ量を変えて prep から作り直す。
- ブラウザ実機（`/` と `/confirm.html`）での見た目は未確認。

### 追記: あごの形（標準・丸顔・細あご）の差が出ていなかった

**⚠️ 本当の原因は `bodyMesh` が遠景用(LOD)メッシュを指していたこと**
- `buildPart` の末尾が `if (isBody) { bodyMesh = mesh; bodyCells = cells; }` で、
  **lod の判定が無かった**。遠景用メッシュ(lod>1)を作った時点で `bodyMesh` が
  そちらに差し替わり、`setJaw` は**遠景用のほうを作り直していた**。
  表示されるメッシュ(`byPart.get("body")`)は一度も変わらない。
- 実測（`probe-jaw2`。確認ページと同じ 見本→選手クローン の経路）:
  `setJaw` 直後に `bodyMesh` は 11,136 頂点になるのに、`byPart.get("body")` は
  12,576 頂点のまま。あごの形を3種切り替えても幅は 15.0/13.1cm で完全に同一だった。
- 直し: `if (isBody && lod === 1)` にする。
  実測（修正後）: `byPart.get("body")` が 12,576 / 11,136 / 11,120 と形ごとに変わり、
  **選手のクローンも見本と一致**するようになった。

**⚠️ 私の測り方の誤り（2つ）**
- はじめ `buildRawModel` を直接使う probe で測っていたが、**確認ページはその経路を
  使っていない**（`rawPrototype` → `buildRawVoxelBody`）。ページと同じ経路で測ること。
- あごの幅を「hair 以外の全メッシュ」で測っていたので、深さ 0.22m の位置が
  **シャツの襟＝肩幅(18.8cm)** になっていた。body だけで測ること。
- ⚠️ 計測の窓はボクセル間隔(18.75mm)の半分以上にする。6mm では頂点が1つも入らない
  高さが出て、測れていないのに「同一」に見える。

**`JAW_PROFILE` も強めた**
- LOD の不具合を直した状態で元の値を測ると、差が出るのは 0.22m 以深だけだった
  （頬〜あご中ほど 0.14〜0.20m は3種とも同一）。ボクセルが 18.75mm では変形量が
  1マスに届かず丸められる。
- 丸顔 … 幅は控えめ(0.34)・**奥行を強く落とし(0.55)**・いちばん短く(0.055m)＝短く丸いあご
- 細あご … **幅を強く落とし(0.85)**・奥行は控えめ(0.30)・短縮は最小(0.012m)＝Vライン
- 実測（修正後・強化後、幅/奥行 cm）:
  | 深さ | normal | round | narrow |
  |---|---|---|---|
  | 0.18m | 16.9/24.4 | 16.9/24.4 | **13.1**/24.4 |
  | 0.20m | 15.0/22.5 | 15.0/22.5 | **11.3**/22.5 |
  | 0.22m | 13.1/20.6 | **11.3/16.9** | **9.4**/20.6 |
  | 0.24m | 13.1/18.8 | **9.4/11.3** | **9.4/16.9** |
- ⚠️ 強くしすぎると丸顔と細あごが下端で同じ値に飽和する（幅 0.58/0.80 で両方 9.4cm）。
  幅と奥行で性格を分けること。

**既存の検証（変わらず通過）**
- 髭 90/91 種があごの形に追従 / 顔のマーク 16マス中14マスが肌に乗る（3形状とも）
- 26人が組めて 60 フレーム例外なし / 背番号 26/26

## 髪なしの髪残り / 目・口をボクセル格子に乗せ直す

**髪型No 0（髪なし）でも元モデルの髪が出ていた**
- 原因は `buildRawVoxelBody` の部位ループ。`hairstyle_*` だけ除いて、**元モデルの `hair`
  部位を常に生やしていた**。`hair` は髪型を載せる基準点を測るための部位で、描く物ではない。
- 実測: `hair` は 28.1×30.0×31.9cm（髪型001 は 30.0×37.5×33.8cm）で**髪型と同規模の本物の髪**。
  下端 z も 1.541 と髪型001 の 1.569 より低く、髪型を選んでも下からはみ出していた。
- `if (part === "hair") continue;` で描画から外した。
- 実測（probe-hair0）: 髪型No 0 → 髪のメッシュ 0個 / No 1 → 1個 / No 32 → 1個（二重なし）。

**目・口をボクセル格子に乗せて並べ直した**
- 目 3列×2段（黒目は上段中央1マス）を顔の中心列から左右対称に2つ、口 3列×1段。
  1マス = body のボクセル1個（18.75mm）。以前は半分（9.4mm）の升目だった。
- 列は**セル番号で折り返す**こと。x を ±eyeX にして丸めると、格子の原点が S の倍数でない
  ぶん左右で1列ずれる。実測: セル番号で折り返して 左右対称のずれ 0.0mm。
  ⚠️ 対称の軸は x=0 ではなく**顔そのものの中心**。このモデルの頭は x=+1.7mm が中心で、
  x=0 で測ると 3.4mm ずれて見える（最初これを不具合と誤認した）。
- 顔の凹凸に沿わせず、**枡目ごとに1つの深さ**へ揃えて列をまっすぐにした。
  実測: 各段の前面のばらつき 0.0mm。
- ⚠️ 平らに揃えると外側の列が宙に浮く。**奥行きだけ列ごとに伸ばして**肌へ届かせる
  （`Mark.dy`）。厚みは最低2列ぶん。1列だと、前の肌を削った列で 2.0mm の隙間が残った。

**目・口の前にある肌を削る（`carveMarks`）**
- 目を1列 奥へ置くと、鼻ぎわの列（実測 肌 113.5mm）が枡目より 16.8mm 前に来て**目が埋まる**。
  枡目と同じ列だけ、枡目より前にある肌を落とす。実測: 鼻ぎわの列の肌が 113.5mm → 94.7mm。
- 削るのは 1744 個中 4 個（鼻ぎわ 1列 × 2段 × 両目）。頂点数は 12,576 のまま
  （落とした前面と、その裏のボクセルに出る前面が1対1で入れ替わる）。
- ⚠️ `bodyCells` は**削る前**を持つこと。削った後を渡すと `buildMarks` が測る面が1列奥になり、
  貼り直すたびに目・口が奥へ沈む。
- ⚠️ 遠景用(lod>1)では目・口を描かないので削ってはいけない。窪みだけが残る。

**位置の指定（`MARK_POS`）**
- `eyeBack: 1`（目を1列 奥）/ `mouthBack: -1, mouthDown: 2`（口を1列 手前・2列 下）。
- 実測（頭頂の最上ボクセル中心から）: 目 上段 10.3cm・下段 12.2cm、口 21.6cm。
  肌より前に出る量は 2.0〜20.8mm、肌との隙間は全マス 負（＝浮きなし）。

**確認ページ**: 「目口を色分け」チェックで目・口のマスを水色／赤紫／緑にする
（`setMarkDebug`）。どのボクセルを指定しているか目で確かめる用。試合の色はチェック外し。

**⚠️ 肌の面を測るとき `Ray.intersectsMesh` は使わない**
- 一定して 20.5mm 奥を返した（ボクセルデータと頂点データはどちらも 113.5/94.7/76.0mm で
  一致するのに、光線は 92.7/74.3/56.0mm）。原因未特定。
- 頂点を x で拾う測り方も駄目。body メッシュは面をまとめてあるので隣の列（鼻）の角を拾い、
  1ボクセルぶん前にずれる。**前を向いた三角形に点が入るかを自分で判定する**のが確実
  （probe-marks の `skin()`）。

## 目・口を「パレット＋1/4サイズの絵」に作り替え

**考え方**
- 目 3列×2段・口 3列×1段の範囲を**パレット**（平らな下地）にし、その上に
  **縦横1/4サイズ（4.69mm）のボクセル**で目・口を描く（`EYE_ART` / `MOUTH_ART` の文字絵）。
- ⚠️ パレットは**本体(body)のボクセルそのもの**として作る（`fitPalette`）。別メッシュで
  肌色を塗ると `normalizeTint` の染め（選手ごとの肌色）に乗らず、顔に色違いの継ぎ当てが出る。
  パレットの色とウェイトは「その列の奥にある肌」から借りるので、周りの肌と完全に一致する。
- `fitPalette` は (1) パレットの面より前の肌を落とし (2) 面から奥の肌まで隙間なく
  ボクセルを足す。足すので、面が肌より手前に来る列（口の `mouthBack: -1`）でも浮かない。

**実測（probe-marks）**
- 絵のボクセル 114 個（白目72 / 黒目24 / 唇18）、1個 4.7×4.7mm = body の 1/4。
- 絵の各段の前面のばらつき **0.0mm**、肌から浮いた絵 **0個**、埋まった絵 **0個**、隙間 0.0mm。
- パレットの面（升目の中を 6×4 点＝24点で測る）: 右目 94.7mm・左目 94.7mm・口 113.5mm、
  いずれも**ばらつき 0.0mm**（＝完全に平ら）。
- 左右対称の軸 x=+1.7mm（顔そのものの中心）、ずれ **0.0mm**。
- 絵の位置: 目 頭頂から 10.08〜12.42cm、口 21.33〜21.80cm。
- 頂点数: raw_face 360 → 2,736（見本1つを26人で共有。遠景では顔メッシュごと消える）。
  body は 12,576 → 12,592（パレットぶん +16）。あご3形状の差は変わらず。

**確認用の色分け**
- チェックを入れるとパレットの範囲を**塗りつぶす**（目=水色192マス / 口=緑48マス）。
  ⚠️ パレット自体は肌色（body の一部）なので、色を変えて見せるのは絵の側でやる。

**絵の描き換え方**
- `EYE_ART`（12列×8段）と `MOUTH_ART`（12列×4段）の文字を書き換えるだけ。
  `#` 白目／唇、`@` 黒目、`.` 描かない。升目の数が合わないと例外で落ちる（黙って歪めない）。
- 左右の目は同じ絵を**列を反転**して貼る。左右非対称な絵にしても対称位置に出る。

## 目の絵を 黒目2×2 / 虹彩1マス / 白目は半円 に描き替え

- 黒目 2×2、その周り1マスが虹彩（黒目＋虹彩で 4×4）、白目は**半円**。
- 半円は直径12マス・高さ6マス（＝半径6マス）。段ごとの幅を `12*sqrt(1-(dy/6)^2)` から
  丸めて作った（平らな辺の側から 12,12,10,10,8,4）。
- 2パターン用意（`EYE_ART_UP` / `EYE_ART_DOWN`）。`setEyeShape("up"|"down")` で切り替え、
  確認ページの「白目の形」で選べる。上下を反転しただけなので黒目・虹彩の位置は同じ。
- 色: 白目 182灰 / 虹彩 [92,56,32] / 黒目 [2,2,2]。
- 実測（probe-marks、両パターン）: 絵 130個（白目80 / 虹彩24 / 黒目8 / 唇18）。
  段ごとの前面ばらつき 0.0mm、浮き0個・埋まり0個、パレットの面 ばらつき 0.0mm、
  左右対称のずれ 0.0mm。raw_face 2,736 → 3,120 頂点。
- 曲線が上=下の段ほど幅が広い（12.42cm の段が 24個）/ 曲線が下=上の段が広い
  （10.08cm の段が 24個）で、狙いどおり向きが反転している。

## 目を 8×4 に縮め、升目の中で位置をずらせるようにした

- 半円を直径8マス・高さ4マス（半径4マス）に変更。段ごとの幅 `8*sqrt(1-(dy/4)^2)` から
  平らな辺の側から 8,8,6,4。黒目2×2＋虹彩の 4×4 は変えていない。
- 12×8 の升目に 8×4 で描くので、**上下左右に2マスずつ余白**が残る。
- `setEyeOffset(x, y)` で位置をずらす（±2マス）。x は**外向きが正**、y は上が正。
  確認ページに「目の位置 外↔内 / 上↔下」を追加。
- ⚠️ ずらす量は**絵の升目の側**で引くこと（`sc = (flip ? cols-1-c : c) - off.x`）。
  世界座標の x で足すと左右の目が逆方向にずれる。升目側で引けば、反転して貼る反対の目も
  **中心線から同じ位置**に出る。実測: 位置 (0,0) (+2,+2) (-2,-2) (+2,-2) のどれでも
  左右対称のずれ **0.0mm**。
- 実測: 絵 70個（白目20 / 虹彩24 / 黒目8 / 唇18）。±2 までずらしても 70個のまま
  （欠けない）。位置 +2/+2 で目が 0.94cm 上・0.94cm 外へ動き、肌との関係は
  浮き0個・埋まり0個のまま。raw_face 3,120 → 1,680 頂点。
- ⚠️ いまの目の形・位置は**全員共通の設定**（確認ページで見るためのもの）。選手ごとに
  散らすには look に持たせる必要がある。まだやっていない。

## 顔の陰をならす / 目の形を3種に

**顔に焼かれた陰を肌色へ（`flattenFaceSkin`）**
- 元モデルは目のまわりの陰と鼻の下・口のまわりの色がボクセルに焼き込まれていて、描いた
  目・口より目立っていた。実測(probe-facecol): 頭の前を向いた面 692枚のうち **22枚**が
  明るさの中央値(128)の8割より暗い。目の高さ 12.2cm に5枚、鼻の下 14.1cm に7枚、
  口のまわり 19.7〜21.6cm に6枚。色は 92,60,52 や 140,76,68 など。
- 頭（頭頂から0.26m）のセルのうち、明るさが**中央値の0.88倍**未満のものを、
  暗いものを除いた顔の色の**チャンネルごとの中央値**へ置き換える（平均だと暗い色に引かれる）。
- 実測（後）: 最小の明るさ 69 → **113**、中央値の8割より暗い面 22枚 → **0枚**。
- ⚠️ パレットを作る**前**に掛けること。パレットは奥の肌から色を借りるので、後だと
  借りた先の暗い色が目のまわりに残る。
- ⚠️ 選手ごとの肌色は normalizeTint が**明るさ**として乗せるので、暗い色を残すと
  どの肌色の選手でもそこだけ黒ずむ。

**目の形 3種**
| 形 | 大きさ | 段ごとの幅 | 絵の数(片目) |
|---|---|---|---|
| 半円・曲線が上 | 8×5 | 先端から 4,6,8,8,8 | 34（白18/虹彩12/黒4） |
| 半円・曲線が下 | 8×5 | 上下を反転 | 34 |
| 長方形（四隅なし） | 8×4 | 6,8,8,6 | 28（白12/虹彩12/黒4） |
- ⚠️ 半円は**先端の1段を白目**にすること。高さ4マスにすると先端が虹彩で埋まり、
  短い辺の白目が消えて黒目の塊にしか見えない（前回それで指摘を受けた）。
- 位置ずらしの範囲は**絵の描いてある範囲から自動で決める**（`block` の中で抑える）。
  実測: 長方形は上下左右±2マス、半円は高さ5マスなので 上+2/下-1・左右±2。
  範囲外を渡しても欠けない（±3 を渡しても絵は 86個/74個のまま）。
- 実測: どの形・どの位置でも 左右対称のずれ **0.0mm**、浮き0個・埋まり0個、
  段ごとの前面ばらつき 0.0mm。raw_face は 1,680 → 2,064（半円 2,064 / 長方形 1,776）。

## 目6形 / 口6形 に増やした

**目（黒目1マス版を追加）**
| 形 | 大きさ | 段ごとの幅 | 片目の絵（白/虹彩/黒） |
|---|---|---|---|
| 半円・上（黒目2×2） | 8×5 | 4,6,8,8,8 | 18/12/4 |
| 半円・下（黒目2×2） | 8×5 | 反転 | 18/12/4 |
| 長方形（黒目2×2） | 8×4 | 6,8,8,6 | 12/12/4 |
| 半円・上（黒目1） | **7**×4 | 3,5,7,7 | 13/8/1 |
| 半円・下（黒目1） | **7**×4 | 反転 | 13/8/1 |
| 長方形（黒目1） | **7**×3 | 5,7,5 | 8/8/1 |
- ⚠️ 黒目1マスの形は**幅を奇数(7マス)**にすること。升目は12マス（偶数）なので、幅8マスの
  ままだと 3×3 の虹彩が半マスずれ、**片目の中で左右非対称**になる。
- 幅7マスだと絵は升目の中心から半マスずれるが、反対の目は列を反転して貼るので
  **顔の中心線から見れば左右対称**。実測: 6形すべてで左右対称のずれ **0.0mm**。

**口**
| 形 | 段ごとの幅 | 唇の数 |
|---|---|---|
| 太い横線（既定） | 10,8 | 18 |
| 細い横一文字 | 10 | 10 |
| 小さい口 | 6 | 6 |
| 口角が上がる | 両端2+2 / 8 | 12 |
| 口角が下がる | 8 / 両端2+2 | 12 |
| 開いた口 | 6,6 | 12 |
- ⚠️ 口は反転して貼らないので、絵を左右対称に書くこと。

- 実測: どの組み合わせでも 浮き0個・埋まり0個、段ごとの前面ばらつき 0.0mm、
  左右対称のずれ 0.0mm。位置を ±2 ずらしても欠けない。
- 確認ページに「目の形」「口の形」を追加（`setEyeShape` / `setMouthShape`）。

## 目に「内寄り」を追加（7形目）

- 内側（鼻側）の上に 白2列×2段、その下に 虹彩4列×2段、虹彩の横（外側）に 白6列×2段。
  全体で 幅10マス・高さ4マス、片目 24マス（白16 / 虹彩8）。
- ⚠️ **絵の左が内側（鼻側）**。絵の列0は、どちらの目でも顔の中心に近い側へ貼られる
  （反対の目は列を反転して貼るため）。実測: 上の 白2×2 は x ±21.7〜25.1mm＝中心寄りに出る。
- ⚠️ 黒目の指定が無いので黒目は置いていない（虹彩だけ）。
- 幅10マスなので左右にずらせる量は **±1マス**（他の形は±2〜3）。自動で抑えている。
- 実測: 左右対称のずれ 0.0mm、浮き0個・埋まり0個、段ごとの前面ばらつき 0.0mm。

## 目に「縦長」を追加（8形目）

- **縦6マス×横4マス**。内側（鼻側）の2列は 上2段が白・その下4段が虹彩、外側の2列は6段とも白。
  片目 24マス（白16 / 虹彩8）。
- `EYE_ART_INNER`（幅10×高さ4）と積み木は同じで縦横が逆。**どちらも残す**。
- 実測: 各段 片目4マス×6段、左右対称のずれ 0.0mm、浮き0個・埋まり0個、
  段ごとの前面ばらつき 0.0mm。ずらせる量は 左右±3マス・上下±1マス（自動で抑える）。

## 「内寄り」の虹彩を外へずらした4形（目は全12形に）

- 外枠（幅10マス・高さ4マス）と白の 2列×2段（内側の上）はそのまま、虹彩 4列×2段だけを
  外へ 1 / 2 / 4 / 6 マス動かす。動いたあとは白で埋めるので総数24マスは変わらない。
- ⚠️ 絵の左が内側なので、**右へずらすほど外を見ている**顔になる。
- 実測（左目の虹彩の x。大きいほど外側）:
  | 形 | 虹彩の x |
  |---|---|
  | 内寄り | 15.8〜34.5mm |
  | innerO1 | 20.4〜39.2mm（+1マス） |
  | innerO2 | 25.1〜43.9mm（+2マス） |
  | innerO4 | 34.5〜53.3mm（+4マス） |
  | innerO6 | 43.9〜62.6mm（+6マス＝外端） |
- ⚠️ マスの**数**だけ見ても違いが出ない（白と入れ替えるので総数が同じ）。probe-marks に
  「左目の色ごとの x」を出す行を足した。数が合っているだけで通したことにしないこと。
- 実測: 4形とも 左右対称のずれ 0.0mm、浮き0個・埋まり0個、前面ばらつき 0.0mm。

## 顔を選手ごとの持ち物にした（外観データベースへ追加）

**データ（`player-look.ts`）**
- `PlayerLook` に `face: FaceLook`（あご・目の形・目の位置x/y・口の形）を追加。
- `faceFromName(name)` が名前ハッシュから配る。⚠️ **髪・肌と同じビットを使わない**
  （同じビットだと髪型が同じ選手は顔も同じになる）。種を `name + "#face"` にしてある。
- 名前ごとの上書きは `FACE_OVERRIDE`（髪型の HAIR_NO_OVERRIDE と同じ使い方）。
- ⚠️ 名前が空（DB外のダミー）のときは既定の顔。ハッシュが同じ値に潰れて全員同じ顔になる。
- `resolveLook(idx, name)` に名前を渡す形にした。identity.ts の look 4つ組（4015人ぶん）は
  触っていない。顔は名前から決まる。
- 実測（ロスター26人）: あご normal12/narrow7/round7、目は12形のうち10形、位置6種、
  口は6形すべて、**組み合わせ 26/26 が全員別**。同じ名前なら常に同じ顔。

**作り分け（`voxraw.ts` の `RawModel.variant(face)`）**
- ⚠️ あごを変えると**本体メッシュの頂点数が変わる**ので、選手ごとに作ると重い。
  あごごとに1本だけ作って同じあごの選手で共有する（実測: 26人で**本体3種**）。
- 目・口は顔メッシュだけなので組み合わせごとに作る（実測: 26人で**顔26種**、
  1人 1,296〜1,920頂点、26人ぶん合計 **39,504頂点**）。
- 髭のある髪型もあごに追従するので `loadHair(name, jaw)` であごごとに作る。
- ⚠️ 遠景用の粗い本体は**あご共通**（1本を全員で共有）。12m 先では 1ボクセルが1画素に
  満たないので形の違いは出ない。
- ⚠️ `buildPart` は body を作ると `bodyMesh` / `bodyCells` を書き替える。作り分けのぶんで
  上書きすると `setJaw` が見本ではなく作り分けを作り直す（実測: narrow にしても見本の
  形が変わらなかった）。作り分けでは**前後で戻す**こと。
- ⚠️ 数え上げ（ボクセル数・三角形数）も作り分けのぶんを足さないよう前後で戻す。

**確認ページ**
- あご・目の形・目の位置・口の形は、**その選手の look を書き替えて**作り直す（試合と同じ経路）。
  選手を切り替えると、その選手の顔が選択肢に反映される。
- `setEyeShape` / `setEyeOffset` / `setMouthShape` は廃止（グローバル設定をやめた）。
  確認用の色分け `setMarkDebug` だけ残し、切り替えたら作り置きを作り直す（markEpoch）。

**確認**: 目・口 18通りすべてで 浮き0個・埋まり0個・左右対称のずれ 0.0mm、
あご3形が見本・選手とも一致、顔の暗い面0枚、26人1800フレーム例外なし、`vite build` 通過。

## センター付近で足踏みする不具合の原因

**症状の再現（probe-stuck）**
- 「1.5秒の正味移動が 0.4m 未満・その間の平均速度 1.0m/s 超」で数えると、8試合で **121回**。
  最長 4.13秒。うち**センター付近(|z|<2) のスローイン中が 39回**で最多＝報告どおり。
- 進行方向が前フレームと逆を向く割合 中央 **71%**（最大94%）＝1フレームおきに行ったり来たり。
- ⚠️ 押し合いではない（最寄りの選手との距離 中央 2.28m、6.6m離れて4.0m/sで走っている例もある）。
- ⚠️ ハーフラインのクランプ（`h.pos.z = 0.05*s`）は **0回**。見えない壁ではない。

**原因（probe-stuck2 で移動命令の呼び出し元を毎フレーム記録して特定）**
- 同じ選手が1フレームおきに正反対の目標へ動かされていた。実測の記録:
  `shared.ts:getBackOnDefense → (0.0, +6.0)` と `defense/index.ts:マーク → (0.0, -6.9)` が交代。
- `getBackOnDefense` が攻撃側を **`game.possession` から直に**見ていたのが原因。
  呼び出し元の `runDefenseDuringDeadish(game, dt, offTeam)` は引数の `offTeam` で計算するので、
  両者が食い違うと「自陣へ戻る」向きだけが反転する。
- 食い違うのは**交代(ballMode "subs")の間**。`withSubs(..., nextOffense)` は
  「possession はまだ切り替わっていないので呼び出し側が渡す」作りで、ゴール後は
  `shooting.ts` が `1 - shooter`（次に攻める側）を渡す。つまり
  `game.subOffense`(新) ≠ `game.possession`(旧)。
- 直し: `getBackOnDefense(game, dt, d, man, offTeam = game.possession)` にして、
  `runDefenseDuringDeadish` が自分の `offTeam` を渡す。ライブ中は既定値＝従来と同じ。

**効果（同じ種・8試合）**: 121回 → **54回**。スローイン/交代中の分（39回）は**全て消えた**。
ブロック・得点の統計は変わらず（12試合: ブロック 3.58本/試合、得点 13.3）。

**まだ残っている分（54回）**
- 生きたプレー中(`held`)のセンター付近 27回。守備者が `getBackOnDefense` と
  オンボール/オフボールの守備位置の間で同じように揺れている例が混ざる。
- ⚠️ 試した仮説と**外れた**もの（当てずっぽうを残さないため記録）:
  ・`upCourt` の境目(0.5)にヒステリシスを入れる → 24回→26回（効果なし）
  ・`stranded` の「担当から5m」が境目 → 張り付いた時の担当との距離は 0.6〜3.7m で、
    5.0m±0.6 は **0/21件**（外れ）
- 調査用の記録口 `MOVE_TRACE` / `MOVE_LOG` を `src/util.ts` に残してある（既定 off）。

## CPU 同士でもポーカーを見せる（観戦モード）

**やったこと**
- これまで `POKER_OPTS.userTeam === null`（CPU 対 CPU）は「画面を出さないので即座に解決」で、
  交換も役も裏で終わっていた。観戦でも**画面に出して順番に見せる**ようにした。
- 進行（`runWatchRound`）: **アウェイが交換 → ホームが交換 → ホームが確定を判断**
  → 確定なら両チームの手札を表にして役を出す / 持ち越しなら次クォーターへ。
- ⚠️ 手番はホームを**後**にした。確定の決定権を持つ側が相手の動きを見てから決められる。
- ⚠️ 手札は伏せたまま進み、ホームが確定した瞬間に**両チームぶん**が表になる
  （公開のタイミングがホームの権利そのもの）。実測: 確定前に公開された回数 **0/200試合**。
- 画面は 上からアウェイ（伏せ札＋控え）→ コート盤（両チームの先発と置かれた札）→
  ホーム（伏せ札＋控え）。見出しに「ラウンド n/4」と、いま誰の手番かを出す。
- 待ち時間は `POKER_OPTS.revealMs`(3000) / `carryMs`(1400)。ボタンで飛ばせる。

**実測（probe-pokerwatch / 200試合）**
- 交換回数 team0 617 / team1 596 ＝ 両陣営とも打っている。
- 確定ラウンド R1 7 / R2 40 / R3 78 / R4 75 ＝ ホームは良い手が来るまで持ち越す。
- 強化が両チームに乗った 199/200（アウェイだけ 1、どちらも無し 0）。
- 役はチーム13人全員に乗る（実例: 役による強化 65件 / 受けた選手 13人）。
- ホームの役のほうが強く出る（team0 ツーペア66・スリーカード57 / team1 ワンペア67）。
  `cpuWantsConfirm` がホームの手だけを見て確定するので、これは**ホームの権利どおり**。

**⚠️ 直した細かい罠**
- 伏せ札の DOM に `data-oppteam` を足した。観戦では両チームの伏せ札が並ぶので、
  印が無いと「札が飛ぶ」演出の始点に相手の札を掴んでしまう。
- 補充アニメは `pokerFreshTeam` で打った側だけに限定（両方動くと誰が引いたか分からない）。

## 強化が見て分かるようにした（顔アイコンの光 / 役の札）

**顔アイコン（試合中の HUD とポーカー画面）**
- ポーカーで能力が動いた選手は顔の縁を光らせる。強化=金(255,206,92)・妨害=赤(255,110,110)、
  顔の左下に増減量のバッジ。HUD 側はゆっくり明滅させる。
- ⚠️ 光らせるのは**個人に置かれた札（捨て札）ぶんだけ**。役の効果はチーム13人**全員**に
  乗るので、合計で光らせると全員光って目印にならない（実測: 26人中 **23.0人**が光った）。
  `bonusOf(team, idx, "discard")` で分けた。実測: 強化0.8人 / 妨害3.7人 / 変化なし21.4人。
- ⚠️ アイコンの作り直しキーに増減量を足した。入れないとクォーター間で強化が乗っても
  アイコンが作り直されず光らない。

**役の札（コート/ベンチの横）**
- 各チームの「ON COURT / BENCH」タブの横に、確定した役と中身（例「ツーペア / オフェンス系 +3」）
  を出す（`updateHandPills`）。team0 はタブの右、team1 は左＝どちらもコート中央側。
- ⚠️ 出すのは**確定後だけ**。未確定の手を覗けるようにすると、ホームが持つ
  「公開のタイミングを決める権利」が意味を失う。

**気づいたこと（未対応）**
- CPU の捨て札は**妨害に偏る**（強化0.8人 : 妨害3.7人 ≒ 1:4.6）。`pickTarget` が
  「自軍の伸び」と「相手の削り」を同じ物差しで比べた結果、削りのほうが大きく出ている。
  仕様として意図どおりかは未確認。

## 注目（どこを見ているか）システムと、パスコースの争い

**注目システム（`src/ai/attention.ts`）**
- 毎フレーム、全員の注目先を1か所で決めて `gazeX/gazeZ/gazeKind` に書く（`updateAttention`）。
  ・保持者=攻めるリム ・オフボール攻撃=ボール（潰されている間はマーカー）
  ・オンボール守備=相手（＝ボール） ・オフボール守備=**ボール or 自分のマーク**
    （レーンを塞いでいる間はボールを見てパスを狙う＝そのぶんバックドアに弱い）
- 顔はこの注目先を追う（`visuals.ts` の `lookToward` を aim から gaze へ）。
  ⚠️ 胸は aim（プレーの方向）、頭は gaze。ここを aim に戻すとシステムが見えなくなる。
- `attnFactor(p, x, y)` = その方向をどれだけ見ているか（正面1.0 / 真横0.6 / 背中0.35）。
  基準は**頭の向き**（root + torsoTwist + headYaw）。体の向きではない。
- 効かせた先（3か所）:
  ・走り込みのカット `closingBest` … 出し手を見ていないと成功率 0.45〜1.0 倍
  ・レーンに居るカット `interceptChance` … 同上
  ・こぼれ球への反応 `looseReactT` … 見ていない方向は遅れる（`LOOSE_BLIND`）
- 実測（8試合）: 顔の向きの内訳は man 41.4% / ball 40.5% / rim 13.9% / handler 4.3%。
  **オフボール守備の61.3%はボールを見ていない**＝マークを見ている。

**⚠️ 効きめの調整（`LOOSE_BLIND`）**
- こぼれ球の反応遅れが一番効く。実測(8試合・合計得点): **0 → 16.2 / 0.5 → 14.0 / 1.25 → 13.4**。
  上げすぎると 50-50 の球を見ていない側が取り切れず、試合が止まる。既定は 0.5。
- カット2か所だけなら得点 16.2（＝ほぼ影響なし）。落ちるぶんはほぼ全部こぼれ球の遅れ。
- `ATTN.power`（0..1）で鈍りだけ切れる。顔の向きは常に動く。

**パスコースの争い（システム1）**
- 守備: 既存の deny（マークとボールの間へ割り込む）に `denyT` を立て、注目システムが
  「レーンを塞いでいる＝ボールを見る」を選ぶようにした。
- 攻撃: `frontedBy()` で**レーンに入られているか**を判定（近さではなく、ボール方向の成分と
  レーンからの横ずれ<0.95m で見る）。潰されていたら:
  ・クイック系 → **バックドア**（ボールと逆＝守備の背中側へ抜ける）。
    守備が自分を見ていないほど成功率が上がる（`1 - attnTo(d, p) * 0.55`）。
  ・パワー系 → 押し返してレーンへ入り直す（接触で勝つ）。
- 実測（8試合）: レーンを塞ぐ守備 0.13人/フレーム、潰されている攻撃 0.17人/フレーム、
  **バックドア 8.1回/試合**（注目の鈍りを切ると 5.9回/試合＝見られていないほど決まる）。

**⚠️ やってみて外した手**
- バックドア時に `p.cutting = true` を立てた → 既存のカット状態と競合する疑いで測ったが、
  得点は 13.4 → 13.3 で**変化なし**。競合ではなかったが、他の状態機械へ手を入れる必要も
  無かったので外した。

## 攻撃が型に入るのが遅い / 3P内でマンマークしていない

**測った（probe-setup / 6試合 111ポゼッション）**
- 型に入るまで（オフボール4人が自分のスポット2m以内）: **111回中11回**しか成立せず、
  成立しても中央 **10.2秒**。ポゼッションの長さは中央 **12.3秒**。
- ⚠️ ただし「自分のスポット」から遠いだけで、**どこかのスポットには着いていた**
  （8秒時点: 自分のスポットまで 5.09m だが、一番近いスポットまで 1.84m）。
  割り当てが入れ替わるだけで、隊形自体は組めている。数字の読み違いに注意。
- 攻撃の立ち位置: リムから 7.35m 超（＝3Pラインの外）に **30.2%**、自陣側に 25.2%。
- 守備と担当マンの距離: ペイント 1.51m / ミッド 1.70m / **アーク 2.87m（47%が3m超）**。

**原因1: 立ち位置がラインの外すぎた**
- ペリメーターのスポットはリムから **7.4〜8.1m**＝3Pライン(6.75m)の 0.65〜1.37m 外側。
  「アークに近づかない」絵の正体はこれ。3つを **7.0m / 7.0m / 6.95m** へ寄せた。
- ⚠️ コーナーは x=±7.3 だったが、コートのクランプは ±7.0。**届かないスポット**だったので、
  サイドラインを押し続けていた（= 前回測った張り付きの正体）。±6.85 に収めた。
  実測: サイドライン張り付き **87回/のべ177.7秒/最長12.5秒 → 27回/22.0秒/最長3.25秒**。

**原因2: マーク距離を決めていたのは `pickup` ではなく `sag`**
- 最初 `pickup`（mRim < 4.5 → 3Pライン+0.4）を広げたが、アークの距離は 2.87→2.84m で
  **ほぼ変わらなかった**（外した仮説）。実際に効いていたのは sag の式。
- 3Pラインの内側に立っている相手には `sag` を **ボール側1.15m / ウィークサイド1.9m** で
  頭打ちにした。ウィークサイドを詰めないのはリムを空けないため。
- 実測: ペイント 1.51→**1.42m** / ミッド 1.70→**1.37m** / アーク 2.87→**2.04m**
  （3m超 47%→35%）。アークの外は 4.05→4.48m で変えていない（＝ヘルプは維持）。

**外した仮説（記録）**
- 「戻りが遅いのは足が遅いから」→ 遠いときに走らせる係数(1.05〜1.25)を入れて測ったが、
  8秒時点の到達は 1.84m→2.04m で**改善せず**、速度も上がらなかった。戻した。
  移動の律速は速度ではなく**距離（開始時 13m）とポゼッションの短さ（13秒）**。

**残り**: 到着はまだ 8 秒前後。開始時点で 13m 離れているのが効いており、ここを縮めるには
トランジションの設計（誰が先に走るか）を変える必要がある。未着手。

## 攻撃の基準線は3Pライン（ハーフウェイではない）

**ユーザーの指摘**: 3Pラインは「3Pを打つ線」ではなく**オフェンスの基準線**。それより遠くに
立っても攻撃の役に立たない。ハーフウェイを基準にすると配置が崩れる。

**測った（probe-setup / フロントコート確立後・オフボール4人）**
- ⚠️ 「遠い」の大半は**走って戻っている最中**だった（8.5m超のとき 速さ中央 5.0m/s）。
  立ち止まっている選手だけを見ること。混ぜると原因を取り違える。
- 立ち止まっている選手のうち リムから 8.5m超 **11.2%** / 10m超 **8.8%**。

**リムから遠すぎる立ち位置を3か所直した**
1. フォーメーションのスポット: ペリメーター4人が 7.4〜8.1m → **7.0 / 7.0 / 6.95m**。
   ⚠️ コーナーは x=±7.3 でコートのクランプ(±7.0)の外＝**到達できない座標**だった。
2. 釣り出し（`isoSpreadTarget`）のディープウィング: **9.2m / 9.7m** → 7.1m。
   そこに立っても守備はサグしてヘルプに回るだけで、釣り出しにならない。
3. ピック&ポップ（`screen.ts`）: px が広いと 9.7m になっていた → `arcCap` で 7.35m まで。
4. **スペーシングの押し出しに上限が無かった**（`spacingNudge` / `ballSpacingNudge`）。
   ボール・味方から離れ続けてアークの 2〜3m 外で釣り合っていた。`keepInArc` で
   3Pライン+0.8m（ディープシューターは +1.6m）に収める。運び上げ中は掛けない。
- 共通の枠は `arcCap(game, team, x, z, max)`（`ai/offense/reads.ts`）。
  ⚠️ 基準はリムからの距離。ハーフウェイ基準にすると攻めるゴールが遠い側だけ外へ流れる。

**実測（前 → 後）**
| | 前 | 後 |
|---|---|---|
| 立ち止まって 8.5m超 | 11.2% | **6.2%** |
| 立ち止まって 10m超 | 8.8% | **5.2%** |
| アーク帯(4.8〜7.35m)に居る割合 | 26.8% | **33.8%** |
| アークの外(7.35m超) | 24.2% | **19.2%** |
| 守備との距離・ペイント | 1.51m | **1.36m** |
| 守備との距離・ミッド | 1.70m | **1.58m** |
| 守備との距離・アーク | 2.87m | **2.02m**（3m超 47%→35%） |
| レーンを塞ぐ守備 | 0.13人/F | **0.22人/F** |
| バックドア | 8.1回/試合 | **9.5回/試合** |
| 得点（8試合） | 13.4 | **16.3** |

**残り**: ペイント内に居る割合は 6.5% のまま（フォーメーションが「中は1人だけ」の設計）。
ゴール下にもっと人を置くなら `formationSpots` の中の2枠を変える必要がある。未着手。

## ⚠️ ワープ禁止 / 区域制限禁止（指示違反の是正）

**やってしまったこと**: スペーシングの行き過ぎを抑えるのに `keepInArc` で
`p.pos.x/z` を直接書き戻した。これは (1) ワープ (2) フィールドの制限でポジションを
取らせる の二重の違反。**同じ指示は以前にも受けていた**。中央メモリに
[feedback_no_teleport_no_field_limits.md] として残した。

**直したこと**
- `keepInArc`（位置の書き戻し）を削除し、`noOutward()` に置き換えた。押し出しベクトルから
  「アークの外へ出る成分」だけを抜き、横（接線）へ逃がす。**移動はすべて
  `moveToward2D(pos, ..., accelSpeed(dt) * dt)` の刻み**＝走る速さと能力で決まる。
- 位置を直接書く実装が他にも残っていたので測って潰した（`probe-warp.ts` を追加）。
  1フレームの移動が「走る速さ×dt×1.6」を超えた回数を全選手・全フレームで数える。

**見つけて直したワープ（自分の変更以外）**
| 出どころ | 症状 | 直し方 |
|---|---|---|
| スローインの投げ手 | 投げた瞬間 **80cm** 引き戻される（z 14.30→13.50 / x 7.8→7.0） | `oobGraceT` の間は `clampCourt` を素通し。投げたら 1.2 秒で自分の足で入る |
| 捕球の詰め | 1フレームで最大 **40cm** 詰めていた（実測 55〜65cm 飛ぶ） | 詰められるのは `accelSpeed(dt)*dt` まで。届かない球は腕を伸ばして捕る |

**実測（4試合・ライブ中 356,510 サンプル）**
- 走れる以上に飛んだ回数 **206 → 127**（0.058% → 0.036%）。
- そのうち明らかな瞬間移動（上限の3倍以上）は **14回 → 4回**、最大 80.0cm → 74.6cm。
- 残り 4回はデッドボール中にライン外へ置かれた選手が、ライブ復帰時にクランプで戻される分。
  **未特定・未修正**。

**⚠️ 変更のたびに `probe-warp` を回すこと。**「上限の3倍以上」が増えていたら、その変更が
位置を直接書いている。

## 守備へ戻るときに中腰でジリジリ戻る

**原因は速度ではなく**向き**だった**
- `pickClip` は「胸の向きに対する速度の成分」で 前/後ろ/横 を決める。自陣へ戻る間も
  `visuals.ts` が胸をボール（＝後方）へツイストさせ続けるので、全力で走っていても
  `sidestepDash` / `backwalk` が選ばれ、中腰の守備モーションで戻る絵になっていた。
- 実測（戻っている間のモーション）: **横・後ろ歩きが 59.8%**（sidestepDash 38.0 /
  backwalk 10.6 / sidestep 7.9 / backdash 3.3）、run はわずか 18.0%。
- ⚠️ 速さはもともと出ていた（走る速さの中央 **89%**、全力の割合 56%）。
  「遅い」の正体は**モーションの選ばれ方**。速度を上げても直らない。

**直し**: 自陣へ全力で戻っている間（`getBackOnDefense` が動かしている間）は `transitT` を立て、
`visuals.ts` で**胸も走る向き**へ向ける。頭は注目システムのままなので、首だけボールを見ながら
全力疾走する形になる。

**実測（前 → 後）**
| | 前 | 後 |
|---|---|---|
| run | 18.0% | **57.5%** |
| 横・後ろ歩き（中腰） | **59.8%** | **12.3%** |
| 戻っている間の速さ | 走速の89% | 85%（変わらず＝もともと速かった） |
| 自陣へ戻るまで | 1.63秒 | 1.67秒（変わらず） |

⚠️ クリップ名は見た目の sync でしか決まらず 26人ぶんは重い。probe では `pickClip` と
**同じ式**を再現して測っている（`probe-transition.ts`）。

## ハンドラーが止まると周りも止まる

**測った（probe-idle / 3試合・ボール保持中）**
- 他の9人の停止率: ハンドラーが動いているとき 24.8% → **止まっているとき 45.5%**。
- ⚠️ 「やることがある」を自分で定義すると取り違えるので、**AI が実際に出した移動命令**
  （`MOVE_LOG`）で見た。止まっている選手のうち
  ・**移動命令そのものが出ていない 237件（全部が攻撃側／守備は0件）**
  ・命令はあるが目標＝今いる場所 232件
- 注目別: ハンドラーを見ている選手の停止率 56.6% / 見ていない選手 35.8%。

**直したこと（3つとも「判断」の側。位置は触っていない）**
1. ハンドラーが 0.8秒以上止まったら、**その様子が見えている**味方だけ次の行動を早める
   （`offTimer` の進みを `attnTo` 倍率で加速）。見ていない選手は気づかないので変わらない。
2. 膠着中に同じスポットを選び直して立ち尽くすのを防ぐ（`bestOpenSpot(..., avoid)` で
   今のスポットを除外し、別の空きへ動き直す）。
3. 膠着中はカッターを **2人まで**許す（従来は1人。残り3人が立ち尽くしていた）。

**実測（前 → 後）**
| | 前 | 後 |
|---|---|---|
| 移動命令が出ていない | 237件 | **60件** |
| 命令はあるが目標＝現在地 | 232件 | **139件** |
| 他の9人の停止率（ハンドラー停止中） | 45.5% | 46.3%（横ばい） |
| 　└ 守備 / 攻撃 の内訳 | — | 守備 36.4% / **攻撃 58.7%** |
| 得点（8試合） | 16.3 | 15.4 |
| バックドア | 9.5回/試合 | 8.8回/試合 |

**⚠️ 残っている（未対応）**: 攻撃側の停止率 58.7%。これは「スポットに着いたら保持する」
という設計そのもの。動き続けさせるなら、スポット到着後も細かく動き直す（パスコースを
開け続ける）挙動を足す必要があり、スペーシングとのバランスに影響する。判断を仰ぎたい。

## オフボールのポジション争い（接触・切り返し）

**まず見つけた不具合: 「潰されている」判定が動いていなかった**
- `frontedBy`（自分とボールの間に守備が入っているか）を `tryShake` の中で見ていたが、
  tryShake は試行のクールダウン(1.4〜2.8秒)で早期 return する。**潰されていても印が
  立たないフレームが大半**だった（実測: 潰された状態の継続が中央 0.42秒しかない）。
- `markFronted()` として毎フレーム更新に分離。実測: 潰された回数 60.0 → **110.5回/試合**、
  継続 0.42 → 0.53秒。注目システム（潰されている間はマーカーを見る）もこれで初めて効く。

**1. オフボール同士の接触（`BODY_PUSH` / collision.ts）**
- ボディバランス差が大きいほど、弱い方が押し出され、次の動きに入るまでの時間も長い。
- ⚠️ 実分布の差は小さい（接触時の |差| は中央 0.06・最大 0.26）。そのままでは何も起きない
  ので `gain: 3.2` で拡げてから使う。
- ⚠️ 1フレームの押し出しは **0.018m まで**。0.05m にしたら1フレームの移動が走れる量を超え、
  ワープ判定（上限の3倍超）が 2 → 8件に増えた。押し合いは「接触が続く間ずっと押される」
  で表現する。
- ⚠️ **訂正**: 一度「動けない時間が効きすぎて得点が落ちた(15.4→13.4)」と書いたが誤り。
  8試合1本ずつの差を原因と決めつけていた。スタンは攻守どちらにも同じだけ起きる
  （実測: 攻撃が食らった 49% / 守備 51%）。16試合×2種で測り直すと
  **種1: 弱16.2 / 強15.0、種2: 弱15.4 / 強16.3 と大小が逆転**＝ただのばらつき。
  指示（弱い方が動けない時間が長い）に合う 0.30/0.35 を採用する。
- 実測: 押し出し **169.8回/試合**、動けない時間 中央 0.04秒。

**2. 速い選手は曲げて外す（`shakeCurve`）**
- クイックネス（敏捷0.6 + 速さ0.4）で、外しの軌道を毎フレーム回す。
  鋭い切り返し（守備の逆へ折り返す）か、緩い円を描く動きを選ぶ。速いほど鋭い方を選びやすい。
- パワーのシールとバックドアは直線のまま（体を入れる／最短で裏へ抜けるのが目的）。
- 実測: 外し成功のうち **曲げて外したのが 27 / まっすぐ 99**。

**実測（前 → 後）**
| | 前 | 後 |
|---|---|---|
| 潰されている攻撃 | 0.18人/フレーム | **0.35人/フレーム**（見えていなかっただけ） |
| マーク外しの試行 | 62.8回/試合 | 69.5回/試合（成功率 47% → 50%） |
| バックドア | 8.8回/試合 | **11.3回/試合** |
| 押し出し | なし | 169.8回/試合 |
| 得点（8試合） | 15.4 | 15.8 |
| ワープ（上限3倍超） | 2件 | 1件 |

## マンマークが遠い / 外しが効かない（原因2つ）

**原因1: 自分で入れたクランプの境目と、攻撃の立ち位置がぶつかっていた**
- 守備を詰めるクランプは `mRim < THREE_DIST + 0.2`（=6.95m）だったが、攻撃のスポットは
  リムから **6.95〜7.0m**。立ち位置がちょうど枠の外に落ち、そこだけ緩んでいた。
- 実測の段差: 6.4〜6.95m 帯 中央 2.35m ↔ **6.95〜7.5m 帯 中央 2.86m（46%が3m超）**。
- `THREE_DIST + 1.2`（=7.95m）まで広げ、値も 1.15/1.9 → **1.05/1.7** に。
  実測: 6.95〜7.5m 帯 2.86 → **1.74m**（3m超 46% → 28%）。全帯で中央 1.5〜1.7m に揃った。
- ⚠️ 教訓: 判定の境目を決めたら、**その境目に乗る位置が実際にあるか**を確かめる。

**原因2: 外しが「サイコロだけ」で、距離が開いていなかった**
- 実測: 外し成功の 0.5秒後、クイックは距離が **-0.19m**（＝逆に詰められている）。
  1m以上離せたのは 17%。守備が 1.78m/s で動くのに攻撃は 1.45m/s しか出ていなかった。
- 要因を分解（推測で直さない）: 向き変えの罰 1.00 / 体の傾き 1.00 / **プラント中 33%**
  / クール 0% / 着地 1%。つまり**切り返しの再加速の罰**が効いていた。
- 直した4点:
  1. 自分から仕掛けるカットではプラントを解く（不意の方向転換の罰なので）。
  2. 振り切る側の burst 1.15 → **1.32**（守備は deny 中 1.25 で動く。同等以下では抜けない）。
  3. 振り切られた守備は追走が鈍る（`eff *= 1 - min(0.55, shakeOpenT*0.45)`）。
  4. **守備は「見えている位置」を守る**（`trackX/trackZ`）。実位置へ遅れて追従し、
     見ていない相手ほど遅れる（注目システム）。⚠️ 更新は `attention.ts` で**全守備者・
     毎フレーム**。守備の分岐の中だけで更新すると見え位置が古くなり、戻った瞬間に
     目標が飛ぶ（実測: 遅れの90%が 4.74m まで伸びた）。
  5. 外している時間 0.6〜1.0 → **1.1〜1.7秒**（短いと加速し切る前に終わる）。
- ⚠️ 計測窓が短すぎると効果が見えない。外しが 1.1〜1.7秒なのに 0.5秒で測っていた。
  **1.0秒で測り直し**: クイック **+0.28m** / パワー **+0.61m** / 1m以上離せた **36%**。

**実測（前 → 後）**
| | 前 | 後 |
|---|---|---|
| 外している最中の速さ（攻/守） | 1.45 / 1.78 m/s | **1.92 / 1.71 m/s** |
| 外した後の距離（1.0秒後・クイック） | （0.5秒で -0.19m） | **+0.28m** |
| 1m以上離せた割合 | 17%（0.5秒） | **36%**（1.0秒） |
| 6.95〜7.5m 帯のマーク距離 | 2.86m | **1.74m** |
| 得点（8試合） | 15.8 | 14.1 |
| ワープ（上限3倍超） | 1件 | 3件（いずれもライン際） |

## フリーランニングと、押して位置を取る勝負の使い分け

**選び方（ユーザー指定の考え方）**
- 場所が入れ替わってよい（アークで受けて撃つ／そこから突く）→ **スピードで空きへ走る**
- その場所でないと意味が無い（ゴール下の位置取り、リバウンド前）→ **押して無力化する**
- `spotMatters = p.spotIdx >= 5 || リムから4.5m以内` で分け、能力はその次に効かせる。

**フリーランニング（`freePhase`）**
- ⚠️ 持ち場で**止まっていた**のが最大の原因だった。止まった状態から仕掛けると、外しの
  1.2秒では加速し切れず守備に置いていかれる（実測: 外し中 攻 1.92 / 守 1.71 m/s なのに
  1秒後の距離が ±0m）。スポットの周りをゆっくり回り続けて足を作っておく。
  半径・速さは 敏捷/速さ/スタミナで決まり、選手ごとに逆回り。
- クイックの狙いも「守備から離れる」→ **空いている所へ走る**に変更（8方向を試して、
  守備から遠く・パスコースが通り・アークの内側に残る方向を選ぶ）。

**注目 → マークの追従**
- ボールの方を見ている守備は、マークの動き出しに気づくのが遅れる
  （`see = (0.12 + attn*0.88) * (視線がマークなら 1 / それ以外 0.72)`）。

**押し合いはリバウンド・シュート前にも同じ式で効いている**（別実装ではない）
- 実測の内訳: held 734 / pass 103 / loose 81 / **リバウンド 79** / **シュート前 67**。
- ⚠️ デッドボール中（交代・一時停止・スローイン）は押さないよう `live` で絞った
  （実測で交代中に 160 件の押し出しが起きていた＝歩いているだけの場面）。

**実測（この回の前 → 後）**
| | 前 | 後 |
|---|---|---|
| オフボール攻撃がフリー（守備2.5m超） | 52.4% | **54.8%** |
| 　└ パスコースも通っている | 30.6% | **33.6%** |
| 外し試行 | 69.5回/試合 | **98回/試合** |
| 外した1.0秒後の距離（クイック） | 0.00m | **+0.23m** |
| 　　同（パワー） | +0.61m | **+1.18m** |
| 1m以上離せた割合 | 36% | **40%** |
| バックドア | 8.9回/試合 | **13.9回/試合** |
| ハンドラー停止中の他の9人の停止率 | 46.3% | **41.8%** |
| 得点（8試合） | 14.1 | 15.7 |

## マンマークの接触 / センターライン付近の滞留

**⚠️ 計測の誤り（先に訂正）**
- 「体が当たっている」を **0.62m 未満**で数えていたが、分離解決(`BODY_MIN_DIST`=0.62m)が
  毎フレーム押し離すので**その値未満には決してならない**。0.2% という数字は当然だった。
- 正しくは **0.62〜0.85m の帯**が「体が当たっている」。定義を直して測ると 19.3%。

**マークを接触の間合いまで詰めた**
- ボール側の `sag` 上限 1.05 → **0.72m**、deny のレーン割り込み 0.5〜1.3 → **0.35〜1.1m**。
- 実測: 体が当たっている **19.3%** / 1.3m 未満 10.9%（合わせて約30%が至近）。
  以前の測り方だと 1.2m 未満 23.4% だったので、詰まっている。

**「攻めているのに守備へ戻って見える」の一因**
- 速攻のレーン埋め（`game.pushT > 0` の分岐）の速さが **0.55 + 攻撃性×0.8 倍**で、
  攻撃性の低い選手は **0.55倍速**でセンター付近をダラダラ上がっていた
  （実測: センター付近で止まっている選手のうち 75件がこの分岐）。
  下限を **0.85 + 攻撃性×0.5** に上げ、フロントコート確立後にハンドラーが止まったら
  レーン埋めを終了して持ち場へ移るようにした。

**⚠️ まだ直っていない: センター付近の滞留**
- フロントコート確立後にセンター付近(自陣〜+3m)に居る攻撃側 17.9% → **21.8%**、
  そのうち止まっているのは 3.4% → **3.6%**。**改善していない**。
- 呼び出し元を記録すると、止まっている選手の最大要因は **「移動命令が出ていない」271件**。
  スポットを持っているのに、どの分岐にも入らずに立っているフレームがある。
  `p.rooted`(パス後の硬直)だけでは説明が付かない。**原因未特定・次の課題**。

**この回の実測**
| | 前 | 後 |
|---|---|---|
| 体が当たっているマーク | （0.2%＝測り方が誤り） | **19.3%**（0.62〜0.85m） |
| バックドア | 13.9回/試合 | 10.3回/試合 |
| 得点（8試合） | 15.7 | 16.1 |
| ワープ（上限3倍超） | 3件 | 3件 |

## フリーのレイアップ/ダンクが外れすぎる

**原因: ダンクの確率式が シュート精度を一切見ていなかった**
- 旧: `p = ダンク ? 0.82 + ダンク能力×0.15 : 0.5 + 中距離精度×0.35`
  ・ダンク経路は精度を無視。・レイアップは上手い選手でも**上限 85%**。
- しかも誰がダンクを選ぶかが `0.06 + 運動能力×0.7` で、実測すると
  **運動能力 55〜70 の選手でも 50% がダンクを選んでいた**。
  → 精度型が跳べないのにダンクへ行き、精度が結果に出ない。

**⚠️ 外した修正（記録）**: 「跳べる選手だけダンク」に絞ろうとして
`clamp((athletic-0.50)/0.32,0,1)*0.9` にしたら、ダンク選択率が 50% → **67〜81% に増えた**。
`athletic` はバランスを含むので全体的に高く出る。閾値を決める前に分布を見ること。

**採用した設計（ユーザー提案）**: 役割を分ける
- **素の決定力 = シュート精度**（ダンクでもレイアップでも）。フリーなら決まる。
  `p = (ダンク ? 0.78 : 0.62) + 中距離精度 × (ダンク ? 0.20 : 0.33)`
- **ダンク能力 = 競られても決め切る強さ**。接触の罰を減らす方に効かせる。
  `through = ダンク能力×0.6 + S威力×0.4` → `罰 × (1 - through×0.78)`
- ダンクを選ぶこと自体は運動能力のまま（跳べるなら跳ぶ）。

**実測（16試合）**
| | 結果 |
|---|---|
| フリー(1.5m超)の成功率 | **96.9%**（旧: 上限85%の式で、競り合い込み 55.9%） |
| 　精度65〜80 / 80以上 | 94.4% / **95.2%** |
| 2.5m以上フリー | 100% |
| 競り合い(1.1m以内)・ダンク能力60〜75 | 54.7% |
| 競り合い(1.1m以内)・ダンク能力75〜88 | **71.7%** ← ダンカーの優位性 |
| 得点（8試合） | 15.9 |
- ⚠️ ダンク能力88以上の競り合いは 25%（**8本だけ**）。本数が少なく意味を読めない。

## 4件（運び上げ中の戻り / 押し合いの見え方 / よろめき / リバウンドの踏み切り）

**1. 運び上げ中に3Pライン付近からセンター方向へ戻る選手の正体**
- `updateOffBallMotion` の「運び上げの支援」分岐。`!frontT` かつハンドラーがリムから9m超の間、
  **最良の非ビッグのガード1人**が、ハンドラーの逆サイド・**1.4m後方**へ降りて安全な
  パスの逃がし所を作る（プレス時は 0.4m 後方）。意図は「苦し紛れのパスをビッグではなく
  ガードへ出させる」こと。仕様どおりの動き。

**2. 押し合い・スタンが見えない → よろめきに繋いだ**
- 実測では押し合い自体は起きていた（**255回/試合**）。ただし押し出しは 1フレーム 0.018m、
  スタン中央 **0.06秒**で、数字の上でしか起きていなかった。
- バランス差で大きく負けたとき（`edge > BODY_PUSH.stagger`）は既存の
  **`foulReaction("hurt")`** を呼び、崩れた姿勢でたたらを踏み、立て直してから動く。
  実測 **21.8回/試合**。

**3. リバウンドの踏み切りが早い**
- 実測: ルーズ開始から **0.07秒**後、ボールの高さ **2.60m**（リングから弾んで**上がっている最中**）。
- リバウンドに限り「跳ね返りが頂点を過ぎてから」踏み切る条件を追加（上がっている最中でも、
  頂点までの時間が沈み込み＋滞空の半分より短ければ跳んでよい）。
- 実測: 踏み切り時のボール高さ 2.60 → **3.18m**。踏み切り 83 → 67 回（残りは地上で確保）。

**⚠️ 4. ハンドラー停止時にオフボール攻撃も止まる — 直せていない**
- 実測（ハンドラーが0.5秒以上停止中の停止率）: 攻撃 **45.8%** / 守備 36.3%。
- 止まっている攻撃側の足の状態: **足は自由 80** / 硬直 64 / プラント 56 / 押され 7。
  → 自分が入れた押し合いは主因ではない。「足は自由なのに動いていない」が最多。
- 試して**戻した**もの（いずれも実測で悪化）:
  ・持ち場の周回を距離条件なしにする → 攻撃の停止 45.8% → **55.1%**
  ・止まったら自分で持ち場を取り直す → **52.4%**
- 「足が自由なのに命令が出ていない」フレームが残る。どの分岐で抜けているかは**未特定**。
  ⚠️ 次はそこを直接特定する（1試合の site トレースでは標本4件で足りなかった）。

## 交代のポジション適性ロジックの調査（コード変更なし）

**仕組み**
1. `src/roster.ts` `clubTeam` / `randomizeTeam` — 13枠を `["PG","SG","SF","PF","C", ...BENCH_ROLES]`
   の順で埋める。該当ポジションのDB選手が尽きると `ROLE_FALLBACK` で別ポジションから引き、
   最後は残り全員から `slotValue` 最良を選ぶ。**その後 `role = roles[i]` で本人のロールを
   枠の位置に上書きする**。`posMask`（DB由来の守れるポジション）は本人のまま残る。
2. `src/ai/lineups.ts` `roleFit(p, slot)` — `slot === p.role` **または** `posMask` にビットが
   立っている、または `EXTRA_POSITIONS` 指定、で 1。3つの**どれか**で通る。
3. `src/systems/subs.ts` — 疲労交代 `planSubs` は `roleFit(b, out.role)`、マッチアップ交代
   `matchupSubs` も `roleFit(b, ourDef.role)`。**いずれも「退く選手の role」で判定**する。
   `substitute` は `sub.slot = out.slot` を渡すだけで `role` は書き換えない。

**実測**（probe-subfit / 172クラブ × 13人、試合は4試合）
- ロスター構築時点: DBロールと違う枠 5.3% / `posMask` に無い枠 41.8% / 両方不一致 3.6%（主に控え）。
- ジャンプボール時点のコート上: 適性なし **0.0%**（40件）。先発5人はズレていない。
- 試合中のコート上: 適性なし **7.5%**（先発枠 8.0% / 控え 6.3%）。
- `role` と実スロットの食い違い: **39.5%**。
- 適性なしの選手のうち「自分の role なら適格」: **100%**（718件）。

**結論**: 判定基準が「実際に就くスロットの位置」ではなく「退く選手の role ラベル」なので、
控えが `posMask` 経由で別の枠に入った瞬間に role とスロットがズレ、**次の交代はズレた role
を基準に適格判定する**。ズレが連鎖して適性外が積み上がる。

## ブロックが強すぎる件の調査（コード変更なし）

**コードの構造** `src/move/reaction/contest-block.ts` `bestBlocker`
- 候補の絞り込みは **`dist2D` のみ**。守備者が「ゴール側にいるか背後にいるか」という
  **向きの項が存在しない**。追走してきた守備者と、進路に立ちはだかる守備者が同じ扱い。
- ゴールテンディングの概念なし（`goaltend` の語はコードベースに存在しない）。
  リリース後に追いついた手も同じ抽選に乗る。
- フィニッシュの基礎値 0.34 ×(0.2+blk×1.35)×close、レンジ 1.35×palmRadius、上限 0.85。
- `shotTech` は**ブロック確率に一切入らない**。成立後の `evadeBlockProbability` の
  二段目抽選だけに効く。

**実測** probe-block（16試合、シューターは fga の増分で同定）
- 試投 444 / ブロック 120 → **全試投の 27.0% がブロック**。
- ゴール下 324本 → **35.5%**、うちダンク 178本 → **27.0%**。
- 守備者の位置別（ゴール下）: 前(ゴール側) 41.8% / 横 31.7% / **後ろ 30.0%**。
  成立分の内訳は 前 51.7% / 横 17.5% / **後ろ 30.8%**。
  → 後ろが前より高いわけではない。**後ろに本来あるべき不利がゼロ**。
- shotTech 別のブロックされ率（ゴール下）: 60〜75 → 43.4% / 75〜88 → 34.7% / **88〜101 → 24.4%**。
- `evadeBlockProbability` の実分布: 技術88以上でも平均 35.4% / 最大 38.8%。
  clamp の上限は 0.7 だが、定数 −0.12 とブロッカーのダンク/身長差で 4割に届かない。

**実際のバスケットとの比較（一般知識・未計測）**: NBA はブロック 1チーム約4.5〜5本/約88試投
= 試投の約5%。制限区域は FG 65%超、ダンクは9割以上決まる。本作は 27% で **約5倍**。
チェイスダウンブロックは実在するが稀で、主な理由は ①シューターの体が球を隠す
②下降に入った球/シリンダー上はゴールテンディング ③追走側はリリースに間に合わない。

## 実装①: 背後からのブロックに向きの項 + ファウル化

**変更** `src/move/reaction/contest-block.ts` / `src/move/action/shooting.ts`
- `behindAmount(def, shooter, rim)` を追加。シューター→リムの向きとの cos から「背後度」
  （cos −0.2 で0、−1.0 で1）を出す。
- `bestBlocker` に `rim` 引数を追加し、背後度に応じて確率を `1 − behind×0.75×(1 − chase×0.45)`
  で減衰。`chase` は `(守備の速度 − シューターの速度)/2.5` で、**本物のチェイスダウン
  （明確に速く詰めている）だけは戻る**。
- `behindFoulChance(def, behind)` を追加。背後で手が出たら `behind ×(0.55 − 守判断×0.30)`
  でクリーンなブロックでなく**シューティングファウル**にする（守判断が高いほど叩かない）。
- `tryBlock` の戻り値を `BlockHit = { def, foul }` に変更。`tryShootingFoul` に
  `{ force, by }` を追加し、抽選を飛ばして接触した守備者を指定できるようにした。
  決まっていれば AND-1、外れていればフリースローへ。
  ⚠️ ファウルが成立したフレームで `grazeShot` と通常のファウル抽選を重ねないようガード。

**実測 A/B**（probe-block、同一シード16試合、変更を stash して比較）

| | 前 | 後 |
|---|---|---|
| ブロック（1チーム1試合） | 3.8本 | **2.7本** |
| ゴール下のブロック率 | 35.5% | **28.5%** |
| ダンクのブロック率 | 27.0% | **22.0%** |
| **背後の守備者に対するブロック率** | **30.0%** | **6.1%** |
| 背後がブロック全体に占める割合 | 30.8% | 6.9% |
| 前(ゴール側) | 41.8% | 46.6% |
| 横 | 31.7% | 19.1% |
| フリースロー（1チーム1試合） | 0.6本 | **1.0本** |
| 得点（1チーム） | 13.6 | 13.4 |

- 狙った背後ブロックは **30.0% → 6.1%** に落ちた。ファウルは +0.4本/試合でファウル化も効いている。
- ⚠️ 前(ゴール側)の 41.8→46.6% は標本が小さく（試投141→146、ブロック59→68）**ノイズの範囲**。
  断定しない。ゴール下の試投総数が 324→291 に減っているのは、背後ファウルが FGA から
  外れるぶんを含む。
- ⚠️ **残件**: 全試投に対するブロック率は 87/439 = 19.8%。実際のバスケット（約5%）とは
  まだ4倍差。前方のブロックが高すぎる（②基礎値の調整）は未着手。

## 実装③: 完全なブロックをシビアにし、それ以外は「軌道を乱す」

**変更** `contest-block.ts` に `cleanChance(over, close, airborne, behind)` を追加。
`bestBlocker` が `clean`（完全に叩き落とせる確率）を返す。到達点差 over の実測分布
（ブロック成立時 中央 0.39m / 25% 0.31 / 75% 0.49）に合わせ、neutral 0.30 / span 0.35
を超えたぶんだけ加点。背後は ×(1 − behind×0.8)。
`shooting.ts` に `disturbShot()` を追加 — スワットにならなかった接触は
`shotMade = false` + ジャンパーは `shotGraze` で軌道をずらし、守備者は跳んでコンテスト。
所有は移らないのでボールはリムへ飛び、**リバウンドになる**。

**実測 A/B**（同一シード16試合、`clean` ゲートだけを切り替え）

| | ゲート無効 | ゲート有効 |
|---|---|---|
| ブロック（1チーム1試合） | 2.4本 | **1.3本** |
| 1選手1試合の最多ブロック | 4本 | **3本**（5本以上=0人） |
| ゴール下のブロック率 | 28.5% | **13.7%** |
| 全試投に対するブロック率 | 19.8% | **9.8%** |
| FG% | 49.9% | 49.8% |
| リバウンド（1チーム1試合） | 4.9本 | 4.9本 |
| ミス→リバウンド率 | 74.3% | 76.1% |
| 得点（1チーム） | 14.2 | 13.8 |

- ブロック成立時の到達点差の中央値は 0.39 → 0.47m。**残ったブロックは本物の高さ差**になった。
- ⚠️ **リバウンドは増えていない（4.9 → 4.9）**。理由はスワットも `goLoose(rebound:true)` で
  リバウンドに数えられていたから。ブロックをかすりに変えてもリバウンドの母数は変わらない。
  リバウンドの絶対数が少ないのは別原因（試投数と 76% の確保率）。
- ⚠️ シュート種別のブロック率は **3P 1.6% / 2Pジャンパー 2.6% / ゴール下 13.7%**。
  「3Pやジャンプシュートのブロックが目立つ」は**実測では再現していない**。

## パサーの優位性の調査（実測のみ、コード変更なし）

probe-pass、16試合 / パス1382本。

| P精度 | 本数 | 受け手のオープン度 | その時の最良 | 最良を選べた | ズレ幅 | カット率 |
|---|---|---|---|---|---|---|
| 65〜78 | 699 | 4.65m | 5.42m | 66% | 0.64m | 1.7% |
| 78〜88 | 556 | 4.79m | 6.03m | 63% | 0.42m | 0.7% |
| 88〜101 | 127 | 4.78m | 5.98m | **59%** | **0.29m** | 0.8% |

**効いているもの**: パスのズレ幅（0.64 → 0.29m）とカット率（1.7% → 0.8%）。
**効いていないもの**: **「フリーの味方を選ぶ」は精度と相関していない**。むしろ最良の受け手を
選べた割合は 66% → 59% と**下がる**。

原因（コードを読んで確認）:
- `chooseReceiver` の候補評価 `value = open + progress*3 − far + rand(-1,1)*(1−offense)*0.8`。
  **見極めの誤差は `offense` に紐づいていて `passAcc` に紐づいていない**。
- `passAcc` が上げるのは `riskTolerance`（0.12 + acc×0.2）。つまり**巧いパサーほど
  細い窓を通そうとして、わざとオープンでない受け手を選ぶ**。測定結果と一致する。
- `playmaking` 別では 3.97 → 5.00 → 5.19m と差が出るが、`playmaking` は `role`
  （＝ロスターのスロット）由来で個人の能力値ではない。

## リバウンドの踏み切りを「落ち始めてから」に変更

`src/core/looseball.ts` — 頂点までの猶予で例外的に跳べる条件（`toPeak <= lead`）を**削除**し、
`peakOK = !game.looseIsRebound || game.ball.vel.y <= 0`（下降に入ってから）にした。

| | HEAD（ゲート無し） | 変更後 |
|---|---|---|
| 上昇中に踏み切った割合 | 29/79 (37%) | **0/85** |
| 踏み切り時のボール高さ(中央) | 3.27m | 3.32m |

⚠️ 別件の発見: 最高打点で「ボールの高さ − 手の高さ」が中央 **0.79m**（手が届いていない）。
変更前後で同じなので今回の変更が原因ではない。リバウンドが少ない一因の可能性があるが未確認。

## 攻撃移行と守備フォーメーションの3点

**① アウトレットが後退しない** `src/ai/offense/offball/index.ts`
`otz = bz - s * 1.4`（ハンドラーの1.4m後ろ）→ 同じ高さ〜1m前、さらに
`s > 0 ? Math.max(otzRaw, p.pos.z) : Math.min(...)` で**自分より後ろへは絶対に行かない**。
実測: 運び上げ中の後退命令で、この分岐（offball/index.ts:67）は **7833件 → 上位8位圏外（425未満）**。

**② 持ち場への移動を走らせる** 同ファイル
倍率指定なし（1.0）→ `clamp(0.98 + 距離*0.09, 0.98, 1.15)`。守備の全力復帰(1.12〜1.15)と同等。
遠い時だけ `transitT` を立てて走る姿勢にする。
実測 A/B（倍率だけ切替）: ポゼッション開始から4人が攻撃位置に揃うまで **5.48秒 → 4.93秒**。

**③ 守備をフォーメーション優先へ** `src/ai/defense/offball/index.ts`
`SHELL`（リムから 5.2m/ウイング 5.3m/ベースライン 3.7m の5点、slot で割当）を追加。
`pickup = mRim < THREE_DIST+0.6 || (shooter3 && mRim < THREE_DIST+1.6)` とし、
**担当が陣形へ入っていなければ追わず、自分の持ち場を埋めて return**。
旧「非脅威は半径5mまで」のクランプは削除。

実測（担当＝自分のマーク対象との距離、4試合）

| 担当の位置 | 前 2m以内 | 後 2m以内 |
|---|---|---|
| 〜4.5m（ペイント） | 50% | **54%** |
| 4.5〜6.75m（アーク内） | 47% | 47% |
| 6.75〜7.5m（ライン直上） | 34% | **42%** |
| 7.5〜9m | 14% | 22% |
| 9m超 | 19% | **18%** |

- 守るべき帯（ペイント/ライン直上）は締まった。
- ⚠️ 7.5〜9m が 14→22% に増えた。`shooter3` の例外を THREE_DIST+1.6(=8.35m) まで
  認めたぶん。以前は shooter3 に距離制限が無く**どこまでも**付いていたので上限自体は前進だが、
  8.35m が妥当かは未検証。
- ⚠️ 9m超で 18% 残る。平均距離は 8.25m なので大半はトランジション中のすれ違いと思われるが、
  **断定できていない**。

**全体への影響**（probe-blk2、16試合）: ブロック 1.1本/チーム（最多2本）、
**FG 54.9%（前 49.8%）/ 得点 15.8（前 13.8）**。攻撃の移行が速くなったぶん得点力が上がった。
⚠️ FG 54.9% は実際のバスケット（約47%）より明確に高い。守備側の詰めが追いついていない可能性。

## リバウンド統計を OREB / DREB / 合計に分割

- `Stats` に `oreb` / `dreb` を追加。`reb` はこれまで通り**合計（トータルリバウンド）**。
- `secureLoose` で `offensive = p.team === game.looseOff` を使い、撃った側が確保したら
  オフェンスリバウンド、相手なら守備リバウンドとして計上。
- 表示: ボックススコアに `OR` / `DR` 列を REB の手前へ追加（`ui.ts` の `BOX_COLS`）。
  結果画面のチーム比較にも `OREB` / `DREB` 行を追加（`ui-result.ts`）。
- 実測（8試合）: 1チーム1試合あたり **OREB 1.6 + DREB 3.1 = 4.7**。合計と一致を確認。

## 「ハンドラーが止まると全員止まる」— 真因を特定して修正

**真因** `src/move/action/liveball.ts:63`
`updateLive` の冒頭にあるパス溜め分岐（`if (game.pendingPassTo)` — ジャンプパス/ターンパス/
反動パスの溜め中）が、**`runDefense` だけを呼んで `return` していた**。
オフボール攻撃の駆動 `updateOffBallMotion` は `runOffense` の**内側**にあるため、
この分岐へ入るたびに味方4人が**まるごと1フレーム分の移動命令を失っていた**。
守備だけが更新されるので、攻撃側だけが凍って見える。

⚠️ **過去3回の修正がすべて失敗した理由もこれ**。`offball/index.ts` を何度いじっても、
そのコード自体が実行されていなかった。

**特定の決め手**: `MOVE_TRACE` を esbuild の `--sourcemap` + `node --enable-source-maps`
で回し、呼び出し元を実ファイル:行で取れるようにしたこと。前回は `.mjs:338352` のような
バンドル後の行番号しか出ず、標本も4件しか取れずに打ち切っていた。

**修正**: 同分岐の `runDefense` の直前に `updateOffBallMotion(game, dt, h.team, h)` を追加。
（ボール飛行中は `passing.ts:271` で既に同じことをしていた。溜め中だけ抜けていた。）

**実測**（probe-freeze、3試合、ハンドラーが 0.5秒以上停止中のフレーム）

| | 前 | 後 |
|---|---|---|
| オフボール攻撃の停止率 | **56.1%** | **29.1%** |
| ペリメーター | 60.1% | 26.5% |
| ポスト(spotIdx>=5) | 45.1% | 39.6% |
| **(a) 命令なし** | **69件 (46.6%)** | **0件** |
| (c) 歩幅がほぼ0 | 11.5% | 29.2% |
| (b) 行き先が今いる場所 | 5.4% | 12.2% |

「命令なし」は完全に消えた。残りは (c) 加速が立ち上がらない（静止からの加速ランプ＝
物理的に正しい可能性が高い）と (b) 持ち場周回で今いる場所が目標になるケース。

**全体**（probe-blk2、16試合）: FG 50.4% / 得点 15.3 / リバウンド 5.6（OREB 1.6 + DREB 4.0）/
ブロック 1.4。FG は 54.9% → 50.4% に下がり、実際の値（約47%）へ近づいた。
⚠️ ただし probe-freeze の標本は前後でシード後の分岐がずれている（停止フレーム 66 → 295）ので、
率の比較は同一条件のA/Bではない。「命令なし 0件」という質的な事実が決定的。

## センターがゴール下に居ない / 移動がコート中央に寄る

### ① センターがゴール下に居ない — 真因と修正

`src/game.ts` `homeSpotIdx` が `p.slot === 3 ? 5 : 6` と**スロット番号だけ**で割り当てていた。
スポット6は **ローブロックではなくショートコーナー(x=6.2)**（「中は1人だけ」の設計で
意図的にペイント外へ出したもの）なので、**C（slot 4）は必ずペイント外**へ送られていた。
コメントは「PF=左, C=右」とブロック2つのつもりのまま残っていた。

修正: `postAnchor(team)`（チームのビッグで最も3Pが低い内寄りの1人）を切り出し、
**アンカーを 5(ローブロック) へ、もう1人のポスト役を 6(ショートコーナー) へ**。
スロット番号ではなく役割で決まるようにした。「中は1人だけ」の設計は維持。

実測（probe-lane、6試合、攻撃中）

| | 前 | 後 |
|---|---|---|
| C のリムからの平均距離 | 9.96m | **9.18m** |
| C のゴール下3.5m内 | **12.7%** | **21.0%** |
| C のペイント内(半幅2.45m) | — | 18.5% |
| PF のゴール下3.5m内 | 22.7% | 25.0% |

### ② 移動がコート中央に寄る — 部分的に改善

**実測した事実**: フォーメーションのスポットは広い（ウイング |x|=5.66m、コーナー 6.80m）のに、
選手の実際の位置はウイング 3.48m / コーナー 3.72m と **2〜3m 内側**。持ち場から1.8m以内に
居るのは **19.7%** だけ。ワイドレーン(|x|>4m)に居るのは平均1.7人、**12.9% は0人**だった。

切り分けで**否定した仮説**（いずれも実測）:
- スペーシングのナッジ（`spacingNudge` / `ballSpacingNudge`）が内側へ寄せている
  → 両方を無効にしても |x| は 3.29 → 3.75m、持ち場率 19.4 → 20.8% にしか変わらない。主因でない。
- `getBackOnDefense` の目標 x=0（中央）が原因
  → `runLane()` で自分のレーンを保つように変えたが、守備の中心1.5m以内は 39.9 → 37.9% で
     ほぼ動かず。寄与は小さい。

**入れた修正**:
- `ai/defense/shared.ts` に `runLane(curX, wantX, converge)` を追加。戻りの走路が
  コート中央（x=0）へ吸い寄せられないよう、今のレーンを保って斜めに寄せる。
- `ai/offense/offball/index.ts` の持ち場への接近で、**遠く・外の持ち場・自分がまだ内側**の
  3条件が揃う間は縦の詰めを 0.45 倍に抑え、**先に横へ開いてから上がる**（＝斜めに走る）。

実測

| | 前 | 後 |
|---|---|---|
| オフボール攻撃の実際の \|x\| | 3.35m | **3.82m**（持ち場の定義は 4.62m） |
| 持ち場から1.8m以内 | 19.7% | **23.8%** |
| トランジション中 \|x\|>4.5m | 21.5% | **26.3%** |
| トランジション中 \|x\|<1.5m | 33.9% | **30.0%** |
| ワイドレーンに0人 | 12.9% | **10.4%** |
| ワイドレーンに4人以上 | 8.4% | **25.0%** |

⚠️ **持ち場率は 23.8% で、まだ低い**。残りの 76% が何をしているか（shakeOpenT / cutting /
clearDriveLane / isoSpread の各分岐に居る時間）は**未計測**。分岐別の占有率を測る計装が
途中で動かず、そこまで到達していない。次はそこから。

**全体**（probe-blk2、16試合）: FG 51.7% / 得点 14.6 / リバウンド 4.9 / ブロック 1.2。悪化なし。

## センターに適性外の選手が付く — 調査（コード変更なし）

**ロジック**: `src/ai/defense/index.ts:46`
```ts
const man = offense[d.slot];   // index一致の man-to-man
```
**マッチアップは「スロット番号の一致」だけ**。身長・ポジション・適性を一切見ていない。
`teamPlayers` は `players.slice(0,5)` で配列順＝スロット順、`substitute` は
`game.players[i] = sub` と `sub.slot = out.slot` で番号を引き継ぐ。

これが成立するのは「両チームのスロットNに同種の選手が居る」前提のときだけ。実際は崩れている:
1. `homeSpotIdx` はポストアンカー（3Pが最も低いビッグ）をスロット番号と無関係に
   スポット5/6へ送る → 攻撃側の「ゴール下の選手」はスロット3のことも4のこともある。
2. **交代のドリフト**（既出）: `planSubs` / `matchupSubs` が `roleFit(b, out.role)` と
   **退く選手の role ラベル**で適格判定するため、コート上の 39.5% がスロットと
   role の食い違った状態になる。
3. `matchupSubs` のインテリア・ミスマッチ交代は `gap > 0.12 || hurting` かつ
   `stintT > 8` / `matchupHoldT` 付きで、ポゼッション単位の是正にはならない。

**実測**（probe-match、8試合、マッチアップ標本 38400）

- 担当のスロット位置に適性が無い守備者: **8.2%**
- 相手のポストアンカーに付いている守備者 7680件のうち
  - **本来ガード(PG/SG)だった: 13.3%**
  - 相手より8cm以上低い: **16.0%**
  - そのスロットに適性なし: 5.5%
  - 身長差(守備−攻撃) 中央 −1cm / 25% −5cm / **最小 −16cm**
- 多い組み合わせ: PF↔PF 33.9% / C↔C 20.2% / C↔PF 12.9% / PF↔C 12.2% /
  **PG が PF 6.5% / PG が C 4.2% / SG が C 1.4%**

**対策候補**（未着手）
- (A) 交代の適格判定を `out.role` → `SLOT_POS[out.slot]` に直し、ドリフトの供給を断つ。小さい。
- (B) ポゼッション開始時に**体格/ポジションでマッチアップを組む**関数を入れ、
  `offense[d.slot]` を置き換える。`anchor` 選定・スクリーンカバレッジ・`matchupSubs` が
  すべて slot を前提にしているので影響範囲は広い。

## 交代で適性外のポジションに就く — 修正

**真因** `src/systems/subs.ts` の4か所すべてが `roleFit(b, out.role)` と
**退く選手の role ラベル**で適格判定していた。実際に入るのは**スロット**なのに。
`role` はロスター構築時にスロットの位置で上書きされ、交代で別の枠へ移った後も更新されない。

**修正**
- `src/ai/lineups.ts` に `SLOT_POS` と `slotPosOf(p)`（= `SLOT_POS[p.slot]`）を追加。
- `subs.ts` の4か所を `roleFit(b, slotPosOf(out))` 等へ置換（疲労交代 123行、
  マッチアップ交代 72/93行、事前復帰 151行）。

**実測**

| | 前 | 後 |
|---|---|---|
| コート上でスロット位置に適性なし | **7.5%** | **0.0%** |
| 　先発枠 / 控え | 8.0% / 6.3% | 0.0% / 0.0% |
| 交代で入った回数（1チーム1試合） | 18.4回 | **19.8回**（減っていない） |
| 守備マッチアップで適性なし | **8.2%** | **0.0%** |

⚠️ 適格判定を厳しくしても交代が枯れないことを確認済み（18.4 → 19.8回）。

**残っている問題（別件・未修正）**: 相手のゴール下ビッグに本来ガード(PG/SG)が付く場面は
13.3% → 12.5%（6試合では 11.1%）で**ほぼ変わらない**。理由を静的に確認した:

- `roster.ts` の `ROSTER[team][i].role = roles[i]` でスロットの位置がラベルとして
  上書きされるため、`roleFit` の第1条件 `slot === p.role` が自分のホーム枠では常に真。
  **全2236件のうち 41.8% は「上書きラベルのみ」で適格**になっており、posMask は
  そのスロットを含んでいない。
- ただしロスター構築時点で「本来ガードがビッグ枠に座る」のは **0.2%（4件）** だけ。
  つまり構築が原因ではない。
- `role` と実スロットの食い違いは修正後も **29.0%** 残る。攻撃側のビッグがガード枠に
  座る（posMask 経由で適格）と、その枠の守備者＝ガードが付くことになる。
- `roleFit` は**身長を一切見ない**。

→ 次の手の候補: (1) `applyDbPlayer` で真のDBロールを保持し `roleFit` の第1条件を
そちらにする（適格が厳しくなるので交代が枯れないか要実測）、(2) `roleFit` に身長の項を
足す、(3) ポゼッション開始時に体格でマッチアップを組み替える。**どれも未着手。**

## リザルト画面: リバウンド表示を「合計(内訳)」へ、横幅を圧縮

**課題**: OR / DR / REB を3列に分けたため ①ボックススコアが横に広がって
スクロールが出る ②OR・DR が REB の内訳だと読み取れない。

**変更** `src/ui/ui.ts` `BOX_COLS` / `src/ui/ui-result.ts`
- OR・DR・REB の3列を **`REB(O+D)` の1列**にまとめ、`5 (1+4)` = 合計 (オフェンス+ディフェンス)
  と表示。0本のときは `0` だけにして `0 (0+0)` のノイズを避ける。
- チーム比較も OREB / DREB / REB の3行を **`REB (O+D)` の1行** `38 (10+28)` へ。
- 列同士の余白を **4px → 2px**、名前列を 128 → 124px、各列幅も詰めた
  （MIN 40→32 / PTS 34→26 / FG 48→40 / 3P 44→40 / FT 44→40 / AST・STL・BLK 34→24 / TO 30→24）。

**幅の実測**

| | 合計幅 |
|---|---|
| OR/DR を足す前 | 544px |
| OR/DR を3列で足した後 | **608px**（横スクロール発生） |
| 今回（1列＋余白圧縮） | **482px** |

元より 62px 狭い。REB列の表示は最長で `15 (5+10)` = 9文字 ≒ 60px なので w=64 に収まる
（probe-box で実レンダリング内容を確認済み）。

## リザルト画面: リバウンドを別列に戻し、背景で「内訳と合計」を表す

**変更**（前回の1列統合を取りやめ）
- `src/ui/ui.ts` `BOX_COLS` を **OR / DR / REB の3列**に戻し、各列に `tint` を持たせた
  （OR・DR = `"part"`、REB = `"total"`）。
- `UI.REB_TINT` を追加。色は UI 全体でリバウンドに割り当てている `#ffd85e` に合わせる。
  - `part`  = `rgba(255,216,94,0.10)`（内訳・薄）
  - `total` = `rgba(255,216,94,0.22)`（合計・濃）＋ 太字
- `src/ui/ui-result.ts` に `rebTint(el, tint, first)` を追加し、ヘッダー行とデータ行の
  両方へ適用。
  ⚠️ 行は `flex + gap:2px` なので、そのまま背景を塗ると列の間に 2px の切れ目が入って
  「別々の3列」に見える。`part` に `marginRight:-2px` + `paddingRight:2px` を入れて
  隙間を埋め、帯が連続するようにした。角丸は帯の左端(OR)と右端(REB)だけ。
- チーム比較も OREB / DREB / REB の3行へ戻し、同じ色で背景を敷いた（内訳の行は
  文字の不透明度を 0.75 に落として合計を目立たせる）。上下の角だけ丸める。

**幅**: 500px（名前124 + 列352 + 余白24、`part` の負マージンで実質 −4px）。
OR/DR を3列で足した当初は 608px で横スクロールが出ていた。OR/DR 追加前の 544px より
まだ狭いので、スクロールは出ない見込み。

## リザルト画面: ヘッダーのずれ・列の二重描画を修正（自分が入れた不具合）

**症状**: 項目名と数値の列がずれ、同じ項目が2つ並ぶ。
**原因**: 直前の編集（sed の行置換）で `statsTable` のヘッダー組み立てを壊していた。
1. `cols.appendChild(hc)`（左端の名前セル）が消えていた → ヘッダーだけ 124px 左へずれる。
2. ヘッダーの列追加ループが**2回**残っていた（rebTint 版 + 旧の素の版）→ 同じ項目が二重に並ぶ。
3. `Object.assign(cols.style, …)`（flex/gap/フォント指定）も消えていた。

**修正**: ヘッダー組み立てを書き直し（style → hc → forEach の1回だけ）。

**再発防止**: `rebTint` から**レイアウトに影響する指定を全廃**した。
- 旧: `paddingRight:2px` + `marginRight:-2px` + `boxSizing:content-box` で隙間を埋めていた
  → 列幅が変わり得るので、ずれの温床。
- 新: `boxShadow: "2px 0 0 <色>"` で右へ塗るだけ。**影は場所を取らないので桁は1pxも動かない**。
  関数コメントに「padding / margin / width / box-sizing を入れるな」と明記した。

**検証**: `UI.prototype.*` が各1個、`UI.BOX_COLS.forEach` がちょうど2箇所（ヘッダーとデータ行）
であることを確認。probe-box の出力で 12列が1回だけ並ぶことを確認。合計幅 500px。

## ⚠️ 仮: タイトル画面からリザルト画面を開くボタン（レイアウト確認用）

試合を最後までやらないとボックススコアを確認できないため、一時的な確認導線を追加。

- `src/ui/ui-title.ts` — タイトルに破線枠のボタン「リザルト画面を確認（仮）」を追加。
  `onNeedWorld()` でコート/選手が未生成なら生成し、`ui.game` を使って
  `showResultPreview` を呼ぶ。
- `src/ui/ui-result.ts` — `showResultPreview(game)` を追加。全選手にもっともらしい
  ダミースタッツを入れてから `showResult` を呼ぶ。
  **`reb = oreb + dreb` を必ず満たす**ように作るので、内訳と合計の帯の見え方を検証できる。
  得点はスタッツから逆算し、ラインスコアも合計が一致するように最終Qで帳尻を合わせる。

**削除方法**: `ui-title.ts` の該当ブロックと `ui-result.ts` の `showResultPreview`
（および型宣言の1行）を消せば元に戻る。他のコードには触れていない。

## リザルト画面: 背景色を全列へ拡張（内訳だけ薄く）

トータルリバウンドに使っていた濃い方の背景を **MIN / PTS / FG / 3P / FT / AST / STL / BLK / TO**
にも適用し、行全体を1本の帯にした。OR・DR は従来の薄い色のまま残すことで、
**「薄い＝REB の内訳」「濃い＝通常の項目」** という読み方になる。

- `src/ui/ui.ts` — `REB_TINT` を `CELL_TINT { base, part }` に改名。
  `BOX_COLS` の型を `tint?: "part"; bold?: boolean` に変更（REB は `tint` をやめて `bold: true`）。
- `src/ui/ui-result.ts` — `rebTint` を `cellTint(el, tint, bold, first, last)` に置き換え。
  全セルに `base` を敷き、`tint: "part"` のセルだけ薄くする。角丸は帯の左端(MIN)と
  右端(TO)だけ。隙間埋めの `boxShadow` は最終列以外の全セルへ。
- チーム比較も同様に全行へ `base` を敷き、OREB/DREB だけ `part`。REB は太字(800)。
  角丸は面の上端と下端のみ。

⚠️ 作業中に行番号 sed で `teamCompare` のループを2度壊した（`for` 行や `Object.assign` の
消失、重複行）。**この2ファイルは行番号指定の編集を避け、ブロック単位で書き直すこと。**
修正後、`UI.prototype.*` が各1個・`UI.BOX_COLS.forEach` が2箇所・`rows.forEach` が1箇所
であることを確認済み。列幅 500px は不変。

## リザルト画面: ヘッダー2段化＋ゼブラ縞（背景色の帯は廃止）

- `BOX_COLS` に `group?: string` を追加。連続して同じ `group` を持つ列には、
  **ヘッダー上段にまとめた見出しを1つ**置く。リバウンドは上段「REB」を3列ぶんの幅
  （24+24+30＋列間の余白2×2 = **82px**、OR/DR/TD が占める幅と一致することを確認）で
  centerに置き、下線を引いて束ねを示す。下段は OR / DR / TD。
  グループ以外の列は上段に同じ幅の空セルを置いて桁を合わせる。
- 背景色の帯（`CELL_TINT` / `cellTint`）は**全廃**。代わりに `ROW_ALT` で
  **1行おきの地色**（ゼブラ）にした。
  ⚠️ 左端の名前セルは sticky で不透明な地色を持つため、行の背景が透けない。
  `NAME_ALT`（`#0c0f16` / `#15181f`）で名前セル側にも同じ明暗を持たせる。
- チーム比較も背景を廃止し、OREB / DREB は文字の不透明度 0.6・フォント 11px に落として
  内訳だと分かるようにした（REB は 800 で太字）。

合計幅 500px は不変。`UI.prototype.*` 各1個・`rows.forEach` 1箇所を確認済み。

⚠️ 3列目のラベルは指示どおり **TD** にしたが、合計リバウンドなので **TR / TOT** の
書き間違いの可能性がある。要確認。

## リザルト画面: ヘッダーを1行flexにし、通常の項目名を2段ぶんの高さで縦中央へ

前版は「上段の空セル＋下段のラベル」で2行に分けていたため、MIN などの項目名が
下段に寄っていた。**ヘッダーを1行の flex に変え**、その中で group を持つ列だけ
上下2段に割る構造へ変更。

- `headCell(text, w, dim, bold)` を追加。`display:flex` + `alignItems:center` なので、
  行の高さ（＝REBブロックの2段ぶん）いっぱいに伸びて**項目名が縦中央**に来る。
- REB は `flexDirection: column` のブロック（幅82px）で、上に見出し＋下線、
  下に OR / DR / TD を並べる。
- ヘッダー全体の `opacity` をやめ、セル単位で 0.6（項目名）/ 0.85（まとめ見出し）に。

合計幅 500px・REB見出し 82px は不変（検算済み）。
`UI.prototype.*` 各1個であることを確認。

## ポーカー画面へ役割設定・入れ替え・VSボードを集約（選手一覧の移行 第1段）

前試合画面の選手一覧を廃止する前段として、ポーカー画面の選手セルへ機能を移した。
**前試合画面はまだ残してある**（窮屈だった場合に引き返せるように）。

- **役割ピル** `rolePills()` — セルの名前の下に「攻ロール / オフェンス選択順位 / 守ロール」を
  横並びで追加。順位は攻ロールの隣で、タップごとに 1→2→…→5→自（自動）と回る。
  ロールは既存の `openRolePicker(def, team, el, onPick, "off"|"def")` をそのまま使い、
  `onPick` に `renderPoker` を渡して再描画。控え(size<40)はフォントとパディングを一段小さく。
  自分のチーム以外は表示のみ（`POKER_OPTS.userTeam` で判定）。
- **操作モードのトグル** `pokerModeToggle()` — 「カードで強化」⇄「選手を入れ替え」。
  `ui.pokerMode` / `ui.pokerSwapPick` を追加。入れ替えモードでは1人目を選ぶと緑の枠が付き、
  2人目のタップで枠を交換する。
- **顔タップで選手詳細** — カードを持っていない状態で顔を叩くと `openDetailModal` が開く。
  札を持っているときは従来どおり配置。操作が競合しない。
- **VSボード** — モーダル最上部へ。幅は前試合画面と同じ `min(560px, 100%)` で中央寄せ。

⚠️ **入れ替え時のインデックス付け替え** `swapRosterSlots()`
ポーカーの強化は `applied[].idx` の**インデックス管理**で、`revert()` が
`roster[team][idx]` から能力を引く。枠を入れ替えたら記録側の idx も入れ替えないと
**別人の能力を削る**。置きかけの札（`pokerTargets`）も同じ理由で付け替えている。

**未実施**: 前試合画面の廃止、DBからの選手入れ替えの削除。実機で窮屈さを確認してから。

## ポーカー画面: 役割ピルを顔の右横へ / カードを上段へ集約

**役割ピルの配置** `rolePills()` / `playerCell()`
顔の下 → **顔の右横に縦2段**へ変更。1段目が「攻ロール + オフェンス順位」、2段目が「守ロール」。
`playerCell` の先頭に `headRow`（flex row）を作り、顔とピル列を横並びにした。
名前とカードチップはその下に残る。

**カードを上段へ集約**
以前はアウェイの伏せ札が最上段、ホームの手札が中段と**上下に分かれて**縦に伸びていた。
`top` パネル1か所へ集約し、**今交換している側だけ**を出す。

| 状態 | 上段パネルの中身 |
|---|---|
| 相手が交換中（`pokerStage === "cpu"`） | 相手の伏せ札 |
| 「選手を入れ替え」モード | 控え8人（トグル表示） |
| それ以外（自分の交換中） | 自分の手札 |

⚠️ 判定は**相手の交換中を最優先**にすること。伏せ札の位置が、飛んでくる札の演出
（`flyCardToTarget`）の起点になるため、入れ替えモードで隠すと演出が壊れる。
パネルは `minHeight: 84px` で高さを固定し、中身が切り替わっても下の要素が動かないようにした。

控え8人は独立した行を廃し、上段パネルへ移動。コート盤（先発10人）は中段に残るので、
入れ替えモードでは「上段の控え」と「中段の先発」をタップして交換できる。

## 旧・前試合画面（選手一覧）を導線から外す

役割設定・先発入れ替え・VSボードをポーカー画面へ移し終えたので、旧画面を通らないようにした。

- `UI.startMatch()` を追加（`ui-poker.ts`）。旧 `tipOffButton` と**同じ3手順**
  （`setPhase("playing")` → `onPrepare()` → `beginPoker(() => onStart())`）を保つ。
- `ui-title.ts` のクラブ選択完了と `ui-result.ts` の再戦を `startMatch()` へ差し替え。
  `setPhase("pregame")` の呼び出しはコードベースから消えた（＝旧画面は表示されない）。

⚠️ **`ui-pregame.ts` は削除できない**。ポーカー画面が `buildVsBoard()` を使っており、
`refreshEditors()` もクラブ選択・再戦から呼ばれている。ファイルは残し、画面への導線だけ外した。

⚠️ **先発入れ替えとコートの同期**
`onPrepare()` の `applyRoster()` がコート上の5人を確定させるのは**ポーカーより前**。
ポーカー中に枠を入れ替えても、そのままではコートが古いまま残る。
`swapRosterSlots()` の中で `ui.onPrepare()` を呼び直して組み直すようにした
（ポーカー画面はオーバーレイで `simPaused` なので、下の3Dを組み直しても問題ない）。

## ポーカー画面: カードを盤の下へ / 控えを上へ / ボタンを1行に集約 / 役を戦力値の隣へ

**並び替え** `renderPoker`
```
VSボード（戦力値の隣に役）
[交換継続][カードで強化][選手を入れ替え][手札公開]
控え8人
コート盤（先発10人）
カード（相手の伏せ札 + 自分の手札）
現在の役 + 交換ボタン
```
控えを盤の上、カードを盤の下へ。カードは相手・自分を同じ枠に縦に並べ、
`minHeight: 84px` で高さを固定（相手の番 ⇄ 自分の番で下の要素が動かない）。
⚠️ 相手の伏せ札は常に描くこと。位置が `flyCardToTarget` の演出の起点になる。

**ボタン行** `pokerButtonRow()`
`pokerModeToggle()` を置き換え、左から「交換継続 / カードで強化 / 選手を入れ替え / 手札公開」。
- 手札公開 = 旧「確定」(`m.confirm()`)
- 交換継続 = 旧「持ち越し」(`m.carryOver()`)
下段の確定フェーズのボタンは削除。両脇は確定フェーズでだけ押せ、押せない間も
場所を確保して薄く出す（行幅がフェーズで変わらないように）。

**役を戦力値の隣へ** `buildVsBoard`（ui-pregame.ts）
`ovrEl(v, win, delta, t)` に team を渡し、`game.poker` の役名を戦力値の右へ並べた。
確定前は `peek(t)` の暫定値、確定後は `teams[t].rank` を使う。

## ポーカー画面: カード欄を「出している側だけ」に戻す

盤の下のカード欄に相手の伏せ札と自分の手札を**両方**並べていたため、同じ場所に手札が
2列できて二重に見えていた。`pokerStage` で出し分ける。

| 状態 | カード欄 |
|---|---|
| 相手が交換中（`pokerStage === "cpu"`） | 相手の伏せ札だけ |
| それ以外（自分の交換中・確定・公開） | 自分の手札だけ |

⚠️ 相手の交換中は必ず `opponentArea` を描くこと（伏せ札の位置が `flyCardToTarget` の起点）。
`minHeight: 84px` で高さを固定しているので、切り替わっても下の要素は動かない。

## ポーカー画面: 下段の役表示を削除 / 交換ボタンを上段へ / 交換継続を廃止

- **下段の役表示を削除**。役（ハイカード等）は VS ボードの戦力値の隣に出しているので重複。
  下段に残るのは公開後の「試合へ」だけになった。
- **交換ボタンを上段のボタン行へ**。「交換継続」があった左端の位置に置く。
  表示は置いた枚数で変わる（0枚なら「交換しない」）。押せるのは交換フェーズのみ。
- **「交換継続」（`m.carryOver()`）をユーザー操作から廃止**。

  ⚠️ `carryOver` 自体は残す。CPU 同士の観戦進行（`runWatchRound`）と
  `cpuHomeDecides` が使っており、ホームが確定を先送りする判断は AI 側に必要。
  消えたのは**プレイヤーが持ち越しを選ぶ導線**だけ。

ボタン行は `[交換しない / N枚 交換] [カードで強化] [選手を入れ替え] [手札公開]`。
両脇はそれぞれのフェーズでだけ押せ、押せない間も場所を確保して薄く出す（行幅を固定）。

## ポーカー画面: ボタンをカードの右隣へ縦並び / 戦力バーを薄く

**ボタン** `pokerButtonRow()`
横1行 → **縦並び**（`flexDirection: column` / `alignItems: stretch` / `minWidth: 104px`）。
VSボードの下から、カード欄の**右隣**へ移した（カードとボタンを1つの flex 行に入れる）。
カード欄は `width: auto` にして、ボタンぶんの幅を空ける。

**戦力バー** `buildVsBoard()`（ui-pregame.ts）— 縦を薄くする
| | 前 | 後 |
|---|---|---|
| バーの高さ | 8px | **5px** |
| 軸ラベル（シュート/高さ 等） | 11px | **9px** |
| 値の数字 | 12px | **10px** |
| 戦力値 | 22px | **18px** |
| 行のグリッド | `40px 1fr 54px 1fr 40px` / gap 6px | `34px 1fr 46px 1fr 34px` / gap `2px 5px` |
| 行の `line-height` | 既定 | **1.1** |

## ポーカー画面: 控えのトグルを一覧の左へ

`benchSideToggle()` を横並び＋「控え」の見出し付き → **縦並びのボタンだけ**にして、
控え一覧の**左**へ置いた（`benchRow` の flex 行に トグル → グリッド の順で入れる）。
見出しの文字は削除（チーム名のボタン2つで用が足りる）。
カード欄のボタン列と同じ作り（`flexDirection: column` / `alignItems: stretch`）。

## ポーカー画面: 名前を小さく / 盤の左半分はピルを顔の左へ / ボタンをカードの左へ

- **選手名のフォント**: 先発 10px → **9px** / 控え 9px → **8px**。
- **盤の左半分（相手）のピルを顔の左へ**。`playerCell(..., mirror)` と
  `rolePills(..., mirror)` に `mirror` を追加。`mirror` のときは
  `headRow` の並びを「ピル → 顔」にし、ピル列を `alignItems: flex-end` で右寄せにする。
  `courtBoard` の `put(opp, ...)` にだけ `true` を渡す（盤の中央が空いて見やすくなる）。
  ⚠️ 控え一覧は盤の左右と関係ないので mirror しない。
- **ボタンをカードの左へ**: `bottom.append(pokerButtonRow(...), cards)` と順序を入れ替え。

## ポーカー画面: ボタンをカードの左右へ分離 / 左側チームの順位ピルを左端へ

- `pokerButtonRow()` を **2つの列**に分割。共通の器は `btnCol()`。
  - `pokerActionCol()` … カード欄の**左**。「交換しない / N枚 交換」「手札公開」。
    それぞれのフェーズでだけ押せ、押せない間も場所を確保して薄く出す。
  - `pokerModeCol()`   … カード欄の**右**。「カードで強化」「選手を入れ替え」。
  並びは `bottom.append(pokerActionCol(...), cards, pokerModeCol(...))`。
- `rolePills()` の `mirror` 時の並びを **順位 → 攻ロール** に変更（左側チームは
  オフェンス順位が攻ロールの左端に来る）。`mirror` でない側は従来どおり 攻ロール → 順位。

## ポーカー画面: カードを小さく / 盤との隙間を詰める

- 手札のカード: `cardFace(card, 52, 74)` → **`44, 62`**。
- コート盤の下マージン: `2px 0 14px` → **`2px 0 2px`**。
- カード欄の `minHeight`: 84px → **68px**（カードが小さくなったぶん枠も詰める。
  相手の番 ⇄ 自分の番で下の要素が動かない性質は維持）。

## ポーカー画面: 戦力バーを最上部へ / 控えの上下の隙間をほぼゼロに

- パネル: `padding: "6px 12px 12px"` → **`"0 12px 12px"`**、`gap: "6px"` → **`"3px"`**。
  上詰め（`justifyContent: flex-start`）と合わせて、VSボードがモーダルの上端に着く。
- VSボードの外枠（ui-pregame.ts の `buildVsBoard` の wrap）:
  `padding: "7px 14px"` → **`"2px 14px 4px"`**。
- 控えグリッド `benchGrid`: `padding: "6px 8px"` → **`"1px 8px"`**、
  行間 `gap: "4px 10px"` → **`"1px 10px"`**。上下の余白がほぼ無くなる。

## ポーカー画面: 札を置いてもカード欄の幅を変えない

札を選手へ置くと手札が減り、カード欄が縮んで**左右のボタン列が内側へ寄って動いていた**。
カード欄の幅を手札5枚ぶんで固定し、`flexShrink: 0` を付けた。

- `CARD_W = 44` / `CARD_H = 62` / `CARD_GAP = 8` を定数に切り出し、
  `handCard` と幅の計算で共有。
- `cards.style.width = CARD_W * HAND_SIZE + CARD_GAP * (HAND_SIZE - 1)` px（= 5枚ぶん）。
- 高さは既に `minHeight: 68px` で固定済みなので、これで**上下左右とも動かない**。

## ポーカー画面: 「継続」を選べるように / 「交換しない」は持ち越しへ

左のボタン列の上段が局面で役割を変えるようにした。

| 局面 | ラベル | 動作 |
|---|---|---|
| 交換フェーズ・札あり | 「N枚 交換」 | 置いた札を切る（→ 確定フェーズ） |
| 交換フェーズ・札なし | 「交換しない」 | **確定させず次のクォーターへ持ち越す**（`carryOver`） |
| 確定フェーズ | 「継続」 | 公開せず次のクォーターへ持ち越す |

下段の「手札公開」は従来どおり確定フェーズでのみ。
これで確定フェーズに「手札公開」と「継続」の**両方**が並ぶ。

⚠️ 最終ラウンド（`m.round >= POKER_ROUNDS`）は持ち越せない。この場合の「交換しない」は
0枚のまま確定フェーズへ進める。ここを `carry` に倒すと、持ち越せず公開もできない
**詰み**の局面ができる。

## CPU も編成と役割を動かす

`cpuAdjustSquad(ui, m, cpu)` を追加し、`runCpuTurn` の手番処理から呼ぶ
（観戦モードも `runCpuTurn` 経由なので両CPUに効く）。

1. **役割**: 先発のうち `evalRole` / `defRole` が未設定（＝自動）のものだけ、
   `ui.bestOffRole` / `ui.pickDefRole` で埋める。守備ロールは `takenDef` で重複を避ける。
   ⚠️ 手で設定済みのロールは触らない。
2. **入れ替え**: 最も OVR の低い先発と、その枠に `roleFit` で適格な控えの最良を比べ、
   **SWAP_EDGE(=3) 以上の差**があるときだけ1手番に1回だけ入れ替える。
   ⚠️ 差が小さいと毎ラウンド入れ替わって落ち着かないので閾値を置く。
   ⚠️ 入れ替えは必ず `swapRosterSlots` を通す（`applied[].idx` の付け替えが要る）。

## フォーメーションのベース位置をデータ化（ボードUIの土台）

「各選手が攻撃時・守備時にベースとすべき位置」を編集できるようにするための土台。
**その瞬間に取る位置は従来どおり AI が決める**（オープンスポットへの動き直し・
スペーシング・マーク外しは不変）。ベース位置は開始位置と戻る先の基準。

- `Tactics` に `offBase?` / `defBase?`（`{ x, d }[]`）を追加（`src/attributes.ts`）。
  ⚠️ 座標は**攻める(守る)リムから見た正規化**で持つ。`x` = コート横方向、
  `d` = リムからミッドコート方向への距離。攻守でコートが入れ替わってもそのまま使える。
- `game.ts` に `OFF_BASE_DEFAULT`（7スポット）を切り出し、`formationSpots` は
  `TACTICS[team].offBase ?? OFF_BASE_DEFAULT` を世界座標へ変換して返すだけにした。
- `ai/defense/offball/index.ts` の `SHELL` を `DEF_BASE_DEFAULT`（5か所）に置き換え、
  `shellSpot` は `TACTICS[defTeam].defBase ?? DEF_BASE_DEFAULT` を読む。

**挙動が変わっていないことの確認**（probe-lane 4試合）: C のゴール下3.5m内 24.0% /
ペイント内 20.3% で、データ化前（21.0% / 18.5%）と同水準。既定値のままなら従来と同じ。

**残**: フォーメーションボードのUI（3枚トグル・顔アイコン・ドラッグ編集・相手の攻守表示）。

## フォーメーションボードのUI

盤を3枚にし、上のトグルで切り替える: **試合盤 / 各チームの配置ボード（2枚）**。
`ui.pokerBoard: "match" | 0 | 1` で保持。

**配置ボードの考え方**（`formationBoard`）
フルコート1枚に攻守を同時に載せる。保持形式 `{x, d}` が「リムからの距離」なので、
**攻撃ベースは攻めるリム側（右端）／守備ベースは守るリム側（左端）**に自然に分かれる。
さらに左端には**相手の攻撃ベース**、右端には**相手の守備ベース**を薄く重ねるので、
そのままマッチアップの絵になる（相手側は `pointer-events: none` で動かせない）。

- 顔アイコンのみ。名前も役割ピルも置かない（指示どおり細かい設定は載せない）。
- 自分のベースはドラッグで移動。`pointerdown` で `setPointerCapture` し、
  移動のたびに `TACTICS[team].offBase/defBase` を書き換える（AI が即座に読む）。
- 攻撃側は `homeSpotIdx(player)` が返すスポットを動かす（ポストのビッグは 5/6）。
  守備側は slot と1対1。
- 編集できるのは `POKER_OPTS.userTeam` のチームだけ。相手チームの盤は閲覧のみ。

**座標変換**（`baseToPct` / `pctToBase`）— COURT は 28m × 15m、RIM.z = 13.0。
検算: 攻撃トップ `{0, 7.0}` → 盤 (71.4%, 50.0%) / 攻撃左コーナー `{-6.85, 1.15}` →
(92.3%, 4.3%) / 守備トップ `{0, 5.2}` → (22.1%, 50.0%)。
範囲は `x` が ±7.0（コートのクランプと同じ）、`d` が 0.5〜13.5 に丸める。

## 配置ボードを「自チームの攻守トグル」へ / 役割編集をタブで分担

盤のトグルを **[試合] [攻撃] [守備]**（自チーム）に変更。`ui.pokerBoard: "match" | "off" | "def"`。

| タブ | 盤 | 編集できるもの |
|---|---|---|
| 試合 | 試合盤（先発10人） | **カードによる強化と選手交代のみ** |
| 攻撃 | 攻撃ベースの配置ボード | 攻撃ベース位置 / 攻ロール / オフェンス順位 |
| 守備 | 守備ベースの配置ボード | 守備ベース位置 / 守ロール |

- `rolePills(..., mode)` を追加。`"off"` なら攻ロール＋順位、`"def"` なら守ロールだけを出す。
- `playerCell` はタブから `pillMode` を決め、**試合タブではピルを出さない**
  （顔だけ＝カード配置と入れ替えに専念）。
- `formationBoard(ui, team, side)` は片側だけを描く。各アイコンの横にそのタブの
  役割ピルを並べ、**ドラッグの掴み所は顔だけ**にしてピルのタップと衝突させない。
- 相手は対になるベースを薄く重ねる（攻撃タブ→相手の守備 / 守備タブ→相手の攻撃）。
  ⚠️ 相手の座標は相手のリム基準なので `atk` を反転して同じ端へ置く。

## 配置ボード: 名前表示 / ピルの縦並び / ゾーン・マンマークの指定

**データと AI**
- `PlayerDef` に `defMode?: "man" | "zone"` と `markSlot?: number` を追加（`Player` へも引き継ぐ）。
- `ai/defense/index.ts`: 担当を `offense[d.markSlot ?? d.slot]` で引く。
  ⚠️ **相手のスロット番号**で持つので、相手が交代しても枠は変わらない
  （＝「そのポジションを見ろ」という指示になる）。
- `ai/defense/offball/index.ts`: `defMode === "zone"` なら `pickup` を常に false にして、
  担当を追わず守備ベース（シェル）を保つ。

**UI**
- 配置ボードの顔の下に **ポジション + 名前** を表示。
- `rolePills` の並びをタブ別に変更:
  - 攻撃タブ … 攻ロールの**下**にオフェンス順位（縦並び）
  - 守備タブ … 守ロールの**下**に「ゾーン/マン」、その下に「マーク相手」
  - 試合タブ … ピルなし（従来の横並びは他所から呼ばれた場合の保険として残す）
- `defAssignPills()` を追加。マーク相手はタップで 自動 → 相手PG → … → 相手C → 自動 と回る。
  ゾーンに切り替えると `markSlot` は消す（相手指定が要らないため）。

## 配置ボード: ピルの位置を顔の横へ戻す（自分が壊した点の修正）

**症状**: ロールとマーク設定が「顔の横」ではなく「名前の右端の横」に出ていた。
**原因**: 顔と名前を1つの列（`who`）にまとめ、その列の右へピルを並べていたため、
列の幅が**名前の長さ**で決まっていた。名前が長いほどピルが右へ流れる。

**修正**: 顔そのもの（26px）を基準点の要素にし、名前とピルを絶対配置でぶら下げる。
- 名前 … `top: 100%` + `translateX(-50%)` → 顔の真下
- ピル … `left: 100%` + `translateY(-50%)` → 顔の右
これで名前の長さに関係なく、ピルは常に顔の横に来る。顔の中心が基準点に一致する点も維持。

**マーク相手の位置**: 「ゾーン/マン」の**下**ではなく**横**へ（`line2` に2つ並べる）。
守備タブのピルは「守ロール」／「ゾーン・マン + マーク相手」の2段になる。

## CPU が相手を見て守備の割り当てを決める

`cpuAssignDefense(cpu)` を追加（`cpuAdjustSquad` と同じく CPU の手番ごと）。
⚠️ 自チーム（`POKER_OPTS.userTeam`）には触らない。ユーザーの指示が常に優先。

1. 相手5人の**脅威度**（`scoringPower` + 身長）と自分5人の**守備力**
   （守判断・敏捷・身長・反応）を出す
2. 脅威の高い相手から順に、守備力の高い選手を当てる（1対1の組み合わせ）
3. エースが2番手を `ACE_GAP`(0.12) 超で上回れば、**最も脅威の低い相手を見ていた選手を
   エースへ回す＝ダブルチーム**。空いた相手は誰も見ないが、それがダブルチームの代償
4. 守備力が担当の脅威を `ZONE_GAP`(0.18) 超で下回る組み合わせは `defMode = "zone"`。
   追いかけても振り切られるので面を守らせる

## ⚠️ 担当の引き方を `manOf` / `defenderOf` に一本化（バグ修正）

**症状**: `markSlot` を設定しても守備の挙動が変わらない。
**原因**: 担当を引くコードが **6か所**にばらけており（`ai/defense/index.ts` ×2、
`ai/attention.ts` ×3、`schemes/press.ts`、`schemes/zone.ts`、
`ai/offense/offball/index.ts`）、`ai/defense/index.ts` だけを直しても
**注目システムが `[p.slot]` で別人を追い続けていた**。守備者は「見えている担当の位置」
(`trackX/trackZ`) を守るので、そちらが旧担当のままだと指示が効かない。

**修正**: `ai/defense/shared.ts` に
- `manOf(game, d, offTeam?)` … その守備者が見る相手（`markSlot ?? slot`）
- `defenderOf(game, p)` … その攻撃選手に付いている守備者（逆引き）
を置き、上記6か所すべてを差し替えた。**担当を引く場所は今後ここだけにすること。**

**実測**（probe-mark、3試合、「slot i は相手 (i+1)%5 を見る」+ 1人ゾーン）
⚠️ 位置そのものは陣形の幾何に支配されて指標にならない（修正前後どちらでも同番号が近い）。
注目システムの追跡位置 `trackX/trackZ` が誰を指しているかで測る。

| | 修正後 |
|---|---|
| 追跡位置と「指示した相手」のズレ | **3.94m** |
| 追跡位置と「同じ番号の相手」のズレ | 4.80m |

指示した相手の方が近い＝割り当てが効いている。
**全体**（probe-blk2 16試合）: FG 52.8% / 得点 15.7 / リバウンド 5.0 で悪化なし。

## 観戦（CPU同士）の画面をプレイ時と同じ部品で組み直す

**症状**: CPU 同士の画面だけ、配置ボードも控えのトグルも VS ボードも無く、UI が食い違っていた。
**原因**: `renderWatch` がプレイ時の `renderPoker` とは**別の並び**で独自に組んでいたため、
その後に足した部品（VSボード・盤のトグル・配置ボード・控えの切替・カードの集約）が
一切入っていなかった。

**修正**: `renderWatch` を同じ部品・同じ並びで組み直した。
```
watchHeader（観戦固有: ラウンドと進行）
VSボード（戦力値の隣に役）
控えのチーム切替 + 控え8人
盤のトグル + 盤（試合盤 ⇄ 配置ボード）
カード（今打っている側だけ / 公開後は両方）
teamStrip ×2（ホーム・アウェイの役）
watchFooter（スキップ）
```
- 配置ボードに出すチームは**控えのトグルで選んでいるチーム**を流用（ボタンを増やさない）。
- 編集の可否は各部品が `POKER_OPTS.userTeam` を見て決めるので、観戦では自動的に閲覧のみになる。
  ⚠️ 観戦のために別の分岐を書かないこと。書くとまた同じ食い違いが起きる。

## 観戦とプレイのモーダルを1本化（分岐そのものを廃止）

前回は「観戦の並びをプレイ時に合わせる」までだったが、**関数が2本ある限り同じ食い違いが
再発する**ため、`renderWatch` を削除して `renderPoker` に統合した。

- `renderPoker` の中で `watching = POKER_OPTS.userTeam === null` と
  `view = POKER_OPTS.userTeam ?? m.home` を決め、**並びは1本**のまま中身だけ差し替える。
- 消した関数と行き先:
  - `renderWatch` … `renderPoker` に統合
  - `watchHeader` … `pokerHeader` に改名し、**両モードで常に表示**（ラウンドと進行）
  - `teamStrip`   … 役は VS ボードが出しているので廃止。
    「公開の決定権はホーム」だけ `pokerHeader` へ集約
  - `watchFooter` … スキップボタンを `pokerActionCol(watching)` の中へ移動
- カード欄の出し分けも1か所に集約:
  観戦 = 打っている側だけ（公開後は両方）/ プレイ = 相手の番なら伏せ札・それ以外は自分の手札。

⚠️ **今後もプレイヤー対CPUと CPU同士で別の描画関数を作らないこと。**
   違いは「操作できるか」だけで、編集の可否は各部品が `POKER_OPTS.userTeam` を見て決める。

## ⚠️ バグ修正: クォーター間に選手を入れ替えると得点が 0 に戻る

**症状**: 1Q の得点が 2Q へ移る際に 0 になる。手札公開の前後で起きるが、起きない時もある。

**真因**: `swapRosterSlots()` の中で `ui.onPrepare()` を呼んでいた（自分が入れた）。
`onPrepare` は `game.reset()` を含み、これは**試合まるごとのリセット**で
`score` / `qLine` / `quarter` / `gameClock` / `possession` まで初期化する。
つまり**クォーター間のポーカー画面で入れ替えが起きるたびに試合が 1Q へ巻き戻っていた**。

**間欠的だった理由**: 入れ替えが実際に起きた時だけ発火するため。
ユーザーが入れ替えた時に加え、`cpuAdjustSquad` の CPU 側の入れ替え（OVR 差 3 以上）でも起きる。

**修正**: `ui.onPrepare()` → `ui.game?.applyRoster()`。
`applyRoster` は `roster[t][i].applyDef(ROSTER[t][i])` で**既存の Player へ def を載せ直す**だけで、
スコアにもクロックにも触らない。コート上の5人は `roster[t][0..4]` なので、ROSTER を入れ替えれば
そのまま反映される（位置はそのまま引き継がれ、再配置も要らない）。

⚠️ **`onPrepare()` は試合開始時にだけ呼ぶこと。** 試合中に呼ぶと必ず巻き戻る。

### 他に得点を壊す経路が無いかの点検（結果: 無し）

`game.reset()` の呼び出しは4か所だけで、いずれも意図的:
| 場所 | 用途 |
|---|---|
| `game.ts:271` | コンストラクタ |
| `main.ts:84` (`ui.onRestart`) | HUD の RESTART ボタン |
| `main.ts:92` (`ui.onBack`) | 結果画面 → 試合前へ戻る |
| `main.ts:174` (`ui.onPrepare`) | 試合開始（`startMatch` からのみ） |

`score` / `qLine` へ代入するのは `reset()` だけ（他のヒットは別変数の `score` 名）。
ポーカー周りの経路も確認: `finishPokerRound` は `applyRoster` + `resumeFromPoker` のみ、
`awaitPoker` / `resumeFromPoker` は保留した継続を呼ぶだけ、`m.confirm()` は能力値しか触らない、
`announceHands` は `setEvent` のみ。**得点を壊す経路は修正した1か所だけだった。**

⚠️ クォーター終了の流れと症状の一致も確認:
`qLine` へ記録 → `awaitPoker(ended+1, 継続)` → ポーカー中に `reset()` が走ると
score/qLine/quarter が初期化され、**その後に保留の継続が `quarter = ended+1` を実行する**。
＝「得点が 0 になったが 2Q へは進む」という見え方になる。

**回帰テスト** `probe-swapscore`: 6分ぶん進めてから ROSTER を入れ替え + `applyRoster` し、
得点・クォーター・ラインスコアが不変であることと、入れ替えがコートに反映されることを確認。
```
入れ替え前: {"score":[15,20],"q":4,"line":[[5,4,6],[7,5,6]]}
入れ替え後: {"score":[15,20],"q":4,"line":[[5,4,6],[7,5,6]]}
OK
```

## ビッグ(PF/C)が3Pラインに立つ件

**まず事実**: シュート選択は問題なかった。
PF は試投の **97.3% がゴール下3m内 / 3Pは 2.7%**、C は 95.7% / 4.3%。
「3Pを優先して打っている」は**実測では起きていない**。問題は**立ち位置**だけ。

**真因**: `prefersPost()` が**アンカー1人しかポスト扱いしない**設計だった。
2人目のビッグは `false` に落ち、`homeSpotIdx` が自分のスロットのスポット
（3/4 = **コーナー3、リムから 6.95m**）を返す。つまり3Pが下手なビッグが
コーナー3に突っ立つ。「もう1人はショートコーナーへ」というコメントはあったが、
ショートコーナー(spot 6)は `prefersPost` の選手しか取れないので**到達しない**。

**入れた修正（3つ）**
1. `prefersPost`: 残るビッグも `rate(threeAcc) < STRETCH_THREE(0.80)` ならポスト扱い。
   外へ広げるのは**本物のストレッチビッグだけ**。
2. `bestOpenSpot`: ポスト役は外(0..4)のスポットを**取らない**（`continue`）。
   以前は `score` を減点するだけで、空きが無いと結局取れていた。
3. `OFF_BASE_DEFAULT[6]`（右ショートコーナー）を `x 6.2 → 4.2` へ。
   6.26m は3Pライン(6.75m)の直前で、スペーシングの押し出しで簡単に外へ出ていた。

**実測**（probe-big / probe-spotx、6〜8試合）

| | 前 | 後 |
|---|---|---|
| C: ポストのスポット(5/6)を持つ割合 | 53.0% | **90.0%** |
| PF: 同 | 52.2% | **66.9%** |
| PF: リムからの平均距離 | 7.48m | **6.90m** |
| PF: ゴール下3.5m内 | 30.1% | **35.3%** |
| PF: 3Pラインより外 | 43.1% | **38.0%** |
| C: 3Pラインより外 | 36.6% | 38.6% |

持ち場への到達は正確: spot5 は実際 |x| 2.08m に対し定義 2.03m、spot6 は 2.95m / 3.34m。
**割り当てられた post スポットには、ほぼその通り立てている。**

⚠️ **残っている分**: 3Pラインより外の 38% は、ポストのスポットを持っていない時間
（トランジション・スクリーン・カット、および残るペリメーター割り当て）。
ここは未分類。分類用の計装が途中で動かず、内訳を取れていない。

⚠️ 計測の落とし穴: 最初「アーク付近 = リムから 6.25m 超」で測っており、
**ショートコーナー(6.26m)自体が該当**して数字が膨らんでいた。3Pライン(6.75m)で測り直した。

**全体**（probe-blk2 16試合）: FG 54.1% / 得点 16.1 / リバウンド 4.8。
